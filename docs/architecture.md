# Architecture

Vite serves and bundles the TypeScript React presentation. React Router supplies four explicit placeholder routes and an unknown-route fallback. The Tauri command bridge exposes `health_check`, read-only coarse `startup_status`, and the narrow argument-free `request_first_time_setup` operation. Rust constructs their non-sensitive responses, owns the desktop runtime plus startup and setup lifecycles, and exposes no broader database or filesystem authority through IPC.

React may render UI, navigate placeholders, and request health. Privileged operations belong in Rust. React must never directly access a future database, filesystem, encryption key, or privileged service.

The current structure is deliberately small: `src/` contains shell code and frontend tests; `src-tauri/` contains Tauri configuration, one capability, Rust entry points, the command, Rust-owned storage and database trust-boundary foundations, the Windows production SQLCipher dependency, a private Windows-only raw-key application primitive, and Rust tests; `docs/` contains constraints; and `.github/workflows/ci.yml` contains Windows-only validation.

Rust owns production path resolution. Tauri's Rust-side local application-data resolver supplies the application-owned per-user directory associated with `io.github.cltubigon.churchapp`; Rust appends the single fixed filename `parish-data.db`. Production, development, automated-test, and restore-staging paths have distinct wrapper types and separate resolvers. Development uses an explicitly non-production identity, automated tests require a unique safe test identifier beneath an injected temporary root, and restore staging cannot be used as an active production path.

No Tauri command accepts or returns a database or filesystem path. React cannot select, override, supply, or receive the production location. During ordinary startup, Rust resolves the application-owned root, performs canonical installation-state observation, runs the guarded read-only production database trust chain over existing artifacts, re-observes installation state, performs startup authorization, and then performs operational activation. `Ready` is installed only with the resulting real `OperationalProductionDatabase`. Ordinary startup does not create storage, perform setup, migrate, repair, recover, reset, or fall through to a retry or silent initialization path. Explicit first-time setup is a separate Rust-owned path reached only through its narrow request command and accepted `NeverInitialized` authorization.

Canonical persistence naming remains in `storage_foundation`: the existing `PRODUCTION_DATABASE_FILENAME` continues to own `parish-data.db`, while crate-private constants own `parish-data.db.stage`, `installation-evidence`, and the exact active and staged key/evidence wrapper names. Crate-private redacted wrapper types represent the evidence directory, active and staged wrappers, and staged database. One aggregate joins all paths beneath a caller-supplied synthetic root only. Evidence wrappers are always beneath `installation-evidence`; database publication staging is directly beneath the root and remains distinct from the existing restore-staging path. This construction does not call a Tauri resolver or any filesystem API.

`installation_evidence_persistence` adds a bounded helper over an already-supplied `std::io::Read`. It rejects lengths below 15 or above 65,550 before exact-size allocation, reads exactly the validated length with safe `Interrupted` retries, reports premature EOF coarsely, and performs one bounded single-byte read to reject trailing or growth data. It retains no native I/O error and exposes neither contents nor observed lengths through `Debug`; it does not parse, authenticate, or locate a wrapper.

The same module models path-free persisted-presence facts. A production root is validated, unexpectedly typed, or unavailable; each fixed file is absent, a regular file, unexpectedly typed, or unavailable; and the evidence directory is absent, a validated directory with four fixed child facts, unexpectedly typed, or unavailable. The pure classifier gives unavailable facts highest precedence, then unexpected types, then any staging, followed by complete active, external evidence without database, partial active, inconsistent database-only, and conclusive clean absence. `CleanAbsenceCandidate` grants no setup authority, and an absent evidence directory means only that its four fixed children are conclusively absent in the supplied facts.

Pure publication state machines encode three candidate workflows. Initial publication stages database, key, then evidence; reload-verifies all; publishes database, key, then evidence. Evidence-only replacement stages and reload-verifies evidence before replacing it. Key-generation replacement stages key then evidence, verifies both, and replaces key then evidence. Evidence is always the final success boundary. Refusal, staging failure, verification failure, publication failure, and interruption are terminal values; no cleanup, retry, resume, rollback, backup, timestamp, enumeration, or winner-selection transition exists. Every later attempt must begin from fresh supplied presence facts.

These persistence facts and publication states do not construct or convert to `installation_state::InstallationEvidence`. They establish no authentication, database correspondence, startup eligibility, setup authority, operational state, or real persistence behavior.

The crate-private Windows adapter is `installation_evidence_persistence/windows_filesystem.rs`, registered only by `#[cfg(windows)]`. Its accepted binding checks remain, and Windows tests now exercise one authentication-key-wrapper path beneath a unique test-owned temporary root. The sequence validates the opened `installation-evidence` directory, creates the exact stage name with `CREATE_NEW` and zero sharing, transfers the handle to one owner, writes exact bytes, calls `FlushFileBuffers`, closes, independently reopens with read-only sharing, obtains handle-reported size, uses the existing bounded reader, re-queries size, byte-compares, and calls the canonical protected-wrapper parser for object kind 1. It then calls `MoveFileExW(stage, active, MOVEFILE_WRITE_THROUGH)` without replacement or copy fallback, independently reopens and repeats active-file verification, confirms stage absence, and preserves an unrelated sentinel.

The separately scoped existing-file replacement proof remains inside that adapter's existing `#[cfg(test)]` module. It first creates the old active wrapper through the accepted no-replace proof and creates the replacement stage with the same create-new/write/flush/close discipline. Active and stage are independently reopened with restrictive read sharing and validated as regular non-reparse disk files, exact fixed normalized volume-GUID children, bounded exact bytes, and canonical authentication-key wrappers of kind 1. Handle information records both file IDs and volume serials; the evidence directory and both leaves must agree on volume and exact normalized directory. All ordinary active, stage, and directory handles are closed before `ReplaceFileW(active, stage, NULL, 0, NULL, NULL)`.

After the single call returns, pre-call assumptions are discarded. The proof freshly opens only the exact active and stage names and classifies absence, canonical old bytes, canonical replacement bytes, malformed or unexpected bytes, unexpected entry type, and unavailable inspection. The small test-only observation classifier distinguishes ordinary success, unchanged old-active/new-stage state, active-absent/new-stage partial state, reported-failure-but-completed state, and unexpected/unavailable state. A successful return is insufficient by itself: fresh active replacement bytes and canonical validity, exact final path, stage absence, stage-to-result file-ID continuity, old-active/result identity distinction, and sentinel preservation are all required. Deliberate sharing blockers are closed before fresh inspection, and the call is never retried.

The loader contract is bounded, stable selection and validation of the current active wrapper pair. It does not promise detection of every transient historical disappearance/recreation event when that history is not distinguishable through its existing observations. The corrected Windows test `second_file_disappearance_is_rejected_and_replacement_never_returns_stale_pair` requires disappearance without replacement to return `Err`; after replacement the loader may return `Err`, or it may succeed only with the fully validated current recreated pair. Success with a stale or mixed pair is forbidden. The test-only correction changed no production-loader bytes and does not weaken production security.

Opened directories and files must report `FILE_TYPE_DISK`; files must not be directories or reparse points. Normalized final paths use `VOLUME_NAME_GUID`, reject UNC forms, and must equal the validated evidence-directory final path plus the exact fixed stage or active filename. Stage and active parents are also required to be the same typed test directory. This narrows risk in the proof but does not establish complete reparse, junction, path-race, or TOCTOU resistance.

The normal-tree hardening proof is a separate Windows-test-only inspection sequence. It opens the operating-system temporary-directory anchor, the unique root, one ordinary intermediate, and the exact `installation-evidence` directory independently with `OPEN_EXISTING`, backup semantics, and open-reparse-point semantics, then retains all four handles while validating an active or staged wrapper. Each directory must be a non-reparse disk directory with a zero tag, a normalized volume-GUID final path, an exact component boundary, and a `FILE_ID_INFO` identity. Directory identities are queried before and after the leaf read while the handles remain live.

For this proof, preferred identity is the 64-bit `FILE_ID_INFO.VolumeSerialNumber` plus all 128 bits of `FILE_ID_INFO.FileId`; the older file-index representation remains only in the unchanged publication/replacement regression code. UTF-16 final-path validation requires the `\\?\Volume{GUID}\` form, rejects UNC and malformed or oversized output, folds ASCII case only in the GUID hex digits, and compares every application-owned component with exact ordinal spelling. Parent and leaf volume serials and volume-GUID prefixes must agree.

Protected active and staged wrapper leaves alone require exactly one link. A leaf snapshot contains identity, size, attributes, reparse tag, link count, and final path before the existing bounded reader and canonical kind-1 parser run; every fact is queried again on the same handle afterward and any change fails closed. The deterministic policy seam covers changed identities, links, paths, volume facts, and unavailable reinspection without creating hostile filesystem objects. No directory or database hard-link rule is introduced.

This normal-tree evidence does not remove the remaining path-based publication races. The accepted initial-publication and replacement APIs and their handle-closure rules are unchanged; the hard-link and controlled exact-target substitution fixtures add only test evidence, while hostile reparse, broader directory substitution, local-volume, and hot-plug policy remain separate future work.

The Windows-test-only hard-link fixture extends that evidence without adding a callable production operation. Separate active and staged cases create one canonical kind-1 wrapper beneath `synthetic-component/installation-evidence`, observe one initial link, and use only `std::fs::hard_link` to create `wrapper-hard-link-alias.synthetic`. The approved name and alias must share the complete volume serial and 128-bit file ID plus byte-identical canonical content while retaining distinct normalized final paths. Exact-name validation rejects the alias, and the existing hardening path rejects the approved name at two links before its injected mutation or bounded-reader closures can run. Removing only the alias must restore the approved name to one link with unchanged identity and bytes. No publication or replacement function runs in these cases.

The separate Windows-test-only evidence-directory substitution fixture targets only `ordinary-component/installation-evidence`. It retains the temporary-directory anchor, unique root, intermediate, and a read-access evidence-directory handle opened without delete sharing. With wrapper and temporary inspection handles closed, `std::fs::rename` refuses to move the target and fresh inspection confirms unchanged full identity, exact normalized path, candidate identity/path, independently bounded canonical wrapper bytes, retained ancestors, and sentinel. The target and every descendant or duplicate handle then close while the temporary/root/intermediate handles remain retained. Exactly two synthetic renames move the original to `evidence-displaced.synthetic` and `substitute-candidate.synthetic` into exact `installation-evidence`.

Fresh post-substitution opens show the displaced directory retains the saved original full identity and exact displaced path, while the exact evidence path has the saved candidate identity and a byte-identical canonical authentication-key wrapper. The exact-path identity differs from the saved original identity, so identity mismatch is decisive despite exact path and equal bytes. The test-only continuity classifier reports substitution and does not invoke its publication, replacement, or wrapper-mutation continuation. This is one controlled case only: residual path-based races, hostile concurrency, intermediate/root substitution, and production enforcement remain unresolved.

The retained evidence-directory replacement compatibility proof is another Windows-test-only case. After creating the old active through the accepted initial-publication path and the stage through create-new/write/flush/close, it opens only the exact evidence directory for retention with `GENERIC_READ`, `FILE_SHARE_READ | FILE_SHARE_WRITE`, no delete sharing, `OPEN_EXISTING`, backup semantics, and open-reparse-point semantics. The synthetic parent is observed only long enough to validate the directory's exact component and volume relationship; no temporary, root, intermediate, active, or staged handle spans replacement. Active and stage are independently validated by the accepted preflight, and every leaf handle closes before the call.

The same retained handle is queried immediately before and immediately after the unchanged `ReplaceFileW(active, stage, NULL, 0, NULL, NULL)` call and again after fresh exact-name child inspection. Each boundary requires unchanged full directory `FILE_ID_INFO`, normalized volume-GUID final path, disk-directory and reparse facts, exact component relationship, and saved-parent volume relationship. Fresh child inspection runs while that parent handle remains open and reuses the existing replacement classifier; normal completion still requires canonical new bytes at active, stage absence, exact active path, staged/result identity continuity, and old/result identity difference. This narrows ordinary same-operation directory-substitution exposure but does not make the child operation handle-relative or remove child-entry, cross-process, or other TOCTOU races.

The local-volume candidate proof is private to the existing Windows test module. It starts from one retained, disk-backed, non-reparse directory handle for a unique test root and reuses the bounded `FILE_NAME_NORMALIZED | VOLUME_NAME_GUID` query and strict GUID-prefix validation. The exact first 49 UTF-16 units form the derived `\\?\Volume{GUID}\` root; one terminating NUL is appended only for one `GetDriveTypeW` call. Coarse path and drive facts yield only candidate, rejected, or unavailable outcomes. Fixed plus strict GUID evidence produces only `LocalFixedCandidate`; it has no conversion into installation, setup, startup, persistence, publication, replacement, or database authority.

The private nested device-property proof supplements that prerequisite without changing it. It retains the validated test-root handle, removes exactly the final separator from the strict 49-unit root, appends one NUL, and opens that volume device once with desired access zero, read/write sharing, `OPEN_EXISTING`, and flags zero. It submits one fully initialized `StorageDeviceProperty`/`PropertyStandardQuery` first into an exact `STORAGE_DESCRIPTOR_HEADER` buffer and then into an exactly sized zeroed buffer capped at 65,536 bytes. The parser reads raw byte slices only: exact compiled layout version, stable bounded size, the removable byte, bus type, and bounds for nonzero string offsets that are never followed. It performs no structure cast and decodes or reports no device string or identifier.

Only accepted `LocalFixedCandidate`, a strict non-removable descriptor, and an approved candidate bus family can yield private non-authoritative `DevicePropertyCandidate`. Removable media rejects; virtual, file-backed virtual, iSCSI, Fibre, and Spaces remain unresolved; USB, 1394, SD, MMC, SCSI, and RAID require controlled-host review; every other value fails closed. The proof performs exactly two storage-property IOCTL calls and no hot-plug query. Driver-reported device properties do not establish device non-removability, non-hot-plug or surprise-removal behavior, internal placement, physical locality, absence of virtual/profile/remote backing, durability, or production suitability. It does not resolve Church App production storage or convert into any authority-bearing type.

Only Windows tests create filesystem state. Each test creates exactly one unique root beneath the operating-system temporary directory, its required synthetic children, one sentinel, and the files needed by that case. Successful tests remove that exact root; failure unwinding attempts at most one best-effort root-only removal without replacing the original failure. This teardown is not production stale-stage cleanup or repair.

Rust also owns a narrow initialization decision boundary. `InstallationEvidence` distinguishes never initialized, initialized with expected storage present or missing, inconsistent evidence, and unavailable evidence. Ordinary startup supplies no setup authorization: never-initialized remains setup-required, initialized-and-present indicates only future open eligibility, and initialized-but-missing, inconsistent, or unavailable evidence fails closed.

First-time setup authorization is a separate in-memory Rust transition with a private-field authorization type. It can be produced only after the dedicated transition accepts never-initialized evidence; raw frontend values cannot construct it. Installation evidence, setup authorization, and future storage opening remain separate. This stage does not inspect the real filesystem, persist evidence, perform setup, resolve a production path during startup, or create or open storage. No frontend or Tauri IPC operation has setup authority.

Rust now also owns a platform-neutral logical installation-evidence contract. Its fields are grouped by purpose:

- authoritative identity inputs for future cross-checking: permanent application identifier, application database-format identity, and immutable parish identifier;
- cross-check fields: installation identifier, database-key generation identifier, and setup publication identifier;
- lineage fields: installation generation and recovery or replacement generation, both with 1 as the initial valid committed generation and zero rejected;
- non-authoritative format and metadata fields: evidence-format identity, evidence-format version, and creation timestamp represented as nonzero UTC seconds since the Unix epoch.

The raw-to-typed constructor validates only current format identity and version, canonical application and database-format identities, canonical parish-identifier syntax, nonzero fixed-size opaque identifiers, nonzero generations, and a nonzero timestamp. A `StructurallyValidatedInstallationEvidence` value therefore means only structurally valid and locally coherent under the current contract. It is explicitly distinct from future authenticated evidence, future database-cross-checked evidence, and the current operational `installation_state::InstallationEvidence`. There is no conversion between those types in this stage.

Serialization is limited to one canonical version-1 plaintext encoder whose only input is `StructurallyValidatedInstallationEvidence`. It returns a fixed 164-byte value: magic at `0..8`, encoding version at `8..10`, payload length at `10..12`, evidence-format identity at `12..28`, logical evidence-format version at `28..30`, application-identifier length at byte `30`, the exact application identifier at `31..60`, database-format identity at `60..76`, parish identity at `76..92`, installation identity at `92..108`, installation generation at `108..116`, recovery or replacement generation at `116..124`, database-key generation identity at `124..140`, setup publication identity at `140..156`, and creation timestamp at `156..164`. The payload length is 152 (`00 98`). All multi-byte integers are unsigned big-endian, and identities are raw 16-byte values. There is no padding, reserved area, extension area, or trailing data.

The encoder writes each field explicitly and deterministically. It does not authenticate bytes, establish operational authority, or convert the structural contract into `installation_state::InstallationEvidence`.

The parsing flow is `&[u8]` raw bytes → strict version-1 parser → `ParsedUntrustedInstallationEvidenceContract` → explicit structural validation → `StructurallyValidatedInstallationEvidence`. The parser rejects any total length other than 164 before field decoding, then checks the exact eight-byte magic, unsigned big-endian encoding version 1, unsigned big-endian payload length 152, application-identifier length 29, and valid UTF-8 in the fixed 29-byte application-identifier field. It decodes every remaining field from its fixed offset without padding, extension, trailing-byte acceptance, scanning, guessing, fallback, or normalization.

The approved outer authenticated-envelope framing is exactly 226 bytes: `CHEVAUTH` magic at `0..8`; unsigned big-endian envelope version 1 at `8..10`; unsigned big-endian authentication algorithm identifier 1 at `10..12`; a distinct nonzero 16-byte evidence-authentication-key generation identifier at `12..28`; unsigned big-endian plaintext length 164 at `28..30`; the exact unchanged canonical plaintext at `30..194`; and a complete 32-byte HMAC-SHA-256 tag at `194..226`. The authentication-input prefix is exactly `0..194`. There is no nonce, salt, padding, reserved field, extension area, negotiation, fallback, or trailing data.

Construction accepts only an existing canonical `EncodedInstallationEvidence`, a nonzero authentication-key generation identifier, and a caller-supplied `EvidenceAuthenticationKey`. It writes the fixed prefix, computes `Hmac<Sha256>` over exact bytes `0..194`, and copies the complete 32-byte tag to `194..226`. Trusted construction can produce both the encoded envelope and a private `CryptographicallyAuthenticatedEnvelopeV1`.

Verification follows `&[u8]` raw bytes → strict framing parser → `ParsedUntrustedAuthenticatedEnvelopeV1` → HMAC verification → `CryptographicallyAuthenticatedEnvelopeV1`. The parsed untrusted type retains the exact prefix bytes, and verification uses those bytes without parsing, reconstructing, or normalizing the inner plaintext. The cryptographically authenticated type retains the HMAC-covered generation identifier but cannot release plaintext. Only a successful generation-identifier match produces `GenerationMatchedAuthenticatedEnvelopeV1`, which can release the exact 164 bytes to the existing plaintext parser. That parser still returns `ParsedUntrustedInstallationEvidenceContract`, and structural validation remains a later explicit transition. No conversion to operational `installation_state::InstallationEvidence` exists.

The separate `EvidenceAuthenticationKey` type owns exactly 32 bytes passed into its Rust ownership boundary, including bytes moved from the production generator. It is distinct from the evidence-authentication-key generation identifier, database-key generation identifier, and authenticated-envelope framing. Its private bytes are exposed through a crate-private closure only long enough to initialize HMAC state; authenticated results retain no key.

Authentication-material generation is a separate crate-private transition: `getrandom::fill` requests exactly 32 bytes into a caller-owned buffer that moves directly into `EvidenceAuthenticationKey`, then a separate `getrandom::fill` requests exactly 16 bytes for the existing `EvidenceAuthenticationKeyGenerationIdentifier` validation boundary. An all-zero identifier is retried with a fresh identifier-only fill, with three total identifier attempts. Randomness failure or three zero identifiers fails closed. The redacted result owns only the key and identifier and has no production caller. Generation does not invoke HMAC construction and remains separate from DPAPI, persistence, setup, startup, database cross-checking, active-generation policy, and operational installation state.

Malformed encoding returns `InstallationEvidenceParseError`; well-framed but logically invalid fields parse successfully and later return `ContractValidationError` through the existing validation rules. Parsed fields are private and remain untrusted. Neither parsed nor structurally validated evidence converts to operational `installation_state::InstallationEvidence`.

Dependency-free deterministic malformed-input tests exercise every byte position, framing and field boundaries, wrong lengths, representative byte patterns, and selected two-byte framing changes. Any mutated input that parses and remains structurally valid must re-encode byte-identically. This is testing evidence for parser behavior and canonicality, not authentication or a security proof.

Windows in-memory protection adds one outer wrapper for opaque DPAPI output. Version 1 is exactly a 14-byte header plus the blob: `CHDPAPI\0` at `0..8`, wrapper version 1 at byte `8`, protected-object kind at byte `9` (`1` for authentication material and `2` for authenticated evidence), an unsigned big-endian `u32` blob length at `10..14`, and the exact opaque bytes at `14..`. The blob must be nonempty and no longer than 65,536 bytes; the total wrapper is therefore capped at 65,550 bytes. Parsing requires an exact declared length and rejects unknown versions, kinds, mismatches, and trailing data before allocation.

The protected authentication-key plaintext is exactly 49 bytes: payload version 1 at byte `0`, the nonzero 16-byte evidence-authentication-key generation identifier at `1..17`, and the exact 32-byte key at `17..49`. It has no magic, key-length field, padding, reserved area, or extension data. Key payload and authenticated evidence are protected separately.

The recovery flow is key first: validate the key wrapper, DPAPI-unprotect candidate key bytes, validate the fixed payload, then validate and DPAPI-unprotect the evidence wrapper. Strict envelope framing and HMAC verification precede comparison of the HMAC-covered generation identifier with the identifier recovered inside DPAPI. Only `GenerationMatchedAuthenticatedEnvelopeV1` can release plaintext to the existing parser; structural validation remains a separate later transition.

All DPAPI unsafe code remains confined to the Windows current-user DPAPI adapter. It hard-codes UI-forbidden current-user calls with null entropy, description, reserved, and prompt pointers. Successful native output is validated and copied before release; unprotected native output is best-effort zeroized before its single `LocalFree`. Filesystem unsafe code is separately confined to the Windows filesystem adapter. Neither boundary maps results into setup, startup, IPC, frontend, database, or installation-state authority.

Authenticated-envelope hardening deterministically mutates every one of the 226 byte positions and classifies strict framing failures separately from HMAC authentication failures. Header mutations follow the existing framing rules; framing-preserving authentication-key generation identifier and plaintext mutations must fail authentication, and every terminal-tag mutation must parse as framing and then fail authentication. Wrong-key, wrong-length, pattern, boundary, and selected cross-boundary two-byte corpora exercise the same chain without randomness or side effects.

DPAPI malformed-input hardening deterministically covers every byte of the fixed wrapper header and protected-key payload, bounded declared-length, truncation, trailing-data, pattern, boundary, fake-protector, and opaque-blob corpora, plus first, midpoint, and final-byte corruption of real protected key and evidence blobs. DPAPI-unprotected output remains candidate data until strict payload or envelope framing, HMAC, generation, plaintext, and structural validation succeeds. Parsing a protected-key payload establishes only candidate key material, not HMAC authority. These tests do not change either protected format, any production API, or the established trust boundaries.

Tests also deliberately recompute valid tags over malformed inner plaintext. Those envelopes authenticate successfully but then fail the existing plaintext parser or structural-validation boundary, demonstrating that authentication and logical validity are separate. An alternate canonical plaintext completes authentication, parsing, structural validation, and byte-identical re-encoding. This hardening provides neither replay nor rollback protection.

The contract and HMAC transitions themselves perform no randomness, persistence, loading, filesystem or registry access, DPAPI, database access, startup integration, setup authorization, or Tauri IPC. The separate in-memory DPAPI layer wraps the authenticated envelope. Database-first and evidence-last multi-file publication ordering remains represented only by the pure state machines; the temporary runtime proof covers only one wrapper and must not be treated as that sequence.

Future encrypted local parish data is intended to be authoritative, while central services are non-authoritative. The future public web application and canonical contracts are separate repositories. No interfaces, ports, adapters, schemas, clients, or placeholders for those systems exist here.

Production installation-evidence observation and trusted loading, production root resolution, guarded read-only database opening, database/evidence correspondence, freshness validation, startup authorization, operational activation, explicit first-time setup orchestration, encrypted database creation, minimal V1 metadata/bootstrap initialization, and Rust-owned lifecycle composition are implemented as described below. Migration, parish/business schema, replacement, user authentication, recovery, backup/restore, product workflows, and broader operational database interfaces remain deferred or absent. Monotonic rollback protection, supersession handling, and the unresolved production path and race controls described below also remain separate work. The temporary proofs establish visible correctness for one synthetic wrapper on the observed Windows filesystem, not directory-entry or volume flush durability after power loss, ACL or metadata preservation, named-stream behavior, backup, cross-volume behavior, complete path-race resistance, or runtime replacement detection. Special failure-family simulation proves only deterministic classifier behavior, not occurrence of those native states on the host.

## Current-account baseline and USB-flash controlled host harness

The private `controlled_storage_host_matrix` module is wholly beneath the existing Windows `#[cfg(test)]` boundary. It supports exactly `CurrentWindows11InternalBaseline` and `UsbFlash`. The baseline creates its exact test-owned child beneath the operating-system temporary directory. The USB case accepts only the manually supplied `CHURCH_APP_USB_TEST_ROOT`; it performs no drive discovery, enumeration, guessing, or current-directory inference.

Both cases open and retain the hardened exact test-root handle, reuse the accepted handle-derived volume-GUID evidence and local-volume/device-property classifiers, and release the handle before one-attempt exact-root cleanup. This is test orchestration only. It has no production caller, arbitrary-root production function, command, setup/startup integration, persistence, database, IPC, frontend, or policy-authority conversion.

After the manually supplied USB case failed before its first drive-type query, the private controlled-host module gained a first-failed-stage diagnostic that mirrors the accepted directory helper without changing it. The diagnostic preserves the helper's actual native order: attribute/tag information is queried before standard-fact conversion, and identity, link, and normalized-path facts are queried before directory, reparse, and strict GUID policy checks. It adds no runtime native operation; the repeated disk and reparse confirmations remain.

The accepted diagnostic rerun reached exact-directory open, the disk-handle fact, and the standard-information query, then stopped fail-closed when `FILE_ATTRIBUTE_TAG_INFO` was unavailable (`DirectoryAttributeInfoUnavailable`, disposition `Unavailable`). No drive-type or device-property query followed. This observation does not establish a filesystem type, hardware cause, or general USB incompatibility, and it does not justify relaxing the accepted attribute/tag, reparse, identity, or normalized-path boundary. The shared hardening checks remain unchanged.

## Approved production database architecture, guarded handoff, and validation

Carlo approved this bounded architecture package as the canonical production database foundation. The exact Windows production dependency, private raw-key application primitive, metadata-only production database-file inspection, guarded read-only SQLCipher connection handoff, consuming validation chain, internal startup authorization, operational activation, Rust-owned application lifecycle, explicit first-time setup, minimal V1 metadata/bootstrap schema, migration opportunity/revalidation and exclusivity, full-integrity migration source, verified encrypted migration-backup stage, pure recovery-envelope primitive, private recovery-envelope backup verification, C1 Migration Recovery Key Custody Format/private state foundation, private C2a Windows native adapter, C2b lifecycle/main-thread/real-main-window custody dispatch, first-set database, envelope, and manifest-last publication with independent per-artifact fresh verification, recovered-key verification, exact three-entry layout verification, independent first complete-recovery-set verification, and second-set database and recovery-envelope publication with fresh verification against the original retained source are implemented. C2b remains private, unreachable through normal product interaction, and manually unvalidated. Existing installation-evidence contracts and protections remain unchanged. The lifecycle uses the real operational owner for `Ready`, orderly close, and the implemented migration custody path. Migration execution, parish/business schema, operational portable recovery, complete durable Layer D publication, restore, recovery, replacement, retention/deletion, authentication, parish workflows, and a general frontend database-operation surface remain absent.

The layers remain deliberately separate: canonical installation-state observation; connection-lifetime guarding; trusted key recovery; guarded connection opening; raw-key application; SQLite/SQLCipher readability and integrity validation; live metadata/header observation and decoding; correspondence; freshness; final canonical installation-state observation; startup authorization; operational activation; lifecycle ownership; and separately authorized setup, migration, rekey, restore, recovery, replacement, and destructive cleanup. No earlier value or stage grants later authority. In particular, application authorization, filesystem observation, storage integrity, and operational activation remain distinct concepts; activation is a post-authorization transition, not another authorization step.

SQLCipher Community Edition is the selected engine for production parish databases, production backups, and production recovery databases; plain SQLite is prohibited for those artifacts. Windows production depends on exactly `rusqlite = { version = "=0.39.0", default-features = false, features = ["backup", "bundled-sqlcipher-vendored-openssl"] }` under `[target.'cfg(windows)'.dependencies]`, without a Windows `rusqlite` development dependency or direct `libsqlite3-sys` dependency. The earlier `sqlcipher_windows_feasibility` module and former development-dependency state remain historical test-only evidence. Pinned SQLCipher/OpenSSL identities and the lockfile remain release evidence.

The private Windows-only `sqlcipher_database_key_application` module still owns only the raw-key submission transition. The guarded handoff is its sole application-level caller and passes only `&GenerationBoundDatabaseKey` plus the already-open connection handle. The primitive encodes the key in one fixed 67-byte owner as lowercase `x'<64 hex characters>'`, calls `sqlite3_key` exactly once, treats only `SQLITE_OK` as success, best-effort clears its owned encoded bytes, and collapses failure to `DatabaseKeyApplicationError::Failed`. Neither the primitive nor the handoff exposes a public command, frontend, or IPC surface.

The exact ownership chain is `ProductionDatabasePath` -> `InspectedProductionDatabaseFile` -> private `ConnectionLifetimeWriteGuard` -> `rusqlite::Connection`, with `GenerationBoundDatabaseKey` borrowed only for the one key application, followed by an opaque keyed-but-unvalidated owner, `ReadabilityAndIntegrityValidatedProductionDatabaseConnection`, `LiveMetadataAndHeaderValidatedProductionDatabaseConnection`, `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection`, `DatabaseFreshnessValidatedProductionDatabaseConnection`, `StartupAuthorizedProductionDatabaseConnection`, and post-authorization `OperationalProductionDatabase`. Each owner retains the inspected proof, guard, and connection as one lifetime unit; the correspondence, freshness, and startup-authorized owners additionally retain one owned `DatabaseMetadataContractV1` and one owned `TrustedCurrentInstallationEvidenceAssessment`. The freshness owner retains no normalized observation or anchor. No guard handle, file, path, identity, content-read API, raw Windows handle, raw SQLite handle, or database-content capability escapes. Each transition establishes only its named authority; operational activation is distinct from startup authorization.

The exact implemented order is:

1. Receive the existing successful metadata-only `InspectedProductionDatabaseFile` proof.
2. Acquire the private `ConnectionLifetimeWriteGuard` with `FILE_READ_ATTRIBUTES | FILE_READ_DATA`, `FILE_SHARE_READ`, `OPEN_EXISTING`, and `FILE_FLAG_OPEN_REPARSE_POINT`.
3. Identity-match the guard to the inspected database proof.
4. Perform exactly one application-level path-based `Connection::open_with_flags_and_vfs(..., "win32")` call using `SQLITE_OPEN_READ_ONLY | SQLITE_OPEN_FULL_MUTEX | SQLITE_OPEN_PRIVATE_CACHE | SQLITE_OPEN_NOFOLLOW` and no read-write, create, shared-cache, no-mutex, memory, or URI flag.
5. Retrieve SQLite's borrowed main-database Windows handle through `SQLITE_FCNTL_WIN32_GET_HANDLE` and identity-match it to the same inspected proof.
6. Apply the pre-key policy: five-second busy timeout; extension loading disabled; defensive mode enabled; trusted schema disabled; no-checkpoint-on-close enabled; attach-create and attach-write disabled; and the main database confirmed read-only.
7. Apply the existing `GenerationBoundDatabaseKey` exactly once.
8. Enable and verify query-only only after key application.
9. Return the opaque keyed-but-unvalidated owner.

The connection, guard, and inspection proof form one lifetime unit. They remain together after a normal close failure or a construction-time close failure, and the trust-chain APIs expose only consuming close or close-retry transitions. The Rust-owned startup worker is the operational caller; no direct frontend database caller exists.

The supported production baseline is Windows 10 x64 and Windows 11 x64, local NTFS, a non-elevated standard user, and CurrentUser DPAPI. Windows ARM64, 32-bit Windows, ReFS, removable and network storage, Wine, Windows Server, and compatibility layers remain unsupported until separately approved.

### Key separation

The database key is an independent random 256-bit value generated from the Windows OS cryptographic RNG. It is never password-derived or derived from parish, installation, evidence, device, or user values, and it is never shared with the evidence HMAC key. Its owner is non-`Copy`, exposes no unrestricted `Clone`, redacts formatting, and best-effort zeroizes owned buffers. A dedicated CurrentUser-DPAPI wrapper protects the live database key. Portable recovery instead uses a separate random recovery key and authenticated recovery envelope. C1 and private C2a exist, but operational duplication, physical separation, rotation, loss handling, and destruction remain explicit future operator/security responsibilities.

### Metadata version 1

The singleton table is named `church_app_database_metadata` and contains exactly one canonical, non-null row. Missing, duplicate, partial, wrong-storage-class, wrong-length, unsupported, or malformed state fails closed.

| Field | Approved SQLite representation and rule |
| --- | --- |
| `singleton_id` | `INTEGER`; canonical value 1 |
| `metadata_contract_version` | positive `INTEGER`; initial value 1 |
| `database_schema_version` | positive `INTEGER`; initial value 1 |
| `permanent_application_identifier` | bounded canonical UTF-8 `TEXT`; exactly `io.github.cltubigon.churchapp` |
| `database_format_identity` | existing typed `ApplicationDatabaseFormatIdentity`; `BLOB`; exactly 16 bytes; direct typed decoding; exact typed equality; never text |
| `parish_identifier` | `BLOB`; exactly 16 bytes |
| `installation_identifier` | `BLOB`; exactly 16 bytes |
| `installation_generation` | `BLOB`; exactly 8-byte big-endian encoding |
| `recovery_replacement_generation` | `BLOB`; exactly 8-byte big-endian encoding |
| `database_key_generation_identifier` | `BLOB`; exactly 16 bytes |
| `setup_publication_identifier` | `BLOB`; exactly 16 bytes |
| `database_created_at` | non-negative UTC Unix milliseconds in `INTEGER`; diagnostic only |

The database-format identity is never UTF-8, hex, UUID text, Base64, a numeric reinterpretation, alternate spelling, hash, prefix, or fingerprint. SQLite `application_id` is `0x43484150`. SQLite `user_version` mirrors `database_schema_version`, while metadata remains authoritative. A wrong fixed application ID, unsupported correctly represented metadata version, malformed representation, and supported metadata/user-version disagreement have distinct fail-closed categories under the implemented live-observation boundary; no state is auto-repaired.

### Correspondence, freshness, and authority

Exact typed equality is required across database and evidence for the permanent application identifier, `ApplicationDatabaseFormatIdentity`, parish identifier, installation identifier, database-key generation identifier, and setup-publication identifier. The identity-only correspondence boundary ignores installation generation and recovery/replacement generation; only the later freshness boundary compares those values against the local anchor. Evidence-format identity and version remain evidence-only. Timestamps are diagnostic and affect neither correspondence nor freshness authorization.

The implemented freshness transition consumes `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection` plus exactly one preloaded `NormalizedFreshnessAnchorObservation`, advances only when the existing pure classifier returns `Fresh`, and otherwise explicitly closes. Anchor loading and normalization remain upstream. The Rust-owned lifecycle invokes this transition before the final canonical installation-state re-observation, startup authorization, and operational activation.

Setup is the only creation authority. Ordinary startup never creates a database, key wrapper, evidence, anchor, metadata, or sidecar. Recovery, restore, migration, rekey, anchor replacement, database replacement, and destructive cleanup require separate explicit authorization. There is no generic repair authority.

### Opening, integrity, storage, and migrations

Production database-file inspection remains metadata-only and separate from the guard. The guard deliberately has native file content-read access for connection lifetime compatibility, but exposes no content-read API. The accepted open is read-only and no-create for the main database file, bounded to one application-level path open, and has no application retry loop or writable fallback; SQLite internal retries remain engine behavior. Later SQLite operations may still involve auxiliary-file behavior. `SQLITE_OPEN_NOFOLLOW` is defense in depth and not the primary Windows reparse guarantee; query-only is defense in depth and not the primary read-only guarantee. No `immutable=1` or URI open is used.

While the verified guard remains open, ordinary Windows filesystem opens requesting incompatible write or delete access to the guarded file object are refused under normal Windows sharing semantics. This bounded guarantee does not cover pre-guard mutation, kernel-mode activity, filesystem filters, raw-volume access, privileged bypass, storage or hardware faults, or operating-system/filesystem defects.

The implemented and accepted readability-and-integrity transition consumes `ProductionReadOnlyDatabaseConnection` and uses its same privately owned connection sequentially. It runs exactly `PRAGMA cipher_integrity_check`, requires normal end-of-stream with zero rows, then runs exactly `PRAGMA main.quick_check(1)`, requires exactly one SQLite `TEXT` row whose complete value is exactly `ok`, and requires normal end-of-stream after that row. No separate readability query exists: the limited quick check is the SQLite readability boundary. This stage does not read `sqlite_master`, metadata tables, `application_id`, `user_version`, product tables, or arbitrary content and exposes no unrestricted SQL.

The cipher row contract is asymmetric. Normal zero-row completion is success. The first returned row is sufficient to establish `EncryptedDatabaseAuthenticationOrCipherIntegrityFailed`; its diagnostic text is not decoded, copied, formatted, retained, or logged, later rows need not be consumed, and the row stream must be dropped or reset before close. Stopping after that first row is not incomplete success because failure is already established. Any cipher statement error, interruption, malformed completion, or premature termination fails closed rather than establishing success.

For `PRAGMA main.quick_check(1)`, zero rows, multiple rows, a non-text value, a text value other than exact `ok`, a stepping error, interruption, malformed shape, or premature termination fails closed. Diagnostic text never crosses the boundary. This limited quick check is not full `integrity_check` and does not prove every SQLite invariant. Ordinary startup continues to use only its cipher-integrity plus quick-check sequence.

A separate private, consuming transition from `ReadabilityAndIntegrityValidatedProductionDatabaseConnection` to `FullIntegrityValidatedProductionDatabaseConnection` retains the same guarded connection lifetime and runs only fixed `PRAGMA main.integrity_check`. Success requires exactly one SQLite `TEXT` row exactly equal to `ok` followed by normal completion; malformed, unavailable, interrupted, incomplete, zero-row, extra-row, non-`TEXT`, and non-`ok` outcomes fail closed. Explicit close and ownership-bearing close retry remain intact, while connection access, arbitrary SQL, paths, keys, metadata, diagnostic text, raw handles, and backend errors remain hidden. Later implementation composes it only into the production migration source consumed by the encrypted backup stage; it is not wired into ordinary startup, setup, restore, recovery, IPC, or frontend.

Both checks retain the existing `ConnectionLifetimeWriteGuard` and `InspectedProductionDatabaseFile`; query-only, defensive mode, and read-only opening remain active. No explicit SQLite transaction is added, and no stronger snapshot guarantee is claimed beyond the guarded same-connection lifetime. No active cancellation API or rusqlite hooks feature is part of this stage. `SQLITE_INTERRUPT`, statement errors, malformed result shape, and premature termination map to fail-closed coarse outcomes, and active user cancellation remains separately scoped.

Success is represented only by possession of a new opaque readability-and-integrity-validated production connection owner. The consuming type transition moves the same `rusqlite::Connection`, guard, and inspection proof into that owner, which exposes only consuming explicit close and later separately approved fixed consuming transitions. It establishes no metadata table existence, metadata row validity, SQLite `application_id`, SQLite `user_version`, schema-contract validity, database/evidence correspondence, freshness, startup/setup authority, migration authority, recovery authority, backup/restore authority, or operational database use, and exposes no connection reference, arbitrary-SQL closure, raw handle, path, or identity.

The canonical validation failure categories are `EncryptedDatabaseAuthenticationOrCipherIntegrityFailed`, `SQLiteReadabilityOrIntegrityFailed`, `ValidationUnavailable`, and `ValidationInterruptedOrIncomplete`. If validation fails, all statement and row-stream borrows are released and explicit close is attempted. A successful close returns the primary coarse validation category. A close failure preserves both that category and an opaque consuming close-retry owner containing the complete connection/guard/inspection-proof lifetime unit; retry failure retains the same unit and category again. SQL or PRAGMA text, result diagnostics, paths, keys, identifiers or identities, SQLite/native codes, raw handles, and rusqlite errors or chains are never retained or exposed.

`cipher_integrity_check` may perform a synchronous whole-file scan with work proportional to database pages. This stage adds no production database-size ceiling and promises neither bounded latency nor active cancellation. The accepted lifecycle runs it inside the single blocking startup worker rather than on the UI/event loop. That preserves UI/event-loop responsiveness but does not bound worker duration or make drain immediate.

The exact application-owned production directory and filename `parish-data.db` are required on local NTFS. Reparse points, symlinks, junctions, mount traversal, cloud placeholders, hard links, network paths, removable storage, unstable final path/file identity, and unexpected sidecars fail closed. Database-aware race protection and a dedicated database hardening adapter are required; the evidence-wrapper loader cannot simply be reused. WAL and SHM are initially prohibited. Startup never deletes or repairs sidecars.

Transactions use rollback-journal `DELETE`, `synchronous=FULL`, explicit transactions, `secure_delete=ON`, and `auto_vacuum=NONE`. There is no automatic journal-mode switch, VACUUM, or WAL checkpoint policy. Rust owns forward-only migrations. Ordinary startup may detect but never execute a migration. The source-1/target-2 direction, migration safety foundations, the complete first recovery set, and freshly verified second database, envelope, and manifest-last artifacts exist, but migration execution remains unapproved and unimplemented. The recovery-enveloped backup is not yet operationally portable or completely durably published through Layer D; normal product ceremony reachability and manual validation, a writable maintenance owner, interruption/restart policy, ordered updates, independent exact second-directory layout and complete-set verification, and later execution-chain close-failure composition remain required. A newer unsupported schema is never automatically downgraded.

Coarse errors outside this validation stage remain separately scoped. Within this stage the only primary categories are `EncryptedDatabaseAuthenticationOrCipherIntegrityFailed`, `SQLiteReadabilityOrIntegrityFailed`, `ValidationUnavailable`, and `ValidationInterruptedOrIncomplete`; wrong key versus cipher-page corruption and corrupt versus unsupported SQLite content are intentionally not split. Later metadata, correspondence, freshness, sidecar, and operational errors remain separate. No error exposes a path, SQL or PRAGMA text, result diagnostic, key, identifier or identity, generation, metadata value, DPAPI data, native status, raw handle, raw error chain, username, or profile directory.

### Implemented live metadata and header validation

The implemented transition consumes `ReadabilityAndIntegrityValidatedProductionDatabaseConnection` and uses only fixed operations on its same privately retained connection. It first prepares and fully observes `PRAGMA main.application_id`, requiring one result column, exactly one row, SQLite `INTEGER`, signed-32-bit representability, and normal terminal end-of-stream. It then compares with `0x43484150` and stops immediately with `WrongApplicationId` on mismatch. Only after that success does it prepare and fully observe `PRAGMA main.user_version` under the same strict result-shape policy, retaining the signed value temporarily.

It then prepares and executes exactly:

```sql
SELECT
    singleton_id,
    metadata_contract_version,
    database_schema_version,
    permanent_application_identifier,
    database_format_identity,
    parish_identifier,
    installation_identifier,
    installation_generation,
    recovery_replacement_generation,
    database_key_generation_identifier,
    setup_publication_identifier,
    database_created_at
FROM main.church_app_database_metadata
LIMIT 2
```

The metadata statement must expose exactly 12 result columns. Normal completion with no first row is `MetadataRowMissing`. The adapter copies the first row's 12 values into one private owned raw observation before stepping again because live row references must not outlive their row-stream step. A second row is `DuplicateMetadataRows`; normal end after the first proves exactly one row; a stepping failure is `MetadataObservationInterruptedOrIncomplete`. Fixed `LIMIT 2` distinguishes zero, exactly one, and more than one row without unbounded enumeration. The query does not filter on `singleton_id = 1`, because such a predicate could hide additional noncanonical rows.

The adapter preserves exact SQLite storage classes without casts or `typeof()`: `INTEGER` maps to `Integer(i64)`; `TEXT` undergoes strict UTF-8 validation before `Text(&str)`; `BLOB` maps to `Blob(&[u8])`; `NULL` maps to `Null`; and `REAL` is `MalformedMetadata`. The adapter constructs exactly one `RawDatabaseMetadataRow`, calls its existing `parse()` exactly once, calls `validate_structure()` exactly once, and does not duplicate pure field validation. It finally compares the validated metadata `database_schema_version` with the observed `user_version`; equality returns opaque `LiveMetadataAndHeaderValidatedProductionDatabaseConnection`. No other query, PRAGMA, schema introspection, dynamic SQL, cast, `typeof()`, or arbitrary content access is approved.

The success owner privately retains the unchanged `ConnectionLifetimeOwner` and one owned `DatabaseMetadataContractV1`. The accepted application ID is fixed and the accepted user version duplicates the validated metadata schema version, so neither header value remains separately stored. The owner exposes only consuming explicit close and later separately approved fixed consuming transitions. It exposes no `Connection`, arbitrary SQL, callback over `Connection`, detachable proof, raw header, raw metadata observation, path, handle, identity, or diagnostic.

The implementation resides in the sealed private `live_metadata_and_header_validation` child module beneath `production_database_connection_handoff`. The private visibility of `ConnectionLifetimeOwner`, `Connection`, the guard, the inspection proof, and close helpers remains unchanged. It introduces no generic connection callback or crate-root sibling bridge.

On every primary failure, `Rows` and `Statement` are released before close, and temporary header values, owned raw observations, parsed metadata, and partially validated metadata are discarded. Explicit close success returns the primary category. Close failure retains that same category plus the complete connection/guard/inspection lifetime unit in an opaque consuming retry owner, and repeated failures preserve both. Explicit close of a successfully validated live-metadata owner first discards its metadata contract; if close fails, only the complete connection/guard/inspection lifetime unit remains in the close-retry owner.

Success proves only the two exact header observations, correct `application_id`, exactly one metadata row, exact storage-class representations, strict UTF-8, success of the existing pure parse and structural validation, `user_version` agreement with the supported metadata schema version, and continued ownership of the same guarded readability-and-integrity-validated connection. Production only reads the already-existing named relation. The stage validates no physical DDL, constraints, indexes, triggers, object kind, wider product schema, database/evidence correspondence, freshness, startup authorization, operational activation, setup completion, migration status, recovery, backup/restore suitability, replacement authority, business data, or broader database use. The lifecycle calls it only as one consuming stage in the complete Rust-owned trust chain.

### Implemented identity-only database/evidence correspondence adapter

The implemented consuming transition is:

```rust
validate_production_database_evidence_correspondence(
    database: LiveMetadataAndHeaderValidatedProductionDatabaseConnection,
    evidence: TrustedCurrentInstallationEvidenceAssessment,
) -> DatabaseEvidenceCorrespondenceValidationOutcome
```

Both owners are consumed. No weaker evidence type is approved, and the adapter performs no hidden evidence loading. It borrows the retained metadata and trusted assessment only long enough to invoke exactly once:

```rust
classify_database_metadata_correspondence(
    &metadata_contract,
    trusted_assessment.evidence(),
)
```

The existing pure classifier remains the sole correspondence authority. The adapter neither duplicates nor reinterprets its exact comparisons of permanent application identifier, application database-format identity, parish identifier, installation identifier, database-key generation identifier, and setup-publication identifier. One or multiple mismatches produce only unit-like `DatabaseEvidenceCorrespondenceMismatch`. Internal comparison order is not outward behavior; field-level precedence, mismatch counts, indexes, sets, and diagnostics are intentionally unobservable. There is no `CorrespondenceUnavailable`, evidence-loading failure, lineage/freshness category, or primary `CloseFailed` category.

Success returns opaque `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection`. It privately owns exactly the unchanged `ConnectionLifetimeOwner`, one `DatabaseMetadataContractV1`, and one `TrustedCurrentInstallationEvidenceAssessment`; it retains no separate `DatabaseMetadataCorrespondence::Corresponds`. Possession of this owner is the correspondence proof. Keeping the full trusted assessment preserves trusted loading/authentication provenance, structurally validated evidence, and `TrustedCurrentInstallationIdentity`, while retaining no path, wrapper bytes, key, envelope bytes, native error, or loader state and avoiding an additional trusted projection.

The success owner retains internally the database metadata, trusted structurally validated evidence, trusted current installation identity, installation generation, recovery/replacement generation, installation identifier, database-key generation identifier, and setup-publication identifier. The lifecycle consumes this entire owner with one preloaded normalized anchor observation in the freshness boundary described below.

Correspondence itself ignores both generation values, evidence-format identity and version, and both database and evidence creation timestamps. It establishes no equal lineage, freshness, or rollback resistance. Both generations remain unchanged for the next separately scoped composition with the existing local-anchor freshness foundation.

The implementation resides in the private nested child:

```text
production_database_connection_handoff/
  live_metadata_and_header_validation.rs
  live_metadata_and_header_validation/
    database_evidence_correspondence_validation.rs
```

This placement allows the child to destructure the live owner and retain the ancestor-private lifetime owner without widening production visibility. `database_metadata_correspondence` remains pure and independent. No `Connection`, arbitrary-SQL callback, crate-root bridge, or detachable proof is exposed.

On mismatch, both inputs have been consumed, the one pure comparison has completed, and all temporary borrows end. The adapter discards `DatabaseMetadataContractV1` and `TrustedCurrentInstallationEvidenceAssessment` before explicitly closing the unchanged lifetime owner. Close success returns `DatabaseEvidenceCorrespondenceMismatch`. Close failure returns ownership-bearing `DatabaseEvidenceCorrespondenceValidationCloseFailure`, retaining only that category and the complete lifetime owner. Its consuming retry performs only close; repeated failure preserves both and eventual success returns the original mismatch. Metadata and trusted evidence never enter this close-failure owner.

Closing a successful correspondence owner first discards its metadata and trusted assessment, then attempts SQLite close. Failure reuses the existing capability-free `ProductionDatabaseConnectionCloseFailure` and retains only `ConnectionLifetimeOwner`; no second general close system is approved. The implemented type family is `DatabaseEvidenceCorrespondenceMismatch`, `DatabaseEvidenceCorrespondenceValidationOutcome`, `DatabaseEvidenceCorrespondenceValidationCloseFailure`, `DatabaseEvidenceCorrespondenceValidationCloseRetryOutcome`, and `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection`. The initial outcome distinguishes validated ownership, mismatch after successful close, and mismatch with retained close-failure ownership.

Manual coarse `Debug` is implemented for the owner, category, initial outcome, close-failure owner, and retry outcome. Formatting never delegates to metadata, evidence, or trusted-assessment formatting and discloses no field, count, order, identifier, generation, timestamp, metadata/evidence value, evidence-format field, path, wrapper, DPAPI/HMAC state, SQL/PRAGMA text, SQLite/native error or code, handle, or connection detail. Ordinary success, mismatch, and close failure require no logging.

Success proves only that all preceding database stages remain satisfied, the six approved identity fields correspond, the same guarded lifetime remains owned, the compared trusted assessment remains retained, and both generations remain available internally for later freshness. It proves no equal lineage, freshness, rollback resistance, startup authorization, operational activation, setup completion, migration status, physical DDL or object kind, wider schema correctness, recovery fitness, backup/restore suitability, replacement authority, or business-data correctness.

Tests use only the narrow `#[cfg(test)] pub(crate)` `trusted_current_installation_evidence_assessment_for_test` owned by `installation_evidence_protection/trusted_current_installation_evidence_assessment.rs`. It accepts synthetic owned structurally validated evidence and derives `TrustedCurrentInstallationIdentity` internally through the existing pure derivation; it cannot accept a separately supplied identity, is absent from production compilation, performs no filesystem, DPAPI, HMAC, loading, or parsing, and grants no production authority.

### Implemented preloaded normalized database-freshness transition

The implemented transition is:

```rust
validate_production_database_freshness(
    database: DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection,
    anchor_observation: NormalizedFreshnessAnchorObservation,
) -> ProductionDatabaseFreshnessValidationOutcome
```

Both inputs are consumed. `NormalizedFreshnessAnchorObservation` is the mandatory and only live anchor input, accepting `Present(AssuredFreshnessAnchor)`, `Missing`, `Unavailable`, and `Invalid`. The adapter accepts no paths, `FreshnessAnchorPersistencePaths`, `LoadedActiveFreshnessAnchorWrapperPair`, `AuthenticatedActiveFreshnessAnchor`, `InstallationBoundAuthenticatedActiveFreshnessAnchor`, bare `AssuredFreshnessAnchor`, or generic alternative. It performs no presence inspection, filesystem or path operation, wrapper loading, DPAPI, HMAC, parsing, authentication, generation matching, installation binding, assurance, or normalization. That complete trust chain finishes before the input crosses this boundary.

The correspondence owner contains no correspondence enum because possession is the proof. After privately destructuring the consumed owner, the sealed child constructs one ephemeral `DatabaseMetadataCorrespondence::Corresponds` solely to call:

```rust
classify_database_freshness(
    DatabaseMetadataCorrespondence::Corresponds,
    &metadata_contract,
    trusted_assessment.evidence(),
    &anchor_observation,
)
```

The call occurs exactly once. The enum is not retained, returned, or exposed, and the correspondence classifier is not rerun. `classify_database_freshness` remains unchanged and is the sole authority for generation comparisons, three-way anchor identities, lineage combination, precedence, and timestamp exclusion. The adapter duplicates none of that logic and performs no SQL, PRAGMA, query, row read, or other database-content operation.

Only `DatabaseFreshnessClassification::Fresh` produces `DatabaseFreshnessValidatedProductionDatabaseConnection`. Every other existing classification is non-success: `StaleEvidence`, `StaleDatabase`, `RollbackSuspicion`, `IdentityMismatch`, `AnchorMissing`, `AnchorUnavailable`, `AnchorInvalid`, and `Ambiguous`. The live outcome directly reuses `DatabaseFreshnessClassification`: `Fresh` advances; non-Fresh plus successful explicit close is `Failed(classification)`; non-Fresh plus failed close is ownership-bearing `CloseFailed`. `Fresh` is impossible in failure and retry forms. The live adapter adds no precedence beyond advancing `Fresh` and closing after an already determined non-Fresh classification.

`DatabaseFreshnessValidatedProductionDatabaseConnection` privately retains exactly the unchanged `ConnectionLifetimeOwner`, one `DatabaseMetadataContractV1`, and one `TrustedCurrentInstallationEvidenceAssessment`. It retains no freshness classification, correspondence enum, normalized observation, assured/authenticated/installation-bound anchor, anchor contract or fields, path, wrapper bytes, loader state, or protection error. The normalized observation and any contained assured anchor are discarded after classification, including on success. Possession of the owner is the type-level proof that the approved classifier returned `Fresh` against the supplied normalized observation.

For non-success, classifier borrows end before destruction and close. The classification is preserved while metadata, trusted assessment, normalized observation, and any assured anchor are discarded. The unchanged lifetime owner is then explicitly closed. Close success returns the original classification. `ProductionDatabaseFreshnessValidationCloseFailure` retains exactly `classification: DatabaseFreshnessClassification` and `owner: ConnectionLifetimeOwner`; consuming retry attempts only close, preserves both on repeated failure, and returns the original classification on eventual success. No metadata, assessment, trusted identity, observation, or anchor survives close failure. Closing the successful owner similarly discards metadata and the assessment before close and reuses `ProductionDatabaseConnectionCloseFailure`, which retains only lifetime ownership.

The implemented family is `ProductionDatabaseFreshnessValidationOutcome`, `ProductionDatabaseFreshnessValidationCloseFailure`, `ProductionDatabaseFreshnessValidationCloseRetryOutcome`, and `DatabaseFreshnessValidatedProductionDatabaseConnection`. Manual `Debug` is coarse: payload-free non-Fresh variant names may be shown, while owners and ownership-bearing failures are redacted. Formatting and ordinary logs reveal no identifier, generation, timestamp, metadata, evidence, trusted identity, anchor value, wrapper/protection state, path, DPAPI/HMAC detail, SQL/PRAGMA text, SQLite/native detail, handle, connection detail, or raw error chain. No ordinary success, non-success, or close-failure logging is required.

The privacy-preserving placement is:

```text
production_database_connection_handoff/
  live_metadata_and_header_validation/
    database_evidence_correspondence_validation.rs
    database_evidence_correspondence_validation/
      database_freshness_validation.rs
```

The correspondence module declares and narrowly reexports its child. The child can destructure the correspondence owner without production visibility widening. There is no crate-root bridge or arbitrary production `Connection` callback. The accepted implementation uses canonical `close_lifetime_owner` paths; injected close callbacks are `cfg(test)`-only.

Accepted live tests obtain genuine correspondence owners through the existing SQLCipher predecessor chain and provide synthetic preloaded normalized observations through existing contract, bound-anchor, assurance, and direct non-present-state seams. Source assertions prohibit production constructors, visibility widening, arbitrary production connection callbacks, forgeable freshness-success constructors, and classifier bypasses.

Success proves only that preceding guarded, keyed, readability, integrity, header, metadata, and correspondence stages remain represented; correspondence had already succeeded; the pure classifier ran once with ephemeral `Corresponds`; the supplied observation was `Present` with an assured anchor; the three-way identities passed; both lineages were `Current`; and the same guarded lifetime remains owned. It does not prove cryptographic monotonic rollback prevention, absolute newest state, coordinated-rollback detection, startup authorization, operational activation, setup completion, migration state, physical DDL, object kind, wider schema correctness, recovery/replacement authority, backup/restore suitability, business-data correctness, or continued validity after external changes. The lifecycle next performs the final canonical installation-state observation and supplies that observation to the startup-authorization boundary recorded below.

### Implemented internal startup authorization after freshness

The exact implemented transition is:

```rust
pub(crate) fn authorize_production_database_startup(
    database: DatabaseFreshnessValidatedProductionDatabaseConnection,
    installation_evidence: InstallationEvidence,
) -> ProductionDatabaseStartupAuthorizationOutcome
```

It consumes exactly the freshness-validated owner and one preloaded `installation_state::InstallationEvidence`. No hidden installation-state loading is allowed. This adapter observes no filesystem, path, sidecar, freshness anchor, database, environment, account, administrator-group, standard-user, or elevation state. Freshness has already succeeded; the adapter makes exactly one fixed decision over the supplied installation evidence and does not rerun freshness, correspondence, metadata/header validation, readability/integrity validation, database-file inspection, evidence loading, path/sidecar inspection, or anchor loading/normalization. There is no earlier-stage precedence inside it.

Only `InstallationEvidence::Initialized(ExpectedStorageEvidence::Present)` advances. Success returns opaque `StartupAuthorizedProductionDatabaseConnection`, which privately retains exactly:

- `ConnectionLifetimeOwner`;
- `DatabaseMetadataContractV1`;
- `TrustedCurrentInstallationEvidenceAssessment`.

It retains no `InstallationEvidence`, `StorageDecision`, `SetupAuthorizationState`, `FirstTimeSetupAuthorization`, startup-authorized Boolean or enum, `DatabaseFreshnessClassification`, `DatabaseMetadataCorrespondence`, `NormalizedFreshnessAnchorObservation`, assured/authenticated/bound anchor, path, or sidecar observation. Possession of the owner is the authorization proof.

The four coarse primary categories are exactly `ProductionDatabaseStartupAuthorizationError::NeverInitialized`, `ExpectedStorageMissing`, `InstallationStateInconsistent`, and `InstallationStateUnavailable`. The exact mapping is:

| Supplied `InstallationEvidence` | Result |
| --- | --- |
| `Initialized(Present)` | `Authorized` |
| `NeverInitialized` | `NeverInitialized` |
| `Initialized(Missing)` | `ExpectedStorageMissing` |
| `Initialized(Unavailable)` | `InstallationStateUnavailable` |
| `Inconsistent` | `InstallationStateInconsistent` |
| `Unavailable` | `InstallationStateUnavailable` |

The two unavailable observations intentionally collapse. `NeverInitialized` is not setup permission. Neither `StorageDecision` nor `SetupAuthorizationState` is reused as the startup taxonomy.

The implemented family is `StartupAuthorizedProductionDatabaseConnection`, `ProductionDatabaseStartupAuthorizationError`, `ProductionDatabaseStartupAuthorizationOutcome`, `ProductionDatabaseStartupAuthorizationCloseFailure`, and `ProductionDatabaseStartupAuthorizationCloseRetryOutcome`. The initial outcome distinguishes `Authorized(success owner)`, `Failed(primary category after successful close)`, and `CloseFailed(ownership-bearing close failure)`. `CloseFailed` is not a primary authorization category.

On success, the adapter evaluates the supplied evidence, requires `Initialized(Present)`, discards the installation-state observation, and moves the unchanged lifetime owner, metadata contract, and trusted assessment into the startup-authorized owner. On failure, it determines and preserves the primary category; discards `InstallationEvidence`, `DatabaseMetadataContractV1`, and `TrustedCurrentInstallationEvidenceAssessment`; and then explicitly closes `ConnectionLifetimeOwner`. Successful close returns the original primary category. Failed close returns `ProductionDatabaseStartupAuthorizationCloseFailure`, retaining exactly that category and the complete lifetime owner and no other evidence, identity, metadata, assessment, path, anchor, setup authority, or capability.

The close-failure owner exposes a consuming retry returning `ProductionDatabaseStartupAuthorizationCloseRetryOutcome`: `Closed(original category)` or `Failed(close-failure owner)`. Retry performs only close. Repeated failure preserves the original category and complete lifetime owner; eventual success returns the original category. It performs no authorization, observation, loading, database access, recovery, or setup work.

Closing a successful `StartupAuthorizedProductionDatabaseConnection` first discards `DatabaseMetadataContractV1` and `TrustedCurrentInstallationEvidenceAssessment`, then explicitly closes `ConnectionLifetimeOwner`. It reuses the existing `ProductionDatabaseConnectionCloseOutcome` and `ProductionDatabaseConnectionCloseFailure`; a close failure retains only lifetime ownership. No second generic successful-owner close system is approved.

The implemented private source placement is:

```text
production_database_connection_handoff/
  live_metadata_and_header_validation/
    database_evidence_correspondence_validation/
      database_freshness_validation.rs
      database_freshness_validation/
        startup_authorization.rs
```

`database_freshness_validation.rs` declares the private child. This permits private destructuring of the freshness owner and lifetime owner without production visibility widening. No crate-root bridge, generic or arbitrary `Connection` callback, operational connection exposure, or production success constructor was added.

Manual `Debug` is coarse. Only the payload-free primary category names may be visible. Owners and ownership-bearing failures are redacted, and formatting does not delegate to metadata, trusted assessment, installation evidence, lifetime ownership, rusqlite, or native errors. No identifiers, generations, timestamps, metadata/evidence values, trusted identity, paths, sidecars, handles, SQL/PRAGMA text, native codes, connection detail, or raw error chains escape. No ordinary success or failure logging is required.

`StartupAuthorizedProductionDatabaseConnection` is not itself operational. It exposes no `Connection`, SQL/query operation, arbitrary callback, path, metadata accessor, trusted-assessment accessor, or evidence accessor. It proves only that preceding stages remain represented, freshness succeeded, the supplied evidence was `Initialized(Present)`, fixed startup policy accepted it, the same guarded lifetime remains owned, and retained metadata and assessment are available to the consuming operational-activation transition. Authorization grants no SQL or broader product capability.

## Approved first business-schema architecture (not implemented)

PRODUCT-LOCKED scope is exactly `ServiceRequest`, `RequestScheduleOccurrence`, and `RequestCancellationReview`. APPROVED PHYSICAL DESIGN maps those concepts to exactly three `STRICT` tables: `service_requests`, `request_schedule_occurrences`, and `request_cancellation_reviews`. Each uses `id INTEGER PRIMARY KEY` without `WITHOUT ROWID`; IDs are internal database identity only, not frontend or IPC authority. There is no fourth entity, lookup table, generic workflow/audit/history/event/soft-delete system, person table, or permanent/sacramental-record table.

`service_requests` stores the fixed service code, primary status, bounded requester full name/phone/optional email snapshot, and non-negative UTC Unix-millisecond `created_at`. The service codes are exactly `baptism`, `confirmation`, `wedding_marriage`, `burial_funeral`, and `first_communion`; status codes are exactly `pending`, `scheduled`, `completed`, and `cancelled`. Status has no SQLite default: future Rust creation binds `pending` explicitly. Only `Pending -> Scheduled`, `Pending -> Cancelled`, `Scheduled -> Completed`, and `Scheduled -> Cancelled` are allowed.

`request_schedule_occurrences` stores the parent ID, kind `primary`/`funeral`/`burial`, canonical `YYYY-MM-DD` local date, canonical 24-hour `HH:MM` local time at minute precision, and optional bounded location. It does not duplicate service category or timezone. `Asia/Manila` is the system invariant. `UNIQUE(service_request_id, occurrence_kind)` enforces at most one of each kind. The database checks date shape and time shape/ranges; Rust checks Gregorian validity and cross-table kind compatibility.

Only occurrences of `scheduled` parents occupy the shared parish schedule; conflict is exact date/time equality, independent of location. Pending draft occurrences do not reserve a slot. Completed and cancelled requests release occupancy but retain their occurrences as historical facts. Rescheduling updates the existing occurrence in place. Cardinality, exact-slot conflict checks, rescheduling atomicity, and service/kind compatibility are future sealed Rust transaction responsibilities; there is no trigger, schedule-history table, or unique slot constraint.

Baptism, Confirmation, Wedding/Marriage, and First Communion require exactly one `primary` occurrence before scheduling. Burial/Funeral requires at least one of `funeral` or `burial`, and either or both is valid. Wedding/Marriage and First Communion require non-empty location before scheduling and while scheduled; location is optional for the other services, may be absent while pending, and may differ between Funeral and Burial. These category-dependent rules remain Rust-side; no location catalog exists.

`request_cancellation_reviews` stores the parent ID, `pending`/`approved`/`rejected` disposition, non-negative UTC Unix-millisecond `requested_at`, and nullable `resolved_at`. Disposition has no default: Rust explicitly binds `pending`. `resolved_at` is null exactly for pending and non-null/non-negative when resolved, without requiring it to be at least `requested_at`. A partial unique index enforces one pending review per request; resolved reviews remain retained.

Both child foreign keys declare `ON UPDATE RESTRICT ON DELETE RESTRICT`. A future writable connection must separately enable and verify SQLite foreign-key enforcement; current startup policy and PRAGMAs are unchanged. Cancellation approval/rejection, request compare-and-set transitions, occupancy/conflict checks, cardinality, compatibility, location rules, and complete text/date validation remain future Rust-owned transactional work.

The approved indexes are limited to status/creation chronology, exact-slot lookup, per-request review chronology, pending-review priority, and pending-review uniqueness as recorded canonically in `docs/product-decisions.md`. Text bounds are 200 Unicode characters for requester name, 32 for phone, 254 for optional email, and 256 for optional location; database checks enforce present-value length and no surrounding ASCII spaces, while Rust owns trimming, empty/NULL handling, NUL/control rejection, and non-destructive validation.

DATABASE-FORMAT ASSIGNMENT: the implemented metadata/bootstrap source is `database_schema_version = 1`. The approved three-table physical contract above is exactly target `database_schema_version = 2` and nothing broader. Its `metadata_contract_version` remains 1; SQLite `user_version` must mirror the target as 2; `application_id` remains `0x43484150`; and the existing `ApplicationDatabaseFormatIdentity` remains unchanged. The only defined compatibility direction is 1 -> 2. There is no generic `unsupported -> 2` transition, and any version newer than 2 remains unsupported and is never automatically downgraded.

STILL UNIMPLEMENTED: no schema-version-2 business DDL, table, migration, writable operation, Rust business type, IPC, form, or workflow exists. The current schema-version-1 metadata/bootstrap implementation and its source constants and validators stay unchanged. The version assignment does not create migration eligibility or authorization, and observing schema 1 is not sufficient for either. No migration SQL or execution path is approved.

## Implemented first-time setup and minimal V1 bootstrap

First-time setup is an explicit Rust-owned lifecycle, not an ordinary-startup fallback. The argument-free `request_first_time_setup` IPC request supplies no path or authority. Rust resolves the canonical root, requires the accepted `NeverInitialized` path, applies setup exclusivity, and runs the setup orchestration on its dedicated worker. Startup authority, setup authority, and future migration authority are distinct.

The authorized setup path creates the SQLCipher production database at the fixed `parish-data.db` location and initializes the minimal production V1 metadata/header bootstrap contract. That schema exists to establish the current database identity and bootstrap trust contract only. It is not a parish/business schema, contains no parish workflow tables, and does not make broad product schema creation complete. Setup validates the database, prepares and verifies protected artifacts, publishes them under the existing evidence-last and freshness/key rules, reopens and verifies the active state, and performs final canonical installation verification.

Successful setup ends in `SetupRestartRequired`. It does not install an operational owner or authorize same-process `Ready`. A fresh process must follow the ordinary startup path and pass its currently implemented startup trust chain before it may log `ready_installed` and expose `Ready`.

Migration remains future gated maintenance work. Neither setup nor startup may invoke it or serve as its fallback, and setup authority does not imply migration authority. The repository lifecycle-composes an exact full-integrity migration source and migration authorization into an independently verified encrypted SQLCipher backup stage, consumes that stage through recovery-envelope verification and C1 preparation, and dispatches retained `PreparedUndisclosedMigrationRecoveryKeyCustody` through C2b to C2a on the application main thread with the real main-window `HWND`. This capability remains private, normally unreachable, and manually unvalidated; it is not an operational human custody ceremony, a durably published backup, or restore authority. An eventual 1-to-2 migration still requires publication, a writable maintenance owner, interruption/restart handling, ordered metadata/`user_version`/evidence/freshness/lineage updates, and later execution close-failure composition. No migration SQL, execution, or schema-version-2 implementation is approved.

## Locked future maintenance authorization trigger and initial taxonomy

The maintenance-authorizing actor is the local human operator physically using Church App under the dedicated standard Windows account used for production operation. The Windows session identifies the operating context but does not itself grant maintenance authority. Authority is established only by an explicit interactive maintenance confirmation initiated from the Church App application. The confirmation is an authorization and consent boundary, not proof of application-level identity. Future application authentication may strengthen the boundary, but first-generation maintenance architecture does not depend on it.

Any future maintenance authority must be explicit, interactive, single-use, process-local, non-persistent, non-renewing, and scoped to exactly one approved maintenance operation. It cannot be inferred, automatically granted, granted on startup or setup completion, granted because migration is required, retained across restart, serialized, persisted, rebuilt from frontend-supplied Boolean, string, path, or version values, or reused for another operation. Automatic maintenance authorization and automatic maintenance execution are prohibited.

Startup, `Ready`, setup authority, startup authorization, operational activation, full-integrity validation, backup existence or verification, writable ownership, migration eligibility, version compatibility, and concurrency or exclusivity are not maintenance authorization. The safety gates remain independently composed: verified recoverable encrypted backup, full-integrity validation, valid source and target versions, writable maintenance ownership, concurrency and exclusivity, and installation, evidence, and freshness state. React may eventually present or request confirmation but remains non-authoritative; Rust must ultimately construct and own the authority.

The architecture does not introduce a generic `MaintenanceOperation` enum or broad generic `MaintenanceAuthorization` capability. Production database migration is the sole initial maintenance authorization identity because it is the only concrete next maintenance operation. The private Rust capability is `ProductionDatabaseMigrationAuthorization`; it carries no eligibility, version, proof, path, identifier, or execution context. Source and target schema versions are separate eligibility and migration-plan prerequisites. Verified recoverable backup proof is also separate, does not record operator consent, and is not embedded in the authorization. Migration authorization alone never establishes eligibility or executability.

Restore, recovery, rekey or database-key replacement, anchor replacement, database replacement, and destructive cleanup remain distinct future authority domains outside migration authority. Recovery is not restore; rekey is not database replacement; and destructive cleanup is distinct from reset, repair, recovery, replacement, and migration. Setup and startup are not maintenance operations. Backup verification or acceptance and full-integrity validation are safety or proof capabilities rather than operation identities. Explicit diagnostics, including full-integrity diagnostics, are not approved operation variants. Backup creation is not approved as a distinct operator-authorized operation identity. “Installation/evidence replacement” is too broad to serve as a catch-all operation identity.

The approved construction boundary requires Rust to establish an exact, process-local `Pending` production-database-migration confirmation opportunity before any frontend confirmation can have meaning. React cannot create `Pending`, supply its identity, or construct authority; a bare call without that Rust-owned context must fail closed. The owner direction is `ApplicationLifecycle` or a migration-specific substate beneath it, reusing its mutex-serialized privileged transitions and shutdown-race handling. `StartupStatus` must not be overloaded with authority state, and no generic maintenance state machine is introduced.

The implemented one-shot states are `NotOffered -> Pending -> Authorized -> Consumed` and `Pending -> Revoked`. Confirmation atomically consumes `Pending` and directly constructs and retains `ProductionDatabaseMigrationAuthorization`; cancellation atomically moves `Pending` to terminal `Revoked` without authorizing. After confirmation, cancellation, revocation, invalidation, or consumption, no second `Pending` may be created in the same process. `Authorized`, `Consumed`, and `Revoked` are process-terminal for offering confirmation. Only a fresh process may offer again; restart destroys pending state and unconsumed authorization. Shutdown invalidates the opportunity and any retained unconsumed authorization under the lifecycle mutex before the existing shutdown transition. Future Rust-owned migration-context or eligibility invalidation can revoke `Pending`. Eligibility remains separate and must be revalidated when execution is later composed.

The future authority-changing command direction is migration-specific and argument-free: `confirm_production_database_migration`. Its only conceptual input is Tauri-managed Rust `ApplicationLifecycle` state. It accepts no Boolean, operation, version, path, ID, nonce, authorization object, proof, database owner, or arbitrary frontend value. Only the first valid atomic consumption can succeed; repeated, concurrent, replayed, stale, and non-pending calls create nothing. A coarse result may expose only `accepted`, `notPending`, or `unavailable`, and `accepted` means only that Rust retained the authorization internally. Authorization never crosses IPC. The corresponding argument-free `cancel_production_database_migration_confirmation` is non-authorizing and terminally revokes `Pending`; future UI abandonment must invoke it or an equivalent Rust-owned revocation rather than silently leaving `Pending` live.

No intermediate `ConfirmedMigrationIntent` is approved because it would duplicate the authorization trust fact without a new boundary. The eventual authorization remains private, non-`Copy`, non-`Clone`, non-serializable, non-persistent, redacted, and exactly-once consumable. A compromised renderer can still invoke the future argument-free confirm command during a legitimate Rust-owned `Pending` context. This first-generation boundary therefore represents consent in that exact context, not authenticated identity, and does not claim protection from a compromised renderer. Stronger assurance requires separately approved authentication, trusted UI, or another mechanism; the residual risk does not permit React to establish `Pending` or authority.

The state machine and `ProductionDatabaseMigrationAuthorization` are implemented privately and tested before migration execution. Later repository stages add production migration safety foundations, custody foundations, the complete first recovery set, and second-database, envelope, and manifest-last publication with independent fresh verification against the retained source. The confirmation commands and UI, normal product reachability, migration execution, schema-version-2 implementation, independent exact second-directory layout verification, second complete-set verification, final two-set Layer D capability, writable ownership, and authentication remain unimplemented.

## Implemented migration recovery-envelope architecture

`VerifiedEncryptedProductionDatabaseMigrationBackupStage` is layer A. It proves an encrypted SQLCipher stage copied from the exact full-integrity-validated migration source with the same production database key, independent cipher and full SQLite integrity, exact source metadata/header equality, ciphertext at rest, unchanged retained source, and migration authorization still bound to that source. Its retained stage continuity includes the parent handle/identity, read-only leaf handle, and full leaf `FILE_ID_INFO`; the leaf excludes incompatible ordinary write/delete access.

Layer B is the implemented pure **Migration Recovery Envelope Format Version 1**. Its exact framing is magic/domain `CHMRECV\0`, version `1`, algorithm ID `1`, 182 total bytes, 96 payload bytes, 44 AAD bytes, a 24-byte nonce, and a detached 16-byte tag. It uses exactly **XChaCha20-Poly1305**, one separately generated uniformly random 256-bit migration recovery key, one independent nonzero 128-bit recovery-key-generation identifier, one independent nonzero opaque 128-bit migration-backup-set identifier, and one independently generated uniformly random 192-bit nonce per envelope. Version 1 accepts only XChaCha20-Poly1305, with no negotiation or fallback. Plain ChaCha20, 96-bit-nonce ChaCha20-Poly1305, AES-CBC, AES-CTR, unauthenticated encryption, HMAC-only protection, DPAPI as the portable envelope, and SQLCipher as an arbitrary payload cipher are outside the contract. The approved implementation dependency is exactly `chacha20poly1305 = { version = "=0.11.0", default-features = false, features = ["zeroize"] }`.

The migration recovery key uses the repository-approved operating-system randomness source and a dedicated non-`Clone`, non-`Copy`, non-serializable type with manually redacted `Debug`, best-effort drop zeroization, and a process-local plaintext lifetime only during construction and verification. The recovery-key-generation identifier identifies the key generation but is public and never key material. The backup-set identifier binds the encrypted database and envelope into one future recovery set and cannot reuse the setup-publication identifier, installation identifier, database-key-generation identifier, or recovery/replacement generation.

Every envelope uses a fresh random nonce. Nonce reuse under one recovery key is prohibited; generation failure fails closed. There is no counter, persistent global nonce allocator, or deterministic nonce derivation from an identifier, digest, timestamp, path, metadata, or key material. The nonce is encoded and non-secret. The 192-bit random nonce choice intentionally avoids persistent allocation state.

The protected plaintext has exactly four fields: the 256-bit production database key; 128-bit database-key-generation identifier; 128-bit migration-backup-set identifier; and 256-bit SHA-256 digest of the exact closed encrypted backup-stage database bytes. The writer must close successfully and the current verified-encrypted-stage proof must exist before hashing. The digest is public but authenticated. Parish, installation, installation-generation, recovery/replacement-generation, setup-publication, `application_id`, `user_version`, database-schema-version, database-format-identity, evidence/freshness-key, active DPAPI wrapper, path, and timestamp data remain outside this payload absent a later demonstrated need; those database identities are independently validated from the recovered SQLCipher database.

The exact 44-byte associated authenticated data is the immutable framing through the externally encoded backup-set identifier. It provides one fixed explicit domain separator permanent to the Church App migration-recovery-envelope purpose and binds the format version, algorithm identifier, recovery-key-generation identifier, and backup-set identifier. Evidence-envelope and freshness-envelope domains are never reused.

Opening is authentication-before-release. Framing, exact supported version, exact supported algorithm, required structural identifiers, and ciphertext length are validated before AEAD opening, and no production database-key plaintext is released until authentication succeeds. A wrong recovery key or any authentication failure releases no candidate key; unauthenticated plaintext inspection is forbidden. After opening, the payload backup-set identifier must equal the authenticated envelope/set identifier, the database-key-generation identifier must correspond to recovered database metadata, and a fresh SHA-256 digest of the stage must equal the authenticated payload digest. Any mismatch fails closed.

The private consuming transition generates the recovery key and all three independent random values and hashes the exact retained stage before sealing. It independently reloads and recovers the active protected database-key wrapper, binds it exactly for construction, seals the envelope, and destroys that wrapper-derived `GenerationBoundDatabaseKey` before verification. It does not reload the live wrapper during independent verification: it strictly parses and authenticates/decrypts the envelope, reconstructs a fresh generation-bound candidate using the existing trusted installation assessment, hashes the exact retained stage a second time, and requires equality with the authenticated digest. It opens that same stage using only the envelope-recovered key with `READ_ONLY`, `FULL_MUTEX`, `PRIVATE_CACHE`, `NOFOLLOW`, the fixed win32 VFS, and no `CREATE`; applies canonical pre-key hardening and the key once; enables/verifies `query_only`; reruns only canonical cipher integrity and fixed metadata/header observation with exact source equality; explicitly closes the verifier; and performs final stage-identity continuity before success.

The implemented envelope-stage success is exactly `VerifiedRecoveryEnvelopedProductionDatabaseMigrationBackup`, never `VerifiedRecoverableProductionDatabaseBackup`. It retains exact migration authorization, the exact full-integrity source, the verified encrypted stage, generated recovery-key material for the implemented custody foundation, and independently verified envelope proof. Neither this owner nor the custody foundation establishes durable publication or operational portable recovery. The envelope grants no migration/startup authorization, database-write authority, restore authority, evidence/freshness authority, or mutation authority.

Envelope verification deliberately does not repeat full SQLite integrity when the prior encrypted-stage proof already passed it, authenticated SHA-256 equality proves the file bytes are unchanged, recovered-key cipher integrity succeeds, and metadata/header equality succeeds. Failure to establish digest equality fails the transition; it cannot be replaced with weaker verification. The transition never mutates the database stage to add recoverability because all binding is external.

The portable envelope itself is not DPAPI-encrypted. A future optional local convenience copy of the recovery key may use CurrentUser DPAPI but can never constitute sufficient portable custody. Envelope creation preserves recovery/replacement generation; only a separately authorized recovery/replacement transition changes it.

Failure destroys migration authorization before returning outward. Primary failure explicitly closes the source and preserves canonical source-close retry ownership; verifier-close failure preserves a verifier-close-only retry owner. Outward failures have coarse redacted `Debug`, retain stage/envelope artifacts, contain no recovery-key material, and perform no automatic destructive cleanup.

Layers remain separate: A, implemented encrypted migration backup stage; B, implemented cryptographic recovery envelope and private recovered-key verification; C1, implemented custody format/private state foundation; C2a, implemented private Windows native custody adapter; C2b, implemented lifecycle/main-thread/real-main-window dispatch that remains normally unreachable and manually unvalidated; D, implemented complete first set plus second database, envelope, and manifest-last artifacts, with independent exact second-directory layout verification, the second complete-set proof, and final two-set verification unimplemented; E, unimplemented restore authorization and canonical replacement; F, unimplemented supersession and destructive retention/deletion. Cross-process migration exclusivity remains a separate thread-affine worker-local safety gate and is not owned by custody.

## Implemented migration recovery-key custody architecture

Migration Recovery Key Custody Format Version 1 is one plaintext human-readable bearer-secret record. Its canonical prefix is `CHURCH-MIGRATION-RECOVERY-KEY-V1`, and its exact alphabet is `0123456789ABCDEFGHJKMNPQRSTVWXYZ`. Encoding is MSB-first Base32 with no padding and uppercase canonical output. Parsing accepts ASCII case-insensitively but performs no Unicode normalization, provides no ambiguous-character aliases, and rejects `I`, `L`, `O`, and `U` in encoded field positions.

The canonical record is exactly five lines and 196 bytes with LF separators and no terminal newline. Line lengths are 32, 36, 36, 68, and 20. Its only semantic fields are format/version prefix, recovery-key-generation ID, migration-backup-set ID, 256-bit migration recovery key, and checksum. There is no parish, installation, schema, path, timestamp, copy number, operator, or storage-location field. `GEN-`, `SET-`, `KEY-`, and `CHK-` contain exactly 26, 26, 52, and 13 Base32 symbols; their groupings are `4-4-4-4-4-4-2`, `4-4-4-4-4-4-2`, thirteen groups of four, and `4-4-4-1` respectively.

The checksum is the first eight SHA-256 digest bytes over exactly 105 bytes: `ASCII("CHURCH-MIGRATION-RECOVERY-KEY-CHECKSUM") || 0x00 || U16_BE(1) || recovery-key-generation-id[16] || backup-set-id[16] || recovery-key[32]`. It uses the same Base32 alphabet and provides typo/error detection only. It is not authentication, a MAC, password verification, provenance, or restore authority; recovery-envelope AEAD remains the cryptographic authentication boundary.

`EncodedMigrationRecoveryKeyCustodyV1` owns fixed `[u8; 196]`, is non-`Copy`, non-`Clone`, non-serde, redacted, and zeroizing on drop. It exposes no generic production text/string/raw-byte interface; parsed and checksum-validated secret states also zeroize secret material. The private preparation transition consumes only `VerifiedRecoveryEnvelopedProductionDatabaseMigrationBackup`, derives generation/set association from the already independently verified envelope, and uses the exact retained generated recovery-key material. It accepts no independently supplied recovery key or arbitrary identifiers and does not call key generation, set generation, or envelope sealing.

The private state progression is `VerifiedRecoveryEnvelopedProductionDatabaseMigrationBackup -> PreparedUndisclosedMigrationRecoveryKeyCustody -> DisclosedMigrationRecoveryKeyCustody -> FirstCopyVerifiedMigrationRecoveryKeyCustody -> RecoveryKeyCustodyVerifiedProductionDatabaseMigrationBackup`. Lifecycle migration preparation retains the ownership-bearing Prepared value off lifecycle-visible state until dispatch; C2b transfers it to the main thread and returns the ownership-bearing native result to the worker. Before exposure, interruption may retain the exact whole prepared owner for retry; neither key nor envelope is regenerated. Disclosure is irreversible, with no disclosed/first-copy state transition back to prepared. After possible exposure, cancellation or a failed readback is terminal: no custody retry; encoded text, recovery-key material, and migration authorization are destroyed; source close is attempted; only canonical close retry and keyless stage/envelope failure context can remain.

Exactly two successful complete readbacks are required. First-copy verification alone is not custody completion. Success retains only the exact encrypted stage, independently verified envelope, and `VerifiedMigrationRecoveryKeyCustody`; it retains no `MigrationRecoveryKey`, `GeneratedMigrationRecoveryKeyMaterial`, encoded custody text, or decoded readback secret. This keyless post-custody owner is the safe input direction for later publication.

The product direction requires exactly two complete paper copies of the same bearer record, each independently re-entered and verified. They are redundancy, not secret sharing, and do not satisfy the later two-verified-complete-recovery-sets publication requirement. The historical ten recovery codes do not apply. Secret sharing, password/KDF protection, a DPAPI custody copy, clipboard, ordinary file export, print-spooler flow, and QR are prohibited. The secret must not cross React/Tauri IPC.

The C2a adapter is implemented in `src-tauri/src/production_database_migration_backup_stage/recovery_envelope/custody/native_windows.rs`, privately registered under `cfg(windows)` from the existing custody state module. The adapter accepts an already-supplied `HWND` and remains isolated from Tauri handle lookup and dispatch. C2b resolves the real `main` webview window with `get_webview_window("main")`, obtains its `HWND` with `window.hwnd()`, and invokes C2a through `AppHandle::run_on_main_thread`; the `HWND` is neither retained nor logged. C2a uses `DialogBoxIndirectParamW` with only standard `STATIC`, `EDIT`, and `BUTTON` controls for Intro -> Reveal 1 -> Readback 1 -> Reveal 2 -> Readback 2 -> Success.

The existing `windows-sys = "=0.61.2"` dependency remains pinned with `default-features = false`. C2a uses `Win32_UI_Controls`, `Win32_UI_Input_KeyboardAndMouse`, and `Win32_UI_WindowsAndMessaging`. No new crate was added; availability of these bindings does not make C2a operational.

The native display borrows the existing fixed 196-byte custody record only through a narrow crate-private closure-scoped seam; no generic text, string, slice, `Clone`, `Copy`, or serialization surface was added. A zeroizing `[u16; 201]` holds 196 canonical ASCII bytes plus exactly four LF-to-CRLF insertions, yielding 200 text units and an explicit NUL. The display is a non-selectable `STATIC`. Each visible multiline readback `EDIT` has an exact 200-UTF-16-unit `EM_SETLIMITTEXT` cap. Extraction uses `GetWindowTextLengthW`, fixed `[u16; 201]`, returned-length consistency, ASCII-only rejection, fixed zeroizing `[u8; 200]`, and exact handoff to existing strict validation. There is no trimming, Unicode normalization, aliasing, case rewriting, or grouping rewriting.

Control subclasses suppress `WM_PASTE`, `WM_COPY`, `WM_CUT`, `WM_CONTEXTMENU`, Ctrl+V, Ctrl+C, Ctrl+X, and Shift+Insert where applicable, without calling clipboard APIs. This is bounded UI friction, not an operating-system security boundary. There is no `SetWindowDisplayAffinity`/`WDA_EXCLUDEFROMCAPTURE` support or capture-exclusion claim, and no claimed resistance to same-user message injection, accessibility tooling, UI automation, debuggers, malware, screen capture, or photography.

Disclosure ordering is Prepared -> consume `disclose()` -> Disclosed -> borrow secret bytes -> convert -> `SetWindowTextW`; Prepared cannot reach the display seam. Intro cancellation is pre-exposure and retryable. Every native failure/cancellation after disclosure is terminal. The first successful readback advances only to `FirstCopyVerifiedMigrationRecoveryKeyCustody`; the second reveal continues from that owner without rebuilding Prepared or regenerating/resealing. Second success yields the existing keyless `RecoveryKeyCustodyVerifiedProductionDatabaseMigrationBackup`. Post-exposure native failure uses `NativeCeremonyFailedAfterCustodyExposure` and the existing terminal cleanup path, including source close and canonical close-only retry ownership when needed.

Every native callback ABI boundary contains unwind; recovery is independently contained, with fail-stop abort only if recovery itself panics. The adapter logs nothing. Its redacted outward outcomes are `Verified`, `InterruptedBeforeExposure`, `UnavailableBeforeExposure`, and `FailedAfterExposure`. Accessibility implementation is limited to standard controls, deterministic control/tab creation order, default/cancel semantics, and explicit focus transitions; there is no screen-reader, high-DPI, complete accessibility, keyboard-runtime, or UI-automation-resistance validation.

C2a/C2b are not operationally reachable through normal product interaction because production migration discovery invocation, confirmation/cancellation commands, and frontend migration UI are absent. No real main-window parenting, owner modality against the running app, WebView blocking, event-loop behavior, keyboard traversal, Alt+F4/X behavior, shutdown during the actual dialog, high-DPI layout, screen-reader behavior, parent restoration, or real paper-copy ceremony has been tested or observed. The architecture therefore does not claim that paper copies or physical separation exist, that durable physical custody exists, or that current application behavior preserves the key across process or machine loss.

Custody and the native adapter grant no migration authorization, migration eligibility or execution, publication proof, restore authorization or execution, durable external-storage proof, physical-copy proof, or physical-separation proof. A/B/C1/C2a/C2b implementation does not collapse D/E/F. No publication, restore, migration execution, filesystem export, printing, QR, DPAPI custody, password/KDF, secret sharing, recovery-key regeneration, backup-set regeneration, or envelope resealing was added. Migration exclusivity remains a separate worker-local safety gate and is not owned by custody or C2a.

## Implemented Rust-owned application lifecycle

The Tauri window is allowed to appear immediately while the frontend defaults to the non-ready `Starting` view. During `setup`, Rust reserves startup and launches exactly one blocking worker for the complete synchronous production trust chain. Database work, installation-state observation, startup authorization, operational activation, and ownership-bearing failures remain in Rust; the UI/event loop is not used to run the synchronous trust chain.

The worker resolves the Rust-owned application-local-data root and derives fixed evidence, key, anchor, and database paths. On Windows debug builds only, a strictly validated isolated manual fixture root may replace that root and a single blocking pause may occur immediately before the final installation observation. This support is compiled only with Windows plus `debug_assertions`, has no frontend or IPC authority, and is not production behavior. The separate fixture exporter is compiled only for Windows tests, is ignored, and is likewise non-production.

The production sequence is:

1. Observe canonical `installation_state::InstallationEvidence` early and require `Initialized(Present)`.
2. Load trusted installation evidence, normalize the freshness-anchor observation, recover and bind the database key, inspect and open the database read-only, then consume the readability/integrity, metadata/header, correspondence, and freshness transitions.
3. Re-observe canonical installation evidence immediately before final authorization. The actual second observed value is passed to `authorize_production_database_startup`; startup does not manufacture `Initialized(Present)`.
4. Consume the authorized owner through `activate_production_database_for_operational_use`. This is operational activation, not another authorization step.
5. Install `Ready` only with the resulting real `OperationalProductionDatabase`.

Any terminal startup failure becomes coarse `Unavailable`. There is no setup, migration, repair, recovery, reset, or retry fallthrough in this path. The frontend is non-authoritative and can read only coarse lifecycle status through `startup_status`, including `Starting`, `Ready`, `Unavailable`, `SetupInProgress`, `SetupRestartRequired`, `Stopping`, and `ShutdownIncomplete`. The only lifecycle-changing IPC is the narrow first-time setup request alongside the read-only status and existing health commands.

Shutdown requests are handled by Rust. During startup, the lifecycle records `Stopping`, leaves the UI/event loop responsive, requests worker drain, and checks shutdown intent throughout the trust chain and immediately around the final observation and activation. A late operational owner is never installed as `Ready`; it is moved to a blocking close worker. A ready owner is similarly removed from the state before close begins. Successful close permits exit. Failed close retains the complete ownership-bearing failure internally as `CloseRetryRequired` and exposes only `ShutdownIncomplete`. No close-retry UI, IPC command, or user action exists.

Lifecycle logging is fixed and coarse: startup reservation/worker completion/readiness/unavailability and shutdown request/pending/close outcome. Logs and status must not expose paths, keys, identifiers, metadata, native errors, or raw backend chains.

## Implemented lifecycle migration preparation and native custody dispatch

The existing dedicated `production-database-migration` OS thread owns the sequence: authorized/revalidated migration -> cross-process migration exclusivity -> operational-owner retirement -> exact authorization consumption -> canonical full-integrity migration source -> encrypted backup staging -> recovery-envelope construction and independent recovered-key verification -> C1 custody preparation -> C2b main-thread dispatch to C2a. The path does not publish a recovery set, create a writable maintenance owner, or execute migration.

Exclusivity is acquired before the lifecycle removes `OperationalProductionDatabase` from `Ready`. The worker explicitly closes that operational owner before consuming migration authorization. An operational close failure remains owned by the same worker with exclusivity retained, so neither authorization nor the later chain advances. The exact authorization is still created only by successful existing revalidation, is consumed once, and cannot produce a new `Pending` opportunity or renewal.

Backup staging uses the redacted typed `ProductionDatabaseMigrationBackupStagePath` with exact leaf `production-database-migration-backup.stage` directly beneath the Rust/Tauri-resolved application-local-data root. No frontend, caller, or environment path participates. The backup context comes only from canonical database-key persistence paths, and encrypted staging reuses the existing verified SQLCipher backup-stage primitive.

Migration exclusivity is thread-affine and worker-local. The worker retains it throughout preparation, dispatch, native-result resolution, and any close-only retry. `LifecycleInner` retains only coarse state; it does not own plaintext custody data, the Prepared owner, or exclusivity. Prepared remains off lifecycle-visible state until dispatch. No lifecycle or escrow mutex is held while C2a executes. `migration_work_resolved` remains false until privileged migration ownership and exclusivity are resolved.

`may_exit` is conjunctive: startup work, ordinary close work, setup work, and migration work must each be resolved; the ordinary lifecycle must be in its failed/stopped terminal state; and migration-confirmation ownership must be resolved. Confirmation state `Consumed` is not by itself sufficient while the migration worker retains Prepared custody or close-retry ownership.

Dispatch uses `AppHandle::run_on_main_thread` and returns the ownership-bearing native outcome through `std::sync::mpsc`. The real `main` webview window is resolved with `get_webview_window("main")`, its `HWND` is obtained with `window.hwnd()`, and that handle is used only as C2a's parent; it is not retained or logged. Verified custody is keyless and advances only to `CustodyVerifiedAwaitingPublication`.

Shutdown and the main-thread take meet at an explicit synchronization boundary. If shutdown intent wins before the main-thread take, Prepared remains unexposed and is recovered for terminal pre-exposure shutdown handling. If the main-thread take wins, shutdown cannot reconstruct or reclaim Prepared and waits for the ownership-bearing C2a outcome. Verified custody has an explicit terminal shutdown path through `abort_for_shutdown()`. Source-close failures retain only their appropriate close-only retry owner. Completion-send failure and impossible ownership states fail-stop rather than silently dropping authority.

The preparation claim has three outcomes. `NoWork` applies when no authorization exists. `Ready(OperationalProductionDatabase)` transfers the operational owner into normal preparation. `ShutdownWon(AuthorizedProductionDatabaseMigrationHandoff)` applies when authorization exists but ordinary shutdown already removed the operational owner: the worker consumes the exact authorization, skips full integrity/staging/envelope/custody, and closes the exact authorized source. Close failure remains worker-local and close-only retryable under retained exclusivity. The ordinary owner remains solely on the ordinary shutdown close path, preventing double-close.

This composition does not make the workflow operationally reachable. Production migration-discovery invocation, confirmation and cancellation commands, and frontend migration UI remain absent. C2a and C2b are private and unreachable through normal user interaction. Real main-window parenting, dialog modality against the running app, WebView blocking, event-loop behavior, keyboard traversal, Alt+F4/X behavior, shutdown during the actual dialog, high-DPI layout, screen-reader behavior, parent restoration, and the real two-paper-copy ceremony remain manually unvalidated. Layer D is only partially implemented through the complete first set and the second database, envelope, and manifest-last artifacts; independent exact second-directory layout verification, second complete-set verification, final two-set success, E restore, F retention/deletion, schema V2, migration SQL, and migration execution remain unimplemented.

## Approved Layer D durable recovery publication architecture (partially implemented)

Current state: both published recovery sets now have independent complete-set verification. The private keyless `SecondCompleteRecoverySetVerified` retains the published two-set predecessor and only type-level proof of the second set's exact-layout, fresh-artifact, correspondence, fresh-envelope authentication, recovered-key SQLCipher, and continuity checks. Final two-set Layer D capability and lifecycle composition remain unimplemented.

The implemented production slice publishes and independently verifies the complete first recovery set, then independently publishes and completely verifies the second directory's canonical `parish-data.db`, recovery envelope, and 98-byte manifest. `SecondCompleteRecoverySetVerified` retains `FirstCompleteRecoverySetAndSecondRecoverySetArtifactsPublished` plus only type-level proof that the second set passed exact-layout, fresh-artifact, correspondence, recovery-envelope authentication, recovered-key SQLCipher, and continuity verification. Lifecycle integration and final Layer D success remain unimplemented. The manifest remains a non-authoritative publication commit marker.

Layer D begins only from the existing keyless `RecoveryKeyCustodyVerifiedProductionDatabaseMigrationBackup`, which the current lifecycle parks at `CustodyVerifiedAwaitingPublication`. It performs no migration execution. Its conceptual success owner is a keyless Rust-owned capability named `TwoCompleteRecoverySetsVerifiedProductionDatabaseMigrationBackup` unless a later narrowly scoped implementation task requires an equivalent repository-conforming name. That capability proves only Layer D completion and grants no writable database ownership, schema mutation, restore, replacement, retention, deletion, cleanup, or migration execution authority.

Layer D requires exactly two complete digital recovery sets on two physically distinct, physically disconnectable external devices. Each device must be distinct from production storage and from the other recovery device. A second partition, volume, directory, mount point, or drive letter on one physical device does not qualify. The two devices contain redundant replicas of one logical backup set: the exact same retained verified encrypted SQLCipher backup bytes, exact same recovery-envelope bytes, and same migration-backup-set identifier. Set 2 is published independently from the retained verified source, never copied from set 1, and no second recovery key, backup-set identifier, envelope, or encrypted backup generation is created.

One complete digital set has exactly three fixed Layer D artifact names: `parish-data.db`, `migration-recovery-envelope-v1.bin`, and `recovery-set-v1.manifest`. They are constants rather than operator input or manifest content; there is no set-number suffix, localization, alias, case variation, arbitrary filename, or path field. The manifest is deterministic non-secret inventory and the Layer D publication commit marker. It is not a cryptographic trust root, authenticity proof, migration authority, restore authority, publication authority, or substitute for complete Layer D verification. File presence alone is insufficient: the final artifacts must be freshly reopened from the destination and independently verified.

### Recovery Set Manifest Format Version 1 (codec and source-fact construction implemented; Layer D incomplete)

The manifest is exactly 98 bytes. Its magic is the exact eight bytes `CHLDRSM\0` (`43 48 4C 44 52 53 4D 00`). Bytes `8..10` hold manifest version 1 as unsigned big-endian `u16`, canonically `00 01`. No total-length field exists, and trailing bytes are rejected.

| Offset | Width | Meaning |
| --- | ---: | --- |
| `0..8` | 8 | Exact manifest magic |
| `8..10` | 2 | Unsigned big-endian `u16` version, exactly 1 |
| `10..26` | 16 | Exact raw migration backup-set identifier |
| `26..34` | 8 | Exact byte length of `parish-data.db` as unsigned big-endian `u64` |
| `34..66` | 32 | Raw SHA-256 digest of the complete exact `parish-data.db` bytes |
| `66..98` | 32 | Raw SHA-256 digest of the complete exact recovery-envelope bytes |

Version 1 has no reserved bytes, padding, flags, extension area, optional or variable-width fields, delimiters, terminators, version negotiation, or fallback parser. The backup-set identifier is its exact raw 16 bytes, with no textual UUID, hex, Base64, byte-order transformation, or normalization. Every 16-byte value parses, but all zero is structurally invalid. The database length parses from every eight-byte pattern and is structurally valid only in the inclusive range `512..=281_474_976_579_584`. It proves neither existence, actual length, page alignment, nor SQLCipher, authorizes no allocation, and sets no Layer D operational size ceiling.

Both digests are exact raw 32-byte SHA-256 values, never hex or Base64. The database digest covers the complete exact `parish-data.db`; the envelope digest covers the complete exact 182-byte `migration-recovery-envelope-v1.bin`. Every digest pattern, including all-zero and all-`FF`, is structurally representable. The pure codec performs no hashing or comparison, and digest equality alone grants no trust or authority. The manifest implicitly requires the fixed envelope name, Migration Recovery Envelope Format V1, and exact 182-byte length without duplicating those facts as fields. An incompatible future recovery-envelope format requires separately approved manifest-format evolution.

The strict transition is raw `&[u8]` to `ParsedUntrustedRecoverySetManifestV1`: require length 98; require exact magic; decode big-endian version and require 1; copy `10..26`; decode `26..34` by infallible exact-width conversion; copy `34..66` and `66..98`; return parsed-but-untrusted state. Its complete semantic error taxonomy is `WrongTotalLength`, `WrongMagic`, and `UnsupportedVersion`; wrong length covers truncation and trailing data. There is no `MalformedFixedField` or generic decoding error, and outward formatting exposes no observed length/version/offset/byte or attacker-controlled value.

Structural validation transitions parsed state to `RecoverySetManifestV1` by checking, in order, nonzero identifier and the locked database-length range. Its complete taxonomy is `InvalidBackupSetIdentifier` and `InvalidDatabaseByteLength`. It performs no hashing, filesystem access, envelope parsing, database operation, or authority construction. Only the structurally validated type may canonically encode an exact `[u8; 98]` or equivalent fixed-size owned wrapper using the fixed magic/version and exact field bytes. Parsed-but-untrusted values cannot bypass validation. Every accepted structurally valid value satisfies `encode(validate(parse(bytes))) == bytes` across all 98 bytes; no alternate encoding exists.

Although the manifest is non-secret, normal production `Debug` and errors remain coarse: type/state names and fixed error variant names are sufficient. They never expose the complete identifier or digests, manifest bytes, database length, attacker-controlled values, paths, or destination/device identity. Parsing proves only framing and extraction. Structural validation proves only a nonzero identifier and in-range declared database length. Neither proves file existence, size or digest correspondence, source correspondence, envelope authenticity, recovered-key correctness, database validity, publication completion, device continuity or physical separation, Layer D success, migration authorization, or restore authorization.

The private dependency-free pure Rust codec and narrow trusted-internal constructor are implemented and deterministically tested. `RecoverySetManifestV1::from_trusted_internal_facts` accepts only a canonical `MigrationBackupSetIdentifier`, `u64` database byte length, and the two `[u8; 32]` digests; it still enforces `512..=281_474_976_579_584`. It changes none of the locked format, parser taxonomy, structural-validation taxonomy, offsets, encoding, or authority limits.

The keyless custody-verified source owner retains the verified encrypted stage and independently verified exact envelope. In addition to its private borrowed manifest-preparation operation, it now exposes one closure-scoped internal borrow of the exact retained `[u8; 182]` envelope for publication; there is no general byte getter. The borrow repeats strict framing parse only and does not reseal, regenerate, decrypt, or release recovery-key plaintext. The identifier remains derived through the existing strict parser/accessor path, never supplied independently or regenerated. Retained-stage observation and manifest construction remain unchanged, and success or failure preserves the original owner under the existing fixed redacted taxonomy.

This boundary derives four non-authority construction facts and creates no complete recovery set, migration authorization, or restore authority by itself. The retained-root, separation, capacity, and fixed-child prerequisites remain under their existing constraints. First-set `parish-data.db`, the exact retained 182-byte `migration-recovery-envelope-v1.bin`, and manifest-last `recovery-set-v1.manifest` publication with independent per-artifact fresh verification are implemented. Recovered-key verification, exact three-entry layout verification, independent first complete-recovery-set verification, and publication plus fresh verification of the second set's database, envelope, and manifest-last artifact from their original retained sources are also implemented. Partial files are left in place and are never resumed or cleaned up. The native picker, independent exact second-directory layout verification, second complete-set verification, final two-set Layer D success owner, lifecycle composition, normal product reachability, restore, retention/deletion, and migration execution remain unimplemented. Byte or digest equality and retained payload authority alone cannot establish Layer D success.

The private Windows prerequisite now first observes one retained volume as exactly one current physical disk and then consumes that proof into a retained external/disconnectable eligibility owner. Against the same retained volume handle it performs the bounded two-call `StorageDeviceProperty` query and accepts only `BusTypeUsb`, then performs `IOCTL_STORAGE_GET_HOTPLUG_INFO` with strict structure validation and requires `DeviceHotplug != 0`. `RemovableMedia`, `MediaRemovable`, and `MediaHotplug` are observed but are not acceptance conditions: removable media is not the same fact as an externally disconnectable device, and external SSD media may be non-removable. Revalidation repeats the underlying topology proof and both device observations. `RetainedEligibleNtfsRecoveryVolumeRoot` composes this owner with the exact selected root directory handle, its full initial `FILE_ID_INFO`, disk/directory/delete-pending/reparse facts, normalized handle-derived volume-GUID root, and exact NTFS observation. Revalidation requires the same full identity and exact root, repeats all hardened directory and NTFS checks, and revalidates topology plus external/disconnectable eligibility without refreshing ownership. Its consuming production-separation transition first revalidates production topology and the complete retained root, then delegates to the existing device-separation transition; the resulting owner retains the exact root and can repeat both revalidations and the distinction check. These opaque process-local owners expose none of the path, drive letter, volume GUID, filesystem name, disk number, native handle, or device strings and grant no publication, Layer D, restore, or migration authority.

The future operator selection is the exact external volume root, never an arbitrary existing folder. The production primitive accepts only an opaque Rust-private native-selection result; its path constructor is test-only because the native picker API remains unresolved and unimplemented. The retained open requests no mutation access, uses backup semantics and open-reparse-point behavior, and permits no delete sharing. The handle-derived normalized final path must be exactly `\\?\Volume{GUID}\`, with no child component; UNC/network, malformed, and unavailable forms fail closed. `GetVolumeInformationByHandleW` on the retained root must report NTFS under bounded ASCII case handling. exFAT is deferred, FAT32 is categorically rejected, and ReFS, unknown, malformed, unavailable, or any other result fails closed.

The fixed child beneath each root is `church-app-recovery-set`. Unrelated files elsewhere on the selected volume are allowed. Any pre-existing exact or ASCII-case-conflicting child is a conflict and is never adopted, merged, resumed, repaired, overwritten, renamed, or automatically deleted. The private creation boundary and retained destination authority remain unchanged. Destination-device continuity is enforced through first-set publication, recovered-key verification, the independent first complete-set boundary, and second-database publication. The complete-set boundary derives its search pattern only from retained first-directory authority and uses `FindFirstFileW`, `FindNextFileW`, and `FindClose` with exact `ERROR_NO_MORE_FILES` termination. A fixed three-slot state accepts exactly `parish-data.db`, `migration-recovery-envelope-v1.bin`, and `recovery-set-v1.manifest`, ignores only exact `.` and `..`, and rejects malformed names, case conflicts, duplicates, unrelated or fourth entries, enumeration errors, and close failure. It repeats the layout proof after fresh artifact verification. Each artifact is freshly reopened, identity-bound to its retained publication identity, checked to clean EOF and stable hardened facts, and compared with the freshly reobserved trusted source; manifest fields are checked against the actual database and envelope, including backup-set identity. The keyless, redacted, process-local `FirstCompleteRecoverySetVerified` owner retains the recovered-key predecessor and all original source/destination authority. `FirstCompleteRecoverySetAndSecondDatabaseArtifactPublished` retains that complete first-set owner plus the independently reopened and verified second database; it proves neither a complete second set nor Layer D completion. React receives no new surface. Ambiguous, unavailable, inconsistent, substituted, unsupported, non-NTFS, non-USB, non-hotplug, conflicting-child, or changed child/root observations fail closed. Existing test-only local-volume and device-property candidate classifiers remain test-only and non-authoritative.

Each destination is independently selected and validated, then published with create-new, no-replace semantics: no merge, overwrite, automatic repair, automatic deletion, or partial-publication resume. A later retry starts with a newly selected suitable destination. Partial artifacts remain for later separately authorized handling, and Layer F retains all future deletion authority. The application-local encrypted source stage remains retained after zero, one, and two complete sets.

First-set final verification independently proves the exact fixed layout, strict manifest parsing and structural validation, exact envelope/database facts, both artifact digests against the manifest, manifest/envelope backup-set identifier equality, exact backup and envelope correspondence with the retained verified source, the already-established recovered-key predecessor, and destination/device continuity. Set 2 currently contains its independently published and freshly verified database and recovery envelope; its manifest and complete-set boundary remain future work. Only a later two-complete-set ownership transition may construct Layer D success. Either valid paper bearer record may be used for necessary paper entry. The two verified paper copies remain a separate custody layer: each is separate from both digital devices and from the other paper copy; either can recover either digital set; no paper identity or permanent paper/device pairing is recorded; and the application does not claim later physical-separation proof.

Future Layer E may use the manifest only for candidate-set recognition and inventory after its own trusted selection boundary locates the three fixed artifact names. It may parse and structurally validate the manifest, obtain the backup-set identifier, verify sizes and digests, and compare manifest/envelope identity. None of those actions authorizes restore or database replacement; Layer E execution remains separately unapproved and undesigned here.

Recovery Set Manifest V1 rejects JSON, YAML, TOML, XML, CBOR, MessagePack, protobuf, serde-driven generic serialization, textual UUIDs, hex/Base64 digests, variable or operator-supplied filenames, paths, timestamps, copy numbers, set identities, generation counters, device/volume/operator/parish/installation identities, database-key-generation or setup-publication identifiers, self-digest, MAC/signature fields, reserved bytes, extension dictionaries, optional fields, flags, declared manifest or envelope lengths, a separate envelope-version field, permissive unknown-version handling, and trailing-data acceptance.

Durability claims are limited to successful supported writes, file-buffer flushes, closes, final-name publication, the strongest technically available supported namespace/directory durability operation, and fresh reopening/verification. They do not promise absolute survival from sudden power loss, defective media, controller/firmware failure, unsafe removal, or long-term media degradation.

Layer D progress may distinguish zero complete sets, first set publishing, one complete set, second set publishing/partial, and two complete verified sets. A completed first set remains complete if later work fails or is cancelled; partial sets are not resumed, repaired, or automatically deleted. Shutdown prevents new Layer D work, performs required secret destruction and handle closure, preserves close-only ownership when necessary, begins no cleanup/replacement/migration/new-destination work, and emits no Layer D success unless both sets were already fully established. Process-local capability state does not survive restart, so existing artifacts require full revalidation before future adoption.

# Product decisions

## Implemented foundation facts

- **Implemented stack:** Tauri 2, Rust, React, TypeScript, Vite, and npm.
- **Trust boundary:** Rust is trusted; React is presentation and interaction only.
- **Scope:** application shell, placeholder routing, a non-sensitive health command, read-only coarse `startup_status`, the narrow `request_first_time_setup` command, Rust-only storage and trust-chain foundations, the guarded production database chain, startup authorization, operational activation, Rust-owned startup/setup/orderly-shutdown lifecycles, and the implemented migration opportunity/revalidation, exclusivity, full-integrity-source, encrypted migration-backup-stage, and recovery-envelope foundations. Explicit first-time setup and the minimal production V1 metadata/bootstrap schema are implemented. The product still has no migration execution, parish/business schema, recovery-key custody, operational portable recovery, durable backup publication, restore, authentication, parish workflow, or general writable database interface and is not launch-ready.
- **Permanent identity:** production display name `Church App`, application identifier `io.github.cltubigon.churchapp`, version `0.1.0`.
- **Storage-path boundary:** the production database path is resolved only in Rust beneath Tauri's per-user local application-data directory and uses the fixed filename `parish-data.db`.
- **Pure persistence foundation:** Rust owns exact active and publication-stage names, typed path joins beneath caller-supplied synthetic roots, bounded protected-wrapper reading over `Read`, path-free persisted-presence classification, and pure evidence-last publication ordering for initial, evidence-only, and key-generation replacement operations. These are non-operational models only; they do not inspect, create, read, write, publish, replace, or select real files.
- **Windows filesystem binding preflight:** The accepted preflight compile-checks the approved storage-filesystem imports, exact signatures, structures, flags, pointer and length types, and `OwnedHandle` relationships. Those static checks remain alongside the separately scoped test-only runtime proof.
- **Single-wrapper temporary publication proof:** Windows tests now exercise initial no-replace publication for one deterministic synthetic authentication-key wrapper beneath unique test-owned temporary roots. This is test evidence only and adds no production persistence, loading, setup, startup, or operational authority.
- **Existing-file replacement proof:** Windows tests now also exercise one exact null-backup, zero-flag `ReplaceFileW` replacement after independent canonical active/stage validation and ordinary-handle closure, followed by fresh exact-name and file-identity verification. This is test-owned implementation evidence only, not production persistence or operational authority.
- **Normal-tree handle/path hardening proof:** Windows tests validate one ordinary synthetic component chain with retained directory handles, reject-all reparse facts, full `FILE_ID_INFO`, exact normalized volume-GUID components, same-volume checks, a wrapper-only one-link rule, and before/after bounded-read revalidation. This is normal-tree test evidence only and does not establish production path safety.
- **Real hard-link fixture proof:** Separate Windows tests for the active and staged authentication-key wrappers use `std::fs::hard_link` to establish a shared full file identity and canonical bytes at two distinct normalized paths, then prove exact-name and single-link rejection occurs before reading or injected mutation. Removing only the alias restores the original one-link identity and bytes. This is test evidence only and adds no production filesystem or publication authority.
- **Exact evidence-directory substitution test fact:** One Windows-only synthetic fixture observes that a byte-identical canonical wrapper moved into the exact `installation-evidence` path is detected by full directory-identity discontinuity before its injected continuation. This is an observed test fact, not production identity storage, enforcement, repair, persistence, or operational authority.
- **Retained evidence-directory replacement compatibility test fact:** On the observed Windows filesystem, the exact validated `installation-evidence` handle using `GENERIC_READ`, read/write sharing without delete sharing, backup semantics, and open-reparse-point semantics remained open while the accepted one-shot child replacement succeeded and fresh child inspection completed. This is an observed Windows-test fact, not production enforcement or filesystem authority.
- **Local-volume candidate test fact:** On the observed Windows host, a strict handle-derived normalized volume-GUID path combined with the fixed `GetDriveTypeW` category was classified as non-authoritative `LocalFixedCandidate`. This is test evidence only and does not establish production suitability, removable-media assurance, hot-plug assurance, or operational authority.
- **Device-property candidate test fact:** On the observed Windows host, the retained-root-derived volume device opened once with desired access zero and read/write sharing, then accepted one bounded header query and one bounded full `IOCTL_STORAGE_QUERY_PROPERTY` query. The strict descriptor reported non-removable media and an approved candidate bus family, yielding only private `DevicePropertyCandidate`. No hot-plug query ran, and this fact establishes no device non-removability, internal or physical locality, virtual/remote-backing exclusion, durability, production suitability, or application authority.
- **Controlled-host baseline and USB test facts:** Under `Current Windows account, non-elevated session; administrator-group membership not established.`, the Windows 11 baseline beneath the operating-system temporary directory produced the accepted non-authoritative `LocalFixedCandidate` and `DevicePropertyCandidate` results with one drive-type call, one volume open, two property IOCTL calls, zero hot-plug calls, sentinel preservation, and exact-root cleanup. This is not dedicated-standard-account evidence or production approval. Two manually rooted USB attempts ran: the first stopped broadly during hardened child-directory validation, and the accepted diagnostic rerun stopped at `DirectoryAttributeInfoUnavailable` with disposition `Unavailable`. The USB case remained failed and unclassified; no downstream drive-type, local-volume, or device-property classification occurred, and no filesystem or hardware cause was established. Drive-type, volume-open, property-IOCTL, and hot-plug calls were all zero; sentinel verification was `NotReached`; exact-root cleanup was attempted once and succeeded; and the manual leftover-folder check found no entries. No production or operational authority resulted.
- **Current storage limitation:** the Rust-owned startup worker invokes the implemented read-only observation, loading, guarded connection, validation, authorization, and operational-activation chain beneath the fixed per-user application-local-data root. The separate explicit setup worker can create only the encrypted production database and minimal V1 metadata/bootstrap contract. No general writable application-data operation, migration, replacement, recovery, parish/business schema, or parish workflow is authorized.
- **Initialization boundary:** ordinary startup leaves a never-initialized installation setup-required and cannot authorize creation. The separate explicit first-time setup lifecycle begins only from the accepted `NeverInitialized` path through its narrow request command. Successful setup ends in restart-required, never same-process `Ready`; a fresh process must pass the ordinary startup trust chain.
- **Missing-storage behavior:** an initialized installation whose expected storage is missing fails closed and is not treated as a new installation. Inconsistent or unavailable evidence also fails closed.
- **Installation-evidence contract:** Rust now models and structurally validates the platform-neutral logical evidence fields. Structural success establishes only local contract coherence; it is not authentication, persistence, database cross-checking, startup authority, or production trust.
- **Installation-evidence encoding and parsing:** Version 1 is exactly 164 bytes with a 12-byte framing prefix, a 152-byte payload, fixed offsets, and canonical big-endian integer representation. Strict Rust-only parsing accepts only that exact form and returns a distinct parsed-but-untrusted type. Structural validation remains a separate explicit boundary.
- **Authenticated-envelope framing and authentication:** The approved version-1 envelope is exactly 226 bytes with `CHEVAUTH` magic, envelope version 1, fixed authentication algorithm identifier 1, a distinct nonzero 16-byte authentication-key generation identifier, declared plaintext length 164, the unchanged canonical plaintext, and a complete 32-byte HMAC-SHA-256 tag. Authentication covers exact bytes `0..194`; the tag occupies `194..226` and is excluded from its own computation.
- **Evidence-authentication-material generation:** Rust generates each per-parish 32-byte evidence-authentication key and its nonzero 16-byte generation identifier independently from the operating-system randomness provider. No universal or deterministic key exists. Generation returns only Rust-owned, redacted in-memory material; success is neither persistence nor activation and establishes no current-generation, database, setup, startup, or operational authority.
- **In-memory DPAPI protection:** On Windows, Rust can protect the authentication-key payload and authenticated-evidence envelope as separate opaque objects with classic current-user DPAPI. Calls are non-interactive (`CRYPTPROTECT_UI_FORBIDDEN`) and accept no optional entropy or description. This stage performs no filesystem or registry persistence.
- **Protection limits:** DPAPI success establishes only same-profile local protection or unprotection. HMAC verification and authenticated key-generation matching remain required before plaintext release; DPAPI does not establish freshness, rollback resistance, database correspondence, startup permission, or operational authority.
- **Authentication limits:** Successful authentication establishes only that a holder of the caller-supplied key authenticated the exact envelope prefix. It does not establish freshness, rollback protection, current generation policy, database correspondence, startup authority, or operational installation state.

## Carlo-approved production database foundation, guarded handoff, and validation

Carlo explicitly approved the bounded production database and evidence-correspondence architecture package recorded below, including the corrected database-format identity encoding. The architecture has crossed the guarded read-only connection-handoff, validation, startup authorization, operational activation, Rust-owned startup lifecycle, explicit first-time setup, minimal V1 metadata/bootstrap, migration-opportunity/revalidation, migration-exclusivity, full-integrity-source, verified encrypted migration-backup-stage, and recovery-envelope verification gates. Engine selection, setup authority, startup authority, migration authority, each validation gate, operational activation, recovery-envelope verification, custody, publication, restore, and final release approval remain distinct decisions. The repository still has no migration execution, parish/business schema, recovery-key custody, operational portable recovery, durable backup publication, restore, rekey, recovery, authentication, parish workflow, or general frontend database operation.

Delegated architecture and security decisions accepted as part of Carlo's package approval are the read-only/no-create startup inspection semantics, exact correspondence and lineage rules, diagnostic-only timestamps, path restrictions, error redaction, rollback-journal and durability policy, and separation of setup, startup, migration, recovery, and destructive authority.

- **Production engine and dependency gate:** SQLCipher Community Edition is selected. Plain SQLite is prohibited for production parish databases, production backups, and production recovery databases. Windows production now depends on exactly `rusqlite = { version = "=0.39.0", default-features = false, features = ["backup", "bundled-sqlcipher-vendored-openssl"] }` under `[target.'cfg(windows)'.dependencies]`, with no Windows `rusqlite` development dependency and no direct `libsqlite3-sys` dependency. The earlier feasibility module and former development-dependency state are historical test-only evidence. This promotion does not itself open or authorize a production database. SQLCipher/OpenSSL versions and the lockfile remain required release evidence; final release approval has not been granted by this gate.
- **Raw-key primitive gate:** the private Windows-only `sqlcipher_database_key_application` module is registered from Rust and has no public, Tauri, frontend, or IPC surface. Its primitive accepts only `&GenerationBoundDatabaseKey` plus a caller-supplied already-open handle, creates one fixed 67-byte lowercase `x'<64 hex characters>'` owner, invokes `sqlite3_key` exactly once, accepts only `SQLITE_OK`, and exposes only `DatabaseKeyApplicationError::Failed` on failure. It does not itself open, create, select, query, inspect, mutate, rekey, or close a database. The guarded handoff remains its only application-level caller; the lifecycle reaches it only through that handoff.
- **Guarded connection-handoff gate:** after existing metadata-only file inspection, a separate private connection-lifetime guard is identity-bound to the inspected proof. The implementation makes exactly one application-level path-based `Connection::open_with_flags_and_vfs(..., "win32")` call with `SQLITE_OPEN_READ_ONLY | SQLITE_OPEN_FULL_MUTEX | SQLITE_OPEN_PRIVATE_CACHE | SQLITE_OPEN_NOFOLLOW`, identity-binds SQLite's borrowed main-database Windows handle to the same proof, applies the connection-hardening policy, applies the generation-bound key exactly once, and only then enables and verifies query-only. The returned owner is opaque and keyed-but-unvalidated; it carries the connection, guard, and proof together and exposes only consuming close or close-retry transitions. The Rust startup worker consumes it through the fixed later stages; the handoff alone grants no startup authority.
- **Supported production baseline:** Windows 10 x64 and Windows 11 x64, local NTFS, non-elevated standard-user operation, and CurrentUser DPAPI. Windows ARM64, 32-bit Windows, ReFS, removable or network storage, Wine, Windows Server, and compatibility layers are unsupported until separately approved.
- **Database key:** one independent random 256-bit database key generated by the Windows OS cryptographic RNG. It is never password-derived, never derived from parish, installation, evidence, device, or user values, and never shared with the evidence HMAC key. Secret owners must not implement `Copy` or expose unrestricted `Clone`; formatting is redacted and owned secret buffers receive best-effort zeroization. A dedicated CurrentUser-DPAPI wrapper protects the live key. Portable recovery uses a separate random recovery key and authenticated recovery envelope; custody, duplication, rotation, loss, and destruction remain explicit operator/security responsibilities.
- **Metadata contract:** version 1 is exactly one canonical non-null row in the singleton table `church_app_database_metadata`; missing, duplicate, partial, wrong-storage-class, wrong-length, unsupported, or malformed values fail closed. Initial database schema version is 1.
- **SQLite headers:** `application_id = 0x43484150`; `user_version` mirrors the validated supported metadata `database_schema_version`, and metadata is authoritative. A wrong fixed application ID is `WrongApplicationId`; a supported validated metadata schema version that differs from `user_version` is `UserVersionMismatch`; correctly represented unsupported metadata versions retain their dedicated unsupported categories; representation defects are malformed. No automatic repair, assignment, normalization, or fallback exists.
- **First business-schema version:** the implemented metadata/bootstrap source is `database_schema_version = 1`. The approved first business target is `database_schema_version = 2`, and version 2 means exactly the approved `service_requests`, `request_schedule_occurrences`, and `request_cancellation_reviews` physical contract below and nothing broader. Target `metadata_contract_version` remains 1, target SQLite `user_version` is 2, `application_id` remains `0x43484150`, and `ApplicationDatabaseFormatIdentity` remains the existing identity. Compatibility is 1 -> 2 only; there is no generic `unsupported -> 2` migration rule, and versions newer than 2 are unsupported and downgrade-refused. This assignment does not implement schema 2 or authorize migration.
- **Correspondence and freshness:** the implemented and accepted identity-only correspondence adapter invokes the existing pure classifier exactly once for exact typed equality of permanent application identifier, application database-format identity, parish identifier, installation identifier, database-key generation identifier, and setup-publication identifier. It ignores installation generation and recovery/replacement generation while retaining both unchanged for freshness. Evidence-format identity/version and database/evidence timestamps cannot authorize freshness. The implemented freshness boundary consumes the correspondence owner plus exactly one preloaded `NormalizedFreshnessAnchorObservation`, reuses the pure freshness classifier exactly once, advances only on `Fresh`, and retains no anchor in its success owner.
- **Implemented readability-and-integrity gate:** the consuming transition starts from `ProductionReadOnlyDatabaseConnection` and runs only `PRAGMA cipher_integrity_check` to normal zero-row completion, followed by `PRAGMA main.quick_check(1)` to exactly one complete SQLite `TEXT` value `ok` and normal end-of-stream. No separate readability probe exists; the limited quick check establishes SQLite-level readability. The gate does not query `sqlite_master`, metadata tables, `application_id`, `user_version`, product tables, or arbitrary content, and it exposes no unrestricted SQL. Success returns `ReadabilityAndIntegrityValidatedProductionDatabaseConnection`.
- **Implemented live metadata and header gate:** the consuming transition starts from `ReadabilityAndIntegrityValidatedProductionDatabaseConnection`, strictly observes `PRAGMA main.application_id`, requires exact `0x43484150`, strictly observes `PRAGMA main.user_version`, executes only the fixed explicit 12-column metadata query with `LIMIT 2`, copies the first row before the second step, adapts exact SQLite storage classes with strict UTF-8, calls the existing `RawDatabaseMetadataRow::parse()` and `validate_structure()`, and requires `user_version` agreement with the supported metadata schema version. Success returns `LiveMetadataAndHeaderValidatedProductionDatabaseConnection` retaining the unchanged lifetime unit and one owned `DatabaseMetadataContractV1`.
- **Readability-and-integrity failures:** that gate has exactly four primary coarse categories: `EncryptedDatabaseAuthenticationOrCipherIntegrityFailed`, `SQLiteReadabilityOrIntegrityFailed`, `ValidationUnavailable`, and `ValidationInterruptedOrIncomplete`. Wrong key is not separated from cipher-envelope/HMAC failure, and corrupt SQLite content is not separated from unsupported or unreadable SQLite representation. Result text, SQL/PRAGMA text, native codes, and raw errors are never exposed. A validation close failure preserves the already determined primary category together with an opaque close-retry owner holding the complete connection, guard, and inspection proof.
- **Readability-and-integrity completion and cancellation:** no active cancellation API or rusqlite hooks feature is approved for that stage. No check succeeds without its complete terminal result. The first cipher result row already proves failure and may end row consumption after the stream is dropped or reset; a cipher success requires clean zero-row completion. Quick-check zero/multiple/non-text/non-`ok` results, statement errors, interruption, malformed shape, or premature termination fail closed. Full integrity checks remain reserved for controlled backup acceptance, staged restore, migration, rekey/key replacement, journal recovery, database replacement, diagnostics, and abnormal-interruption recovery.
- **Implemented private full-integrity capability:** the consuming transition starts from `ReadabilityAndIntegrityValidatedProductionDatabaseConnection`, retains the guarded connection lifetime, runs only fixed `PRAGMA main.integrity_check`, and advances to `FullIntegrityValidatedProductionDatabaseConnection` only for exactly one SQLite `TEXT` row exactly equal to `ok` followed by normal completion. Malformed, unavailable, interrupted, incomplete, zero-row, extra-row, non-`TEXT`, and non-`ok` outcomes fail closed. Explicit close and ownership-bearing close retry are preserved. The boundary exposes no connection, arbitrary SQL, path, key, metadata, diagnostic text, raw handle, or backend error. Later implementation composes it only into the migration source consumed by the encrypted backup stage; ordinary startup, setup, restore, recovery, IPC, and frontend remain unchanged.
- **Whole-file workload acceptance:** `cipher_integrity_check` may synchronously scan the entire database with work proportional to database pages. This gate introduces no production database-size ceiling and makes no bounded-latency or active-cancellation claim. The accepted lifecycle runs the whole synchronous chain in one Rust blocking worker, off the UI/event loop; this does not bound worker duration or eliminate shutdown drain time.
- **Validation connection and authority:** both checks run sequentially on the same privately owned connection while the existing guard, inspection proof, query-only setting, defensive mode, and read-only opening remain active. No explicit SQLite transaction or stronger snapshot guarantee is added. Success is possession of a new opaque validated owner retaining the same lifetime unit and exposing only consuming close and later separately approved fixed consuming transitions. It establishes no metadata table existence, metadata row validity, SQLite `application_id`, SQLite `user_version`, schema-contract validity, database/evidence correspondence, freshness, startup/setup authority, migration authority, recovery authority, backup/restore authority, or operational database use.
- **Path, transaction, and authority policy:** the fixed `parish-data.db` must be in the exact application-owned production directory on local NTFS. Reparse points, symlinks, junctions, mount traversal, cloud placeholders, hard links, network paths, removable storage, unexpected sidecars, WAL, and SHM fail closed. Initial mode is rollback-journal `DELETE`, `synchronous=FULL`, explicit transactions, `secure_delete=ON`, and `auto_vacuum=NONE`, with no automatic journal switching, VACUUM, cleanup, or repair. Setup alone may create database artifacts. Ordinary startup creates none. Migration, recovery, restore, rekey, anchor or database replacement, and destructive cleanup each require separate explicit authority; no generic repair authority exists.

### Approved metadata fields and encodings

- `singleton_id`: SQLite `INTEGER`, canonical fixed value 1.
- `metadata_contract_version` and `database_schema_version`: positive SQLite `INTEGER` values.
- `permanent_application_identifier`: canonical bounded UTF-8 SQLite `TEXT`, exact value `io.github.cltubigon.churchapp`.
- `database_format_identity`: the existing typed `ApplicationDatabaseFormatIdentity`, SQLite `BLOB`, exactly 16 bytes, direct typed decoding, and exact typed equality. It is never text: never UTF-8, hex, UUID text, Base64, numeric reinterpretation, alternate spelling, hash, prefix, or fingerprint.
- `parish_identifier`, `installation_identifier`, `database_key_generation_identifier`, and `setup_publication_identifier`: SQLite `BLOB`, exactly 16 bytes each.
- `installation_generation` and `recovery_replacement_generation`: SQLite `BLOB`, exactly 8-byte big-endian encodings each.
- `database_created_at`: non-negative UTC Unix milliseconds in SQLite `INTEGER`; diagnostic only.

### Implemented live-observation decisions and deferred approval gates

The engine, dependency/raw-key primitive, guarded connection-handoff, readability/integrity gate, live metadata/header gate, identity-only correspondence gate, preloaded normalized freshness gate, startup-authorization boundary, operational activation, application lifecycle, explicit first-time setup, minimal V1 metadata/bootstrap creation, and recovery-envelope backup verification are implemented. Their fixed operations, taxonomy, ownership, close-failure behavior, and authority limits are canonical. Migration, parish/business schema, recovery-key custody, operational portable recovery, durable publication, restore, recovery authority, authentication, parish workflows, broader frontend/database IPC, destructive retention/cleanup, and final release approval remain separate gates.

### Implemented live-observation decisions

- **Fixed scope and row count:** the transition strictly observes `PRAGMA main.application_id`, fails immediately on a complete wrong value, then strictly observes `PRAGMA main.user_version`, and finally executes the explicit 12-column query from `main.church_app_database_metadata` with `LIMIT 2`. The query has no `singleton_id` filter. `LIMIT 2` is the bounded distinction between zero, exactly one, and more than one row; unbounded enumeration is prohibited.
- **Canonical primary taxonomy:** exactly `HeaderObservationUnavailable`, `WrongApplicationId`, `MetadataObservationUnavailable`, `MetadataObservationInterruptedOrIncomplete`, `MetadataRowMissing`, `DuplicateMetadataRows`, `MalformedMetadata`, `UnsupportedMetadataContractVersion`, `UnsupportedDatabaseSchemaVersion`, and `UserVersionMismatch`. `CloseFailed` is an ownership-bearing result, not a primary category.
- **No table-missing category:** absent named relation, missing required column, malformed relation shape, and other non-preparable fixed-query states map coarsely to `MetadataObservationUnavailable`. The implementation must not infer a table-specific state by parsing raw SQLite or rusqlite error messages. A successfully started fixed query that normally completes with zero rows is separately `MetadataRowMissing`.
- **Version and malformed rules:** a correctly represented `metadata_contract_version` other than 1 is `UnsupportedMetadataContractVersion`; a correctly represented `database_schema_version` other than 1 is `UnsupportedDatabaseSchemaVersion`; wrong storage class, invalid UTF-8, or parse-range failure is `MalformedMetadata`. Unsupported versions take precedence over user-version comparison. Remaining structural defects are malformed.
- **First-failed-stage precedence:** application-ID observation unavailable; wrong application ID; user-version observation unavailable; metadata preparation/query-startup unavailable; metadata stepping interruption or incomplete terminal state; missing row; duplicate rows; malformed storage class, UTF-8, parse, or non-version structural state; unsupported metadata contract version; unsupported database schema version; remaining structural invalidity as malformed; user-version mismatch. Processing stops at the first failure and neither collects nor exposes multiple defects.
- **Success and close ownership:** `LiveMetadataAndHeaderValidatedProductionDatabaseConnection` privately retains only the unchanged `ConnectionLifetimeOwner` and one owned `DatabaseMetadataContractV1`; successful validation does not retain header values separately. It exposes consuming explicit close and later separately approved fixed consuming transitions only. On primary failure, close failure preserves the category and complete connection/guard/inspection unit across consuming retries. Closing the successful owner first discards its metadata contract; a failed close retains only the complete lifetime unit.
- **Placement and authority:** implementation resides in a sealed private child module beneath `production_database_connection_handoff`, without widened visibility, a generic connection callback, or a crate-root sibling bridge. Production only reads the already-existing named relation; test-only synthetic schema/header construction grants no production schema, header-mutation, migration, repair, normalization, correspondence, freshness, startup, setup, recovery, backup/restore, replacement, or operational authority.

### Implemented and accepted identity-only correspondence decisions

- **Mandatory consumed inputs:** `validate_production_database_evidence_correspondence` consumes `LiveMetadataAndHeaderValidatedProductionDatabaseConnection` and exactly `TrustedCurrentInstallationEvidenceAssessment`. No weaker evidence type is approved, and the adapter performs no evidence loading.
- **Sole comparison authority:** implementation calls `classify_database_metadata_correspondence(&metadata_contract, trusted_assessment.evidence())` exactly once. The existing pure classifier alone compares permanent application identifier, application database-format identity, parish identifier, installation identifier, database-key generation identifier, and setup-publication identifier; the live adapter neither duplicates nor reinterprets those comparisons.
- **Excluded fields:** installation generation, recovery/replacement generation, evidence-format identity, evidence-format version, database creation timestamp, and evidence creation timestamp do not affect correspondence. Both generation values remain retained only for the later anchor-based freshness boundary.
- **Single primary taxonomy:** the only primary category is unit-like `DatabaseEvidenceCorrespondenceMismatch`. There are no field-specific variants, `CorrespondenceUnavailable`, evidence-loading failures, lineage/freshness categories, or primary `CloseFailed` category. One or many mismatches map to the same category. Internal comparison order is not outward behavior; field-level precedence, counts, indexes, sets, and diagnostics are intentionally unobservable.
- **Opaque success:** `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection` privately retains exactly the unchanged `ConnectionLifetimeOwner`, one owned `DatabaseMetadataContractV1`, and one owned `TrustedCurrentInstallationEvidenceAssessment`. It retains no separate `DatabaseMetadataCorrespondence::Corresponds`; possession of the owner is the correspondence proof. The trusted assessment preserves authenticated loading provenance, validated evidence, and `TrustedCurrentInstallationIdentity` without paths, wrapper bytes, keys, envelope bytes, native errors, or loader state.
- **Freshness handoff:** the implemented freshness boundary consumes the whole correspondence owner and one preloaded `NormalizedFreshnessAnchorObservation`; correspondence itself still does not classify equal lineage, stale evidence, stale database, opposing movement, rollback suspicion, or ambiguity.
- **Mismatch close ownership:** metadata and trusted evidence are discarded before explicit close. Successful close returns the mismatch category; close failure retains only the same category and complete lifetime owner in `DatabaseEvidenceCorrespondenceValidationCloseFailure`. Consuming retries attempt only close, preserve both on repeated failure, and return the original mismatch on success.
- **Successful-owner close:** metadata and trusted assessment are discarded before close. A failed close reuses `ProductionDatabaseConnectionCloseFailure` and retains only the lifetime owner; no parallel general close system is approved.
- **Placement and test seam:** implementation resides in the private nested `live_metadata_and_header_validation/database_evidence_correspondence_validation.rs` child without visibility widening or connection exposure. Tests use the `#[cfg(test)] pub(crate)` synthetic-assessment function in the trusted-assessment module; it derives trusted identity internally, accepts no separate identity, performs no loading, DPAPI, HMAC, filesystem access, or parsing, and is absent from production compilation.
- **Authority:** success proves preceding stages, six-field correspondence, continued ownership of the same guarded lifetime, retention of the compared trusted assessment, and internal availability of both generations. It proves no lineage equality, freshness, rollback resistance, startup or operational authority, setup completion, migration state, physical DDL/object kind, wider schema correctness, recovery fitness, backup/restore suitability, replacement authority, or business-data correctness.

### Implemented and accepted preloaded normalized freshness-composition decisions

- **Mandatory consumed inputs:** `validate_production_database_freshness` consumes `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection` and exactly `NormalizedFreshnessAnchorObservation`. All four normalized variants are accepted. Paths, persistence paths, loaded wrappers, authenticated anchors, installation-bound anchors, a bare assured anchor, alternate inputs, and generic inputs are prohibited.
- **No hidden loading:** presence inspection, filesystem and path access, wrapper loading, DPAPI, HMAC, parsing, authentication, generation matching, installation binding, assurance, and normalization finish upstream. The adapter also performs no SQL, PRAGMA, query, row read, or database-content operation.
- **Possession translation:** the sealed nested adapter constructs one ephemeral `DatabaseMetadataCorrespondence::Corresponds` only as the pure classifier argument. It does not rerun correspondence, retain or return the enum, expose a detachable proof, or add it to the success owner.
- **Sole freshness authority:** implementation calls `classify_database_freshness(DatabaseMetadataCorrespondence::Corresponds, &metadata_contract, trusted_assessment.evidence(), &anchor_observation)` exactly once. The existing pure classifier remains unchanged and solely owns generation comparisons, anchor-identity comparisons, lineage combination, precedence, and timestamp exclusion.
- **Fresh-only advancement:** only `DatabaseFreshnessClassification::Fresh` returns `DatabaseFreshnessValidatedProductionDatabaseConnection`. Every other classification is non-success and triggers explicit close. A non-Fresh result never retains a usable database owner, and `Fresh` never appears in `Failed`, close-failure ownership, or close-retry results.
- **Primary taxonomy:** the live stage reuses `DatabaseFreshnessClassification` directly. Non-Fresh plus successful close is `Failed(classification)`; failed close is ownership-bearing `CloseFailed`. No duplicate live freshness variants are approved, and close behavior cannot change classification precedence.
- **Opaque success:** `DatabaseFreshnessValidatedProductionDatabaseConnection` privately retains exactly the unchanged `ConnectionLifetimeOwner`, one `DatabaseMetadataContractV1`, and one `TrustedCurrentInstallationEvidenceAssessment`. It retains no classification, correspondence value, normalized observation, assured/authenticated/bound anchor, anchor contract or fields, path, wrapper, loader state, or protection error. The observation and any assured anchor are discarded after classification, including on success.
- **Non-success close ownership:** after classification, metadata, trusted assessment, observation, and any assured anchor are discarded before close. Successful close returns the original non-Fresh classification. `ProductionDatabaseFreshnessValidationCloseFailure` retains only that classification and the complete lifetime owner; consuming retries attempt only close, preserve both on repeated failure, and return the original classification on eventual success.
- **Successful-owner close:** metadata and trusted assessment are discarded before close. Failure reuses `ProductionDatabaseConnectionCloseFailure` and retains only the lifetime owner; no parallel general close system is approved.
- **Placement and tests:** implementation is a nested child at `live_metadata_and_header_validation/database_evidence_correspondence_validation/database_freshness_validation.rs`, avoiding production visibility widening, a crate-root bridge, or an arbitrary production `Connection` callback. Tests use genuine correspondence owners and synthetic preloaded observations; injected close callbacks are `cfg(test)`-only.
- **Redaction:** only a payload-free non-Fresh classification name may appear. Owners and ownership-bearing failures remain redacted; no identifiers, generations, timestamps, metadata, evidence, trusted identity, anchors, protection state, paths, SQL/PRAGMA text, native details, handles, connection details, or raw errors may escape. No ordinary logging is required.
- **Authority:** possession proves only the preceding chain, exactly-once pure classification with ephemeral `Corresponds`, an assured present observation, passing three-way identities, two `Current` lineages, `Fresh`, and retention of the same guarded lifetime. It proves neither absolute newest state nor coordinated-rollback detection and grants no startup, operational, setup, migration, schema, recovery, replacement, backup/restore, or business-data authority.
- **Sequencing:** the lifecycle re-observes canonical installation evidence immediately before calling the implemented startup-authorization boundary and passes the actual second observed value. The authorized owner is then consumed by operational activation. No later consumer is assumed to need retained anchor ownership.

### Implemented Carlo-approved startup-authorization decisions after freshness

- **Exact preloaded inputs:** `authorize_production_database_startup` consumes exactly `DatabaseFreshnessValidatedProductionDatabaseConnection` and one preloaded `installation_state::InstallationEvidence`, returning `ProductionDatabaseStartupAuthorizationOutcome`. The boundary performs no hidden installation-state loading and no filesystem, path, sidecar, database, anchor, environment, current-account, administrator-group, standard-user, or elevation observation.
- **Only successful observation:** only `InstallationEvidence::Initialized(ExpectedStorageEvidence::Present)` may authorize. `NeverInitialized`, `Initialized(Missing)`, `Initialized(Unavailable)`, `Inconsistent`, and `Unavailable` all fail closed and dispose of the database owner through explicit close.
- **Exact coarse taxonomy:** `NeverInitialized` maps to `ProductionDatabaseStartupAuthorizationError::NeverInitialized`; `Initialized(Missing)` maps to `ExpectedStorageMissing`; `Inconsistent` maps to `InstallationStateInconsistent`; and both `Initialized(Unavailable)` and `Unavailable` map to `InstallationStateUnavailable`.
- **Setup remains separate:** `NeverInitialized` does not grant `FirstTimeSetupAuthorization`, does not become `SetupPermitted`, and does not branch into setup. Startup authorization must not call `authorize_first_time_setup`, construct `FirstTimeSetupAuthorization`, use `SetupAuthorizationState`, or use `StorageDecision` as authority. The existing explicit first-time-setup boundary remains the sole setup authorization path.
- **Eligibility is not authority:** `FutureOpenPermitted` retains its standalone installation-state meaning and is not startup authority. `Initialized(Present)` becomes startup authority only when combined at this boundary with possession of the freshness-validated database owner.
- **Distinct typed owner:** success is represented only by opaque `StartupAuthorizedProductionDatabaseConnection`, distinct from `DatabaseFreshnessValidatedProductionDatabaseConnection` and from any later operational database owner. It retains exactly `ConnectionLifetimeOwner`, `DatabaseMetadataContractV1`, and `TrustedCurrentInstallationEvidenceAssessment`; it retains no installation-state observation, setup/storage decision, authorization flag, freshness/correspondence result, anchor, path, or sidecar observation.
- **No earlier-stage reruns:** freshness, correspondence, metadata/header, readability/integrity, database-file inspection, evidence loading, path/sidecar inspection, and anchor loading/normalization have no precedence inside this adapter and are not rerun. One fixed policy decision is made over the supplied `InstallationEvidence`.
- **Failure ownership:** after policy failure, installation evidence, metadata, and trusted assessment are discarded before the lifetime owner is explicitly closed. Close success returns the original category. Close failure retains exactly the category plus complete lifetime ownership. A consuming retry performs only close, preserves both across repeated failure, and returns the original category on eventual success.
- **Successful-owner close:** closing the startup-authorized owner discards metadata and trusted assessment before close and reuses `ProductionDatabaseConnectionCloseOutcome` and `ProductionDatabaseConnectionCloseFailure`; a failure retains only lifetime ownership.
- **Authorization is not activation:** the startup owner exposes no `Connection`, SQL/query interface, arbitrary callback, path, metadata, trusted-assessment, or evidence accessor. The implemented consuming transition `activate_production_database_for_operational_use` creates the real operational owner after authorization; it is not another authorization decision. Startup authorization grants no setup, creation, migration, recovery, replacement, rekey, backup/restore, repair, cleanup, sidecar/WAL/SHM, account/elevation, or arbitrary SQL authority.
- **Private placement and redaction:** implementation resides as private child `database_freshness_validation/startup_authorization.rs`, declared by `database_freshness_validation.rs`, without a crate-root bridge or visibility widening. Manual coarse `Debug` may reveal only the four payload-free primary names; owners and ownership-bearing failures remain redacted.

### Implemented first-time setup and bootstrap decisions

- **Setup authority:** setup begins only from the accepted `NeverInitialized` path and only through the explicit narrow setup request. Startup authorization cannot substitute for setup authorization, and setup authorization cannot substitute for future migration authority.
- **Bootstrap scope:** setup creates encrypted `parish-data.db` and initializes only the production V1 metadata/header bootstrap contract. This is not a parish/business schema, defines no parish workflow tables, and does not complete broad product schema creation.
- **Completion rule:** setup validates the database, prepares and publishes the protected installation artifacts under the existing publication rules, completes final active verification, and then reports restart-required.
- **Fresh-process rule:** setup completion never installs same-process `Ready`. Only a fresh process that passes the ordinary startup trust chain may install the operational owner and report `Ready`.
- **Migration status:** migration execution remains unimplemented. The repository now has migration opportunity/revalidation, exact maintenance authorization binding, dedicated exclusivity, a full-integrity-validated source, an independently verified encrypted SQLCipher migration-backup stage, and recovery-envelope backup verification. The resulting capability is not yet operational portable recovery or durable publication. The exact version compatibility pair is source 1 to target 2 only; writable ownership, custody/publication, interruption/restart classification, ordered metadata/`user_version`/evidence/freshness/lineage updates, and later execution-chain close-failure composition remain required. Merely observing schema 1 creates neither eligibility nor authorization. Migration cannot become a startup/setup fallback.

### Locked maintenance authorization trigger and taxonomy decision

- **Authorizing actor and trigger:** the maintenance-authorizing actor is the local human operator physically using Church App under the dedicated standard Windows account used for production operation. Maintenance authority arises only from an explicit interactive maintenance confirmation initiated from Church App, not from merely being the Windows user.
- **Consent, not application identity:** the confirmation is an authorization and consent boundary, not proof of application-level user identity. Application-level authentication may strengthen it later, but first-generation maintenance architecture does not depend on application authentication existing.
- **Required lifetime and scope:** future maintenance authority must be explicit, interactive, single-use, process-local, non-persistent, non-renewing, and scoped to exactly one approved maintenance operation.
- **Prohibited inference and automation:** authority must not be inferred, granted on startup, granted on setup completion, granted because migration is required, survive restart, be serialized or persisted, be reconstructed from frontend-supplied Boolean, string, path, or version values, or authorize another operation. Automatic authorization and automatic maintenance execution are prohibited for the initial design.
- **No substitute authorities:** application start, `Ready`, setup authority, startup authorization, possession of `OperationalProductionDatabase` or `FullIntegrityValidatedProductionDatabaseConnection`, backup existence or verified-backup proof, writable database ownership, migration eligibility, version compatibility, and concurrency or exclusivity do not create maintenance authorization.
- **Separate safety gates:** verified recoverable encrypted backup, full-integrity validation, valid source and target versions, writable maintenance ownership, concurrency and exclusivity, and installation, evidence, and freshness state remain distinct composed prerequisites. They cannot substitute for operator authorization.
- **Trust boundary:** React may eventually request or present confirmation but remains non-authoritative. Rust must ultimately construct and own future maintenance authority.
- **Specific initial identity:** production database migration is the sole initial maintenance authorization identity because it is the only concrete next maintenance operation. No generic `MaintenanceOperation` enum or broad generic `MaintenanceAuthorization` capability is approved at this stage.
- **Migration-specific capability:** the private Rust capability is `ProductionDatabaseMigrationAuthorization`; it is lifecycle-owned, process-local, non-serializable, and carries none of the separate migration prerequisites.
- **Versions and proofs stay separate:** source and target database schema versions are separate eligibility and migration-plan prerequisites, not part of the authorization identity. Verified recoverable backup proof is not embedded in authorization and does not create operator consent. Full-integrity proof, writable maintenance ownership, exclusivity, installation/evidence/freshness state, version eligibility, and backup proof remain separately composed prerequisites. Migration authorization alone never means migration is eligible or executable.
- **Distinct future authorities:** restore, recovery, rekey or database-key replacement, anchor replacement, database replacement, and destructive cleanup remain separately authorized future domains and cannot be collapsed into migration authority. Recovery and restore remain distinct; rekey remains distinct from database replacement; and destructive cleanup remains distinct from reset, repair, recovery, replacement, and migration.
- **Non-operation capabilities:** setup and startup are not maintenance operations. Backup verification or acceptance and full-integrity validation are safety or proof capabilities, not operation identities. Explicit diagnostics, including full-integrity diagnostics, are not approved as operation variants. Backup creation is not approved as a distinct operator-authorized operation identity. “Installation/evidence replacement” is too broad and is not an approved catch-all identity.
- **Rust-created opportunity:** Rust must establish the exact pending production-database-migration confirmation opportunity before the frontend can confirm or cancel it. React cannot create `Pending`, supply its identity, or construct authority. A bare confirmation call without Rust-owned pending context must never authorize.
- **Owner and state placement:** the approved owner direction is `ApplicationLifecycle` or a migration-specific substate beneath it. Do not overload `StartupStatus` and do not introduce a generic maintenance state machine.
- **One-shot progression:** the conceptual transitions are `NotOffered -> Pending -> Authorized -> Consumed` and `Pending -> Revoked`. Confirmation atomically consumes `Pending`; cancellation atomically revokes it and is non-authorizing.
- **Same-process terminal no-renewal:** `Authorized`, `Consumed`, and `Revoked` are terminal for offering confirmation in the current process. After confirmation, cancellation, revocation, invalidation, or consumption, Rust cannot create a second `Pending`. A fresh process is required before another opportunity may exist. Restart destroys pending state and unconsumed authorization.
- **Revocation:** shutdown revokes `Pending` before or atomically with stopping. Future Rust-detected migration-context or eligibility invalidation also revokes it. Migration eligibility remains separate and must be independently revalidated when execution is later composed.
- **Future confirmation command:** `confirm_production_database_migration` is the approved migration-specific, argument-free direction. Its only conceptual input is Tauri-managed Rust lifecycle state. It accepts no Boolean, operation name, version, path, ID, nonce, authorization object, proof, database owner, or arbitrary frontend value. Only the first valid atomic consumption can succeed; replayed, concurrent, stale, repeated, and non-pending calls create nothing.
- **Safe result and IPC:** a future coarse result may distinguish `accepted`, `notPending`, and `unavailable`. `accepted` means only that Rust retained the authorization internally. Authorization is never serialized or returned over IPC.
- **Future cancellation command:** `cancel_production_database_migration_confirmation` is the approved migration-specific, argument-free, non-authorizing direction. It terminally moves `Pending` to `Revoked`; eventual UI abandonment must cause that transition or equivalent Rust-owned revocation.
- **No intermediate intent:** `ConfirmedMigrationIntent` is not approved because it would duplicate the same trust fact. The private confirmation transition directly constructs and internally retains `ProductionDatabaseMigrationAuthorization`, which is non-`Copy`, non-`Clone`, non-serializable, non-persistent, redacted, and exactly-once consumable.
- **Renderer limitation:** a compromised renderer could invoke the future argument-free confirmation while legitimate Rust-owned `Pending` exists. The first-generation boundary records consent in that exact context, not authenticated identity, and does not protect against a compromised renderer. Stronger assurance needs separately approved authentication, trusted UI, or another mechanism; this risk does not permit frontend-created state or authority.
- **Production-path evolution:** the private state machine was implemented and tested before migration execution; later repository work adds the Rust-owned production opportunity and revalidation path. Authority stays internal, and no confirmation/cancellation Tauri command, frontend flow, or migration execution exists.
- **Still unimplemented at this boundary:** the later repository baseline includes a production migration opportunity/revalidation path, eligibility owner, exclusivity, full-integrity source, encrypted migration-backup stage, and recovery-envelope verification. There is still no confirmation Tauri command or frontend UI, migration execution, schema-version-2 implementation, migration SQL, recovery-key custody, operational portable recovery, durable publication, writable maintenance owner, authentication, or generic maintenance framework. UI wording and flow remain undecided.

### Implemented migration recovery-envelope decisions

- **Implemented proof boundary:** `VerifiedEncryptedProductionDatabaseMigrationBackupStage` proves the exact full-integrity source was copied to an encrypted SQLCipher stage under the same production database key, independently passed cipher integrity and full SQLite integrity, exactly matched source metadata/headers, established ciphertext at rest, retained the unchanged source, and retained migration authorization bound to it. The private consuming transition now produces `VerifiedRecoveryEnvelopedProductionDatabaseMigrationBackup`; it still does not prove custody, durable publication, or operational portable recovery.
- **Construction and version:** Migration Recovery Envelope Format Version 1 uses magic/domain `CHMRECV\0`, version `1`, algorithm ID `1`, exactly XChaCha20-Poly1305, an independently generated uniformly random 256-bit migration recovery key, a fresh uniformly random 192-bit XChaCha20 nonce per envelope, and the fixed untruncated 128-bit detached authentication tag. The envelope is exactly 182 bytes, with an exact 96-byte payload, exact 44-byte AAD, 24-byte nonce, and 16-byte tag. Version 1 accepts only XChaCha20-Poly1305, with no negotiation or fallback.
- **Approved implementation dependency:** exactly `chacha20poly1305 = { version = "=0.11.0", default-features = false, features = ["zeroize"] }`; it is implemented, not a deferred dependency choice.
- **Rejected substitutes:** plain ChaCha20, 96-bit-nonce ChaCha20-Poly1305, AES-CBC, AES-CTR, unauthenticated encryption, HMAC-only protection, DPAPI as the portable envelope, and SQLCipher as an arbitrary payload cipher are prohibited.
- **Recovery-key owner:** the key comes from repository-approved OS randomness and has a dedicated non-`Clone`, non-`Copy`, non-serializable, redacted, best-effort-zeroizing type. Plaintext exists only process-locally during envelope construction/verification. It grants no migration/startup authorization, database-write/restore authority, or evidence/freshness authority.
- **Identifiers:** generate independent nonzero 128-bit recovery-key-generation and opaque migration-backup-set identifiers. They are public, not key material. The backup-set identifier never reuses setup-publication, installation, database-key-generation, or recovery/replacement generation.
- **Nonce:** every envelope gets an independent OS-random 192-bit nonce. Reuse under one key is prohibited; failure fails closed. There is no counter, persistent allocator, or deterministic derivation from identifiers, digests, timestamps, paths, metadata, or key material. The nonce is stored and non-secret.
- **Exact payload:** encrypt only the 256-bit production database key, 128-bit database-key-generation identifier, 128-bit migration-backup-set identifier, and 256-bit SHA-256 digest of the exact closed verified encrypted-stage bytes. Exclude parish, installation and generations, setup-publication, recovery/replacement generation, database headers/format/schema identities, evidence/freshness keys, active DPAPI wrapper, paths, and timestamps absent a later proven need.
- **Digest and AAD:** hash only after successful writer close and verified-stage success. Authenticate the digest and immutable framing. A fixed explicit Church App migration-recovery-envelope domain, format version, algorithm identifier, and recovery-key-generation identifier are AAD; an externally encoded backup-set identifier is AAD too. Do not reuse evidence/freshness domains.
- **Authentication before release:** framing, exact version/algorithm, structural identifiers, ciphertext length, and AEAD authentication must pass before releasing any database-key plaintext. Wrong keys and authentication failures release no candidate. After opening, require exact backup-set equality, database-key-generation correspondence to recovered metadata, and a fresh stage digest match.
- **Independent verifier:** consume the verified encrypted stage; generate the key/identifiers/nonce; hash the exact retained stage; independently reload/recover/bind the active wrapped database key for construction; seal; and destroy that wrapper-derived `GenerationBoundDatabaseKey` before verifier work. Without reloading the wrapper, independently open the envelope, reconstruct a fresh generation-bound candidate using the existing trusted installation assessment, hash the same stage a second time, require exact authenticated digest equality, and open that stage only with the candidate. The open is `READ_ONLY`, `FULL_MUTEX`, `PRIVATE_CACHE`, `NOFOLLOW`, fixed-win32-VFS, no-`CREATE`, canonical-pre-key-hardened, keyed once, and `query_only` enabled/verified. It reruns only canonical cipher integrity and fixed metadata/header observation with exact source equality, explicitly closes, and finally verifies stage identity continuity.
- **Integrity repetition:** do not repeat full SQLite integrity when prior stage full integrity, authenticated byte-identical SHA-256, recovered-key cipher integrity, and exact metadata/header equality all hold. If digest equality cannot be established, fail instead of substituting weaker verification.
- **DPAPI and generation:** the portable envelope is not DPAPI-encrypted. A future CurrentUser-DPAPI local convenience key copy is not sufficient portable custody. Envelope creation preserves recovery/replacement generation.
- **Stage continuity:** the retained verified stage carries parent handle/identity, a read-only leaf handle, and full leaf `FILE_ID_INFO`; the leaf excludes incompatible ordinary write/delete access.
- **Honest authority and naming:** the envelope is external and never mutates the stage. It grants no mutation authority. Implemented success retains exact migration authorization, full-integrity source, encrypted stage, generated recovery-key material for later custody, and independently verified envelope proof. The exact name is `VerifiedRecoveryEnvelopedProductionDatabaseMigrationBackup`, not `VerifiedRecoverableProductionDatabaseBackup`.
- **Failure ownership:** authorization is destroyed before outward failure; primary failure explicitly closes the source and preserves canonical source-close retry ownership; verifier close has a close-only retry owner. Outward failures use coarse redacted `Debug`, retain stage/envelope artifacts, carry no recovery-key material, and perform no automatic destructive cleanup.
- **Exclusivity boundary:** this private transition does not own migration exclusivity. Cross-process migration exclusivity remains an unwired orchestration prerequisite for the later complete execution chain.
- **Separate layers:** A encrypted stage, B implemented cryptographic envelope and independent recovered-key verification, C key custody, D durable publication/set verification, E restore, and F retention/deletion remain distinct.
- **Deferred:** key custody; applicability and format of ten recovery codes; meaning of two verified copies; final root and filenames; publication atomicity/durability; rotation; supersession; post-migration retention; destructive deletion authority; and restore implementation.

### Implemented and accepted application-lifecycle decisions

- **Immediate non-ready window:** the window appears immediately while React begins in `Starting`; application readiness is not a prerequisite for showing the loading boundary.
- **Rust ownership and worker model:** Rust owns startup orchestration. The entire synchronous production trust chain runs in one blocking Rust worker, not on the UI/event loop. React supplies no path, evidence, database authority, or shutdown authority.
- **Coarse frontend contract:** frontend-visible states are `Starting`, `Ready`, `Unavailable`, `SetupInProgress`, `SetupRestartRequired`, `Stopping`, and `ShutdownIncomplete`. The narrow argument-free `request_first_time_setup` command sits beside read-only `startup_status` and the existing health command. Internal ownership-bearing failures are not frontend API.
- **Ready rule:** `Ready` is reached only after the authorized owner is consumed through operational activation and a real `OperationalProductionDatabase` is installed.
- **Independent installation observations:** startup observes canonical installation evidence early and again immediately before authorization. It passes the actual second observation into authorization and never manufactures `Initialized(Present)`.
- **Fail-closed path:** ordinary startup has no setup, migration, repair, recovery, reset, or retry fallthrough. Terminal startup failure is exposed only as `Unavailable`; explicit setup is separately requested and authorized.
- **Responsive shutdown:** shutdown during startup records stopping intent, requests worker drain, and prevents late `Ready` installation. A late owner is closed on a blocking worker.
- **Retained close failure:** close failure remains owned internally as `CloseRetryRequired` and is exposed only as `ShutdownIncomplete`. This implementation intentionally has no close-retry UI, IPC command, or user action.
- **Coarse logging:** lifecycle logs are fixed and local and must not reveal paths, keys, identifiers, metadata, native errors, or raw backend chains.
- **Manual-only support:** startup root/pause selection exists only on Windows debug-assertion builds and grants no frontend or IPC authority. Fixture export is Windows test-only and ignored. Neither is production product behavior.

- **Loader contract:** the anchor loader proves bounded, stable selection and validation of the current active wrapper pair, not absolute historical continuity. Disappearance without replacement is rejected; after recreation the loader may reject or may succeed only with the fully validated current recreated pair. Stale or mixed-pair success is forbidden. The accepted test correction changed no production loader bytes and is not a production-security weakening.

## First business-schema product and physical-design contract (not implemented)

This is the canonical record for the final approved product behavior and physical design. The headings below deliberately distinguish PRODUCT-LOCKED behavior, APPROVED PHYSICAL DESIGN, FUTURE RUST TRANSACTION RESPONSIBILITY, and STILL UNIMPLEMENTED work. The business scope is exactly `ServiceRequest`, `RequestScheduleOccurrence`, and `RequestCancellationReview`. No fourth business entity, lookup table, generic workflow engine, generic audit/history/event-sourcing or soft-delete system, person/parishioner table, or permanent/sacramental-record entity is approved.

### PRODUCT-LOCKED behavior

- Fixed service categories are Baptism, Confirmation, Wedding/Marriage, Burial/Funeral, and First Communion. Wedding/Marriage and Burial/Funeral are each one category. There is no configurable service catalog.
- Primary request states are `Pending`, `Scheduled`, `Completed`, and `Cancelled`. Every request is created as `Pending`. The only transitions are `Pending -> Scheduled`, `Pending -> Cancelled`, `Scheduled -> Completed`, and `Scheduled -> Cancelled`; there is no reopening, reverse transition, or reactivation.
- Cancellation review is separate from primary status. An unresolved review leaves the primary status unchanged and has no independent schedule effect. Approval changes a `Pending` or `Scheduled` request to `Cancelled`; rejection leaves status unchanged. Resolved reviews remain stored, and rejection permits a later review request.
- Only occurrences whose parent request is `Scheduled` occupy schedule slots. `Pending`, `Completed`, and `Cancelled` requests do not occupy slots. Pending occurrences are allowed as drafts. Completion or cancellation releases occupancy without deleting occurrences; occurrences remain historical facts.
- Rescheduling a `Scheduled` request updates its existing occurrence row in place while the request remains `Scheduled`. Old-slot release, new-slot conflict detection, and the update are one atomic operation. There is no schedule-history entity.
- Conflict is exact equality of local date and local time in one shared parish schedule. Location does not affect conflicts, and pending cancellation does not release a slot. Duration, end time, interval overlap, clergy/resource/capacity, and resource-scoped conflicts remain unapproved.
- Baptism, Confirmation, Wedding/Marriage, and First Communion each require exactly one primary occurrence before scheduling. Burial/Funeral requires at least one of Funeral or Burial; Funeral alone, Burial alone, or both are valid, while neither is invalid. At most one occurrence of each kind may exist per request.
- Location may be absent while a request is pending. Wedding/Marriage and First Communion require a non-empty final occurrence location before scheduling and throughout `Scheduled`. Location is optional for Baptism, Confirmation, Funeral, and Burial; Funeral and Burial may use different locations. There is no location catalog.
- The schedule timezone is `Asia/Manila`. Persisted schedule facts use minute precision. `MM/DD/YYYY` and 12-hour AM/PM are presentation only.
- The requester is a request-local snapshot of full name, mobile/phone, and optional email, not a durable person identity. Normalized people, family/household and role relationships, permanent or sacramental records, and completion-driven record creation remain deferred.

### APPROVED PHYSICAL DESIGN

- The exact tables are `service_requests`, `request_schedule_occurrences`, and `request_cancellation_reviews`. All three are `STRICT`, use `id INTEGER PRIMARY KEY`, and do not use `WITHOUT ROWID`. The ID is internal database identity only; raw database IDs are not approved as frontend or IPC authority, whose future design remains separate.
- `service_requests` contains `id`, `service_category`, `status`, `requester_full_name`, `requester_phone`, nullable `requester_email`, and `created_at`.
- `request_schedule_occurrences` contains `id`, `service_request_id`, `occurrence_kind`, `scheduled_local_date`, `scheduled_local_time`, and nullable `location`. It does not duplicate service category.
- `request_cancellation_reviews` contains `id`, `service_request_id`, `disposition`, `requested_at`, and nullable `resolved_at`.
- Persisted service codes are exactly lowercase ASCII `baptism`, `confirmation`, `wedding_marriage`, `burial_funeral`, and `first_communion`. Persisted request-status codes are `pending`, `scheduled`, `completed`, and `cancelled`. Persisted occurrence-kind codes are `primary`, `funeral`, and `burial`. Persisted cancellation dispositions are `pending`, `approved`, and `rejected`. There are no lookup tables.
- `status` and `disposition` are required and have no SQLite default. Rust must bind `pending` explicitly when creating a request or cancellation review.
- `scheduled_local_date` is canonical `YYYY-MM-DD` `TEXT`; the database checks shape only and Rust checks actual Gregorian validity. `scheduled_local_time` is canonical 24-hour `HH:MM` `TEXT`; the database checks shape plus hour/minute ranges. No seconds or per-row timezone column exists.
- `created_at`, `requested_at`, and `resolved_at` are non-negative UTC Unix milliseconds stored as SQLite `INTEGER`. `resolved_at` is `NULL` exactly while disposition is `pending` and non-`NULL` for `approved` or `rejected`. It need not be greater than or equal to `requested_at`, because wall-clock correction may occur. Deterministic chronology may use `(timestamp, id)`.
- `requester_full_name` has a 200-Unicode-character maximum and `requester_phone` a 32-character maximum; both are required. Optional `requester_email` has a 254-character maximum. Optional `location` has a 256-character maximum. Optional values use `NULL` when absent; an empty string is invalid. Database checks enforce length 1 through the maximum when present and no leading or trailing ASCII spaces. They do not replace Rust validation.
- Both `service_request_id` columns reference `service_requests(id)` with `ON UPDATE RESTRICT ON DELETE RESTRICT`. No deletion workflow is approved. These declarations do not provide enforcement until a separately designed future writable connection enables and verifies foreign-key enforcement; current startup connection policy remains unchanged.
- `UNIQUE(service_request_id, occurrence_kind)` enforces at most one occurrence of each kind per request. A partial unique index on cancellation reviews enforces unique `service_request_id` where `disposition = 'pending'`. Resolved approved/rejected reviews remain retained.
- The only approved additional indexes are `service_requests(status, created_at, id)`, `request_schedule_occurrences(scheduled_local_date, scheduled_local_time, id)`, `request_cancellation_reviews(service_request_id, requested_at, id)`, and pending-only `request_cancellation_reviews(requested_at, id)`. There is no unique schedule-slot constraint and there are no speculative indexes.
- No triggers are approved. Row-level checks constrain the persisted code sets, text bounds/ASCII-space rules, non-negative timestamps, resolved-time nullability, date shape, and time shape/ranges.
- No generic `updated_at`, `deleted_at`, status-change, audit, or other timestamps are added. No schedule-history, status-history, audit, event, person, service-catalog, location-catalog, or sacramental-record table is added.

### FUTURE RUST TRANSACTION RESPONSIBILITY

- Rust trims required text and rejects an empty result. It represents absent optional email/location as `NULL` and rejects empty present values, NUL, and disallowed control characters without aggressive case-folding or destructive normalization.
- Sealed Rust mutation APIs enforce lifecycle transitions in explicit write transactions. The recommended compare-and-set update supplies request ID, expected source, and target and succeeds only when exactly one row changes. Triggers are not used.
- Before `Pending -> Scheduled`, Rust enforces service/occurrence compatibility and cardinality: `primary` only for Baptism, Confirmation, Wedding/Marriage, and First Communion; `funeral` and/or `burial` only for Burial/Funeral. A Burial/Funeral `primary`, or `funeral`/`burial` for another service, is invalid.
- Rust enforces category-dependent location requirements, actual Gregorian date validity, and conflict checks against another occurrence at the exact date/time whose parent request is currently `scheduled`.
- Scheduling and rescheduling are atomic Rust write transactions. Rescheduling releases the old slot, checks the new slot, and updates the existing occurrence together.
- Cancellation approval is one sealed Rust-owned write transaction: require the review still `pending`; require parent status `pending` or `scheduled`; update the parent to `cancelled`; update the review to `approved`; set `resolved_at`; require exactly one affected row for each mutation; otherwise roll back.
- Cancellation rejection is one Rust-owned write transaction: require and change exactly one `pending` review to `rejected`, set `resolved_at`, do not change parent status, and commit only after that exact mutation succeeds.
- Cross-table cardinality, service/kind compatibility, status transition, occupancy, conflict, and category-dependent location rules remain Rust-side. No trigger, stored procedure, or generic maintenance abstraction substitutes for them.

### STILL UNIMPLEMENTED

This approval creates no business table, executable SQL file, migration, migration SQL, writable connection policy, foreign-key PRAGMA, transaction API, Rust business type, IPC, form, or workflow. The existing production schema version 1 bootstrap remains unchanged and need not become `STRICT`. This exact physical contract is assigned `database_schema_version = 2`, with `metadata_contract_version = 1`, SQLite `user_version = 2`, unchanged `application_id = 0x43484150`, and unchanged `ApplicationDatabaseFormatIdentity`. The only compatibility direction is 1 -> 2; versions newer than 2 are unsupported and downgrade-refused, and there is no generic unsupported-to-2 rule. Version assignment alone does not create migration eligibility or authorization, does not change migration-confirmation code, and does not implement version 2.

## Other locked future direction (not implemented)

- Desktop-first and offline-capable; Windows 10 and Windows 11 are intended targets.
- Production normal operation uses one dedicated standard Windows account for Church App, and the process should run in a non-elevated session. Historical current-account test evidence does not establish dedicated-account execution or administrator-group membership. Elevation refusal is not yet implemented; this direction does not approve privileged or elevated ordinary operation.
- Production data will operate only from the application-owned per-user local application-data directory.
- Production database creation occurs only through the implemented explicit first-time setup flow; ordinary startup does not authorize setup and never silently creates a missing database.
- Each future parish database will contain an immutable randomly generated opaque parish identifier. Representation and validation boundaries exist, but generation and persistence do not.
- The accepted installation-evidence architecture is external protected evidence plus matching identity inside the encrypted database. Evidence authentication, trusted loading, and database/evidence correspondence are implemented. Neither evidence nor the database alone is sole authority; freshness, startup authorization, and operational activation remain distinct later boundaries. This chain does not imply setup, writable operations, schema completeness, recovery, backup/restore, authentication, or parish workflows.
- The implemented authentication tag sits outside the logical evidence record. The in-memory DPAPI layer wraps the authenticated envelope without redefining the logical encoding. Migration between encoding versions will be explicit.
- SQLCipher Community Edition is the selected production engine under the bounded approval above. Its Windows dependency, private raw-key primitive, metadata-only inspection, guarded keyed connection, validation chain, startup authorization, operational activation, application lifecycle, explicit first-time setup, and minimal V1 metadata/bootstrap schema are implemented; the earlier feasibility proof remains historical test-only evidence. Broader database operations remain absent.
- Local parish data will be authoritative; future central Supabase services will be non-authoritative.
- The public Next.js application and canonical contracts belong in separate repositories.
- The first release direction is English only.
- Schedule timezone, UI formatting, service categories, request statuses, cancellation review, scheduling, location, requester snapshot, timestamps, and explicit deferrals are governed by the approved first business-schema product direction above.

The implemented initialization-state model remains separate decision logic. Parsing establishes no authentication, protection, persistence, provenance, database match, startup authority, or operational trust. Each live trust-chain owner establishes only its named transition. Operational activation occurs only after the independently supplied final installation observation authorizes startup, and it does not imply setup, database creation, writable business operations, schema completeness, migration, authentication, recovery, backup, or parish workflows.

# Security and data

- Never place secrets, tokens, credentials, or production environment configuration in the frontend or repository.
- Never place sensitive parish or personal data in logs, tests, examples, or screenshots. Tests use synthetic bootstrap strings only.
- Privileged operations belong in Rust. React must not directly access files, a future database, encryption material, or privileged services.
- Production database paths cannot be supplied by frontend values, command arguments, environment variables, command-line values, or user-selected strings. No Tauri command returns the production directory, database filename, database path, Windows username, or filesystem metadata.
- Production setup authorization is an in-memory Rust type with a private field, created only by a dedicated transition from never-initialized evidence. Raw booleans, React or IPC values, CLI arguments, environment variables, paths, and arbitrary strings cannot construct it. No setup Tauri command exists.
- Ordinary startup has no setup authorization. Initialized-but-missing storage, inconsistent installation evidence, and unavailable evidence fail closed and cannot fall back to first-time setup or authorize creation.
- A structurally valid logical installation-evidence contract is still untrusted. It must not be treated as authenticated, authoritative, safe for startup, or matched to a database until separately approved authenticated-integrity and database cross-check boundaries exist.
- Its 164-byte canonical plaintext encoding is likewise untrusted, unauthenticated, and unprotected. The encoder contains no secrets beyond the modeled opaque identifiers, and neither complete encoded bytes nor opaque identifier values may be logged or exposed through debug output.
- The 226-byte authenticated-envelope parser validates framing only. Its terminal 32-byte tag field remains untrusted until HMAC verification succeeds; correctly framed bytes alone confer no integrity, provenance, database correspondence, startup permission, or operational authority.
- HMAC-SHA-256 verification uses the HMAC crate's `verify_slice` method over exact envelope bytes `0..194`. No ordinary tag equality or custom constant-time comparison is used. Authentication failures are coarse and redacted.
- The per-parish evidence-authentication key is represented only by a Rust-owned 32-byte private buffer independent from the database key. Production generation requests its bytes from the operating-system randomness provider and independently requests a nonzero 16-byte key-generation identifier. Key and generated identifier bytes do not enter React, IPC, serialization, logs, or ordinary `Debug`; no universal or deterministic key exists.
- Authentication-material generation fails closed on any randomness error and after three all-zero identifier fill attempts. It has no deterministic fallback and derives no entropy from application identifiers, parish or installation identifiers, HMAC output, environment, clock, process, thread, machine, user, path, filesystem, registry, or database values. Provider details, operating-system errors, generated bytes, hashes, fingerprints, and retry values are not exposed.
- Drop applies best-effort zeroization to the type's owned buffer, including partial failure after key ownership has been established. This does not erase compiler temporaries or pre-move stack remnants, prior caller copies, register or stack spills, future cryptographic-state copies, swap, hibernation, crash dumps, debugger snapshots, or microarchitectural leakage.
- Complete envelope bytes, inner plaintext bytes, authentication-key generation identifiers, and terminal tag bytes remain redacted from debug and error output. Malformed or unsupported total length, magic, version, algorithm identifier, key-generation identifier, or declared plaintext length fails closed without scanning, fallback, prefix acceptance, or trailing-byte acceptance.
- Authenticated results retain neither the authentication key nor its bytes. No key bytes, plaintext bytes, envelope bytes, tags, or generation identifiers enter logs, Tauri IPC, or frontend state.
- HMAC authentication does not provide freshness or prevent replay or rollback. Current key-generation policy, persistence, database correspondence, and operational authority remain separate future boundaries.
- Windows protection uses classic DPAPI current-user scope only. Local-machine scope cannot be selected, UI is forbidden, and callers cannot supply optional entropy, descriptions, prompts, reserved data, or generic flags. The opaque DPAPI result is never parsed or normalized.
- Native DPAPI failures collapse to coarse redacted operation errors; Windows codes and native text are not retained in ordinary errors. Successful unprotected native allocations are copied into short-lived Rust-owned containers, best-effort cleared with `zeroize`, and then released once with `LocalFree`.
- Clearing reduces residual plaintext but cannot guarantee erasure of compiler temporaries, register or stack spills, swap, hibernation, crash dumps, debugger snapshots, prior copies, or microarchitectural state. Current-user DPAPI is not a strong boundary against malware already executing as the same user or against administrators.
- DPAPI unprotection establishes only candidate bytes. It does not replace HMAC authentication, generation matching, structural validation, freshness, rollback protection, database correspondence, setup or startup permission, or operational authority. No protected object is persisted in this stage.
- Deterministic malformed-wrapper, protected-key payload, fake-protector, and corrupted real DPAPI inputs fail closed through either native rejection or a later strict application-validation boundary. Candidate key-payload parsing alone grants no HMAC, generation, plaintext, startup, or operational authority.
- This hardening reduces confidence risk around malformed and corrupted protected inputs but does not prove DPAPI or cryptographic security. Replay, rollback, same-user malware, administrator access, persistence, and database correspondence remain unresolved.
- Deterministic authenticated-envelope mutation tests show that the tested arbitrary byte mutations cannot create an authenticated result without a valid recomputed tag. A valid tag still establishes neither logical validity, freshness, current key-generation policy, database correspondence, startup permission, nor operational authority.
- Wrong-key, malformed-length, representative-pattern, full-position, boundary, and retagged-malformed-plaintext corpora retain coarse redacted framing and authentication errors. These tests reduce confidence risk around the implemented boundaries but do not prove cryptographic security.
- Strict parsing fails closed on malformed length, framing, encoding version, declared payload length, application-identifier length, or application-identifier UTF-8. There is no fallback parser, format guessing, trailing-data acceptance, or automatic normalization.
- Successfully parsed values remain untrusted until a separate structural-validation call succeeds, and structural success still does not establish authentication, persistence, provenance, database correspondence, startup permission, or operational authority. Parser errors and parsed-value debug output expose neither encoded content nor complete opaque identifiers.
- Deterministic parser mutation tests reduce confidence risk around malformed inputs, field overlap, canonicality, and panics, but do not prove security. No arbitrary input can create operational authority, and observed parse errors and parsed-value debug output remain redacted.
- The logical contract contains no secrets, paths, personal or sacramental data, passwords, recovery codes, key material, machine identity, Windows user identity, or protection metadata. Full parish, installation, database-key generation, and setup publication identifiers must not appear in logs or debug output; the contract's debug representation redacts them.
- The fixed production path is modeled beneath the application-owned per-user local application-data directory for `io.github.cltubigon.churchapp`. The repository contains a private guarded read-only connection-handoff primitive and consuming readability/integrity, live metadata/header, correspondence, freshness, and internal startup-authorization transitions, but none has an operational startup or setup caller; accepted database testing used synthetic temporary roots rather than the production application-data path. No operational Tauri/application-startup integration, database creation, writable opening, migration, or operational production use exists.
- Pure persistence paths are joined only beneath caller-supplied synthetic roots. New path wrappers and their aggregate have redacted `Debug` output; the presence facts contain no paths, timestamps, contents, or native errors.
- The protected-wrapper reader rejects empty, below-15-byte, and above-65,550-byte reported lengths before exact-size allocation. It retries interrupted reads, collapses premature EOF and other I/O failures to coarse categories, and rejects any byte observed by its single-byte trailing probe. It retains no native error and exposes no path, contents, or observed length through errors or debug output.
- Unavailable inspection has highest classifier precedence, unexpected entry types fail closed as inconsistent, and any observed stage file is non-authoritative unexpected staging. Conclusive clean absence is only `CleanAbsenceCandidate`; it grants no setup, creation, startup, database, or operational authority.
- The pure publication model describes required evidence-last ordering only. It performs no filesystem inspection or mutation and therefore makes no claim about ACLs, reparse points, volumes, atomic replacement, durability, races, flushing, or any other real filesystem security property.
- The Windows filesystem proof uses deterministic synthetic wrapper bytes only and runs only beneath unique test-owned temporary roots. Stage creation is `CREATE_NEW` with zero sharing; reloads use `GENERIC_READ` with `FILE_SHARE_READ`; initial publication uses only `MOVEFILE_WRITE_THROUGH`. Coarse errors and their `Debug` forms retain no path, native code or text, wrapper bytes, user/SID, volume, or identifier data.
- Existing-file replacement is also executable only in Windows tests. Its native call always supplies a null backup path, flags exactly zero, and null reserved pointers. A failed call captures only a private coarse failure family; raw native codes and text are neither retained nor exposed. No retry, rollback, restoration, or per-file cleanup follows the call; only exact test-root teardown is allowed after assertions.
- Fresh exact-name inspection, rather than the native return or failure family, is authoritative for the observed post-call state. Byte equality and canonical-wrapper validity do not prove ACL, security-descriptor, named-stream, timestamp, or other metadata equality. The success proof additionally requires stage-to-result file-ID continuity and old-active/result identity distinction.
- Stage and active handles are independently reopened and checked as disk files that are neither directories nor reparse points. Handle-reported sizes must remain in 15–65,550 bytes and unchanged across the existing bounded read. Normalized GUID final paths must match the validated test evidence directory and exact fixed filename, and UNC forms are refused. These checks reduce risk but do not prove complete reparse, junction, or TOCTOU resistance.
- The normal-tree hardening tests reject every reparse attribute and every nonzero known or unknown reparse tag, including inconsistent attribute/tag combinations. They use both full `FILE_ID_INFO` object identity and normalized namespace identity, retain directory handles across leaf validation, and require parent/leaf volume-serial and volume-GUID agreement.
- Exactly one link is required only for protected active and staged installation-evidence wrapper leaves. Zero or multiple links fail closed; directories and the future database receive no hard-link policy in this stage.
- A real Windows test fixture now confirms that both authentication-key wrapper names move from one link to two after `std::fs::hard_link`, that the alias shares the full `FILE_ID_INFO` identity and canonical bytes but not the normalized final path or approved exact name, and that hardening returns `HardLinkRejected` before reading or injected mutation. Alias-only removal restores the original identity, bytes, and one-link state. This grants no production filesystem or publication authority.
- A separate Windows-test-only fixture confirms that an exact normalized `installation-evidence` path and a byte-identical canonical wrapper do not establish directory continuity. A retained read-access target handle without delete sharing blocks the ordinary rename attempt; after target and descendant handles close, the original and prebuilt candidate can exchange namespace occupancy through two controlled `std::fs::rename` calls, and full `FILE_ID_INFO` discontinuity rejects the substitute before any continuation.
- That controlled substitution test stores no production identity and adds no production enforcement, repair, rollback, or winner selection. It does not mix reparse or hard-link objects into the case and makes no production safety claim; intermediate/root substitution, hostile concurrency, production local-volume enforcement and authority, broader host provenance, and removable/network policy remain deferred. The private Windows-test-only non-authoritative local-volume candidate classifier and controlled observations are already implemented.
- A separate Windows-test-only compatibility case retains the exact validated evidence-directory handle with read access and read/write sharing but no delete sharing while the accepted child replacement runs. The same handle's full identity, normalized path, disk-directory/reparse facts, and saved-parent volume relationship remain stable before the call, immediately after it, and after fresh child inspection. Retaining the parent reduces ordinary same-operation directory-substitution risk, but replacement remains path-based and not handle-relative; child-entry and cross-process races remain. No production identity persistence, enforcement, retry, repair, or rollback is added.
- The private Windows-test-only local-volume classifier trusts neither path spelling nor a drive letter as acceptance evidence. It starts from a retained validated handle, accepts only a strict normalized volume-GUID path, derives only that exact GUID root, and maps one `GetDriveTypeW` result into coarse facts. Actual paths, drive letters, GUIDs, device names, user information, and native errors are not exposed.
- The nested Windows-test-only device-property proof starts only after `LocalFixedCandidate`, retains the test-root handle, opens only its exact derived volume device with access zero and read/write sharing, and makes exactly two bounded `IOCTL_STORAGE_QUERY_PROPERTY` calls. It parses raw fixed fields, caps allocation at 65,536 bytes, never follows descriptor offsets, and exposes no path, GUID, device name, bus number, vendor, product, revision, serial, raw structure, native code, or native text.
- `DevicePropertyCandidate` means only that the approved volume open and bounded query succeeded, the exact installed layout version and stable size passed strict parsing, removable media was not reported, and the bus belonged to the approved candidate family. These driver-reported facts remain insufficient for device non-removability, hot-plug or surprise-removal assurance, internal placement, physical locality, USB-enclosure exclusion, VHD/VHDX or profile-container exclusion, remote-backing exclusion, durability, or production suitability. No hot-plug, physical-drive, disk-extent, device-interface, production-path, setup, startup, persistence, publication, replacement, database, DPAPI, IPC, or frontend authority is added.
- Wrapper identity, size, attributes, reparse tag, link count, and final path are re-queried on the same handle after the bounded canonical read, and retained directory identities are re-queried before and after it. Hardening errors and debug output contain no path or UTF-16 content, file ID, volume GUID or serial, native code or text, or wrapper bytes.
- This remains synthetic-tree test evidence only. The hard-link and single controlled exact-target substitution cases do not establish production path safety, general race resistance, hostile reparse or broader substitution handling, enterprise-profile support, local fixed-volume policy, removable-device or hot-plug safety, or production publication authority.
- Reload verification proves bytes visible through a new handle after `FlushFileBuffers`; it does not prove survival of sudden power loss. Test teardown removes only the unique test root and is not automatic production cleanup. No DPAPI, database, production resolver, setup/startup authority, IPC, or frontend capability participates.
- Ordinary Church App use may run under the current Windows account, and the process should run in a non-elevated session. Administrator-group membership need not be established for the accepted current-account evidence. A dedicated standard Windows account is optional and recommended for a parish-owned shared workstation; it is not an ordinary-use prerequisite. Elevation refusal is not yet implemented, and this direction does not approve privileged or elevated ordinary operation. Future local parish storage must be encrypted and authoritative. Carlo has selected SQLCipher Community Edition, and the exact Windows production dependency, private raw-key primitive, metadata-only inspection, guarded keyed-but-unvalidated connection handoff, readability-and-integrity validation transition, live metadata and SQLite-header validation transition, identity-only database/evidence correspondence transition, preloaded normalized freshness transition, and internal startup-authorization boundary are implemented. Operational Tauri/application-startup integration and operational database opening remain deferred.
- Future central privileged credentials must remain server-side. Supabase is not configured here.
- Current logs are local development terminal output, limited to event name, outcome, and safe code. They contain no payload, raw error, environment value, machine identifier, path, user information, or parish data, and nothing is uploaded.

Automated checks do not prove security, accessibility, desktop runtime behavior, production storage safety, power-loss durability, or complete user flows. The Windows current-user in-memory DPAPI foundation, strict wrappers and recovery orchestration, identity-only correspondence adapter, preloaded normalized freshness adapter, and internal startup-authorization boundary are implemented. Production persistence/loading of DPAPI-protected objects, active-generation policy, operational Tauri/application-startup integration, setup integration, recovery, and operational policy remain deferred. The private Windows-test-only local-volume and device-property candidate evidence remains non-authoritative. Production replacement, rollback protection, cleanup, ACL and replacement-metadata enforcement, named streams, backup, cross-volume behavior, mutex coordination, complete reparse/junction defenses, controlled virtual/profile/mounted-folder hosts, runtime replacement detection, and database creation also remain deferred.

## Approved production database security policy, guarded handoff, and validation

Carlo explicitly approved this bounded security architecture. The Windows production SQLCipher dependency, private raw-key primitive, metadata-only inspection, guarded read-only handoff, and consuming readability/integrity, live metadata/header, correspondence, freshness, and internal startup-authorization transitions are current implemented capability. The internal chain now ends at opaque `StartupAuthorizedProductionDatabaseConnection`. There is still no operational Tauri/application-startup integration, operational database-opening flow, first-time creation, schema, migration, backup/restore, recovery, replacement, or frontend/Tauri database operation.

- SQLCipher Community Edition is mandatory for production parish databases, production backups, and production recovery databases. Plain SQLite and encrypted/unencrypted fallback are prohibited. Windows production now uses exactly pinned `rusqlite` 0.39.0 with default features disabled and only `bundled-sqlcipher-vendored-openssl`, under the Windows production dependency table. There is no Windows `rusqlite` development dependency and no direct `libsqlite3-sys` dependency. The former test-only dependency state and feasibility proof are historical evidence. Pinned SQLCipher/OpenSSL versions and the lockfile remain release evidence.
- The private Windows-only key-application boundary accepts only `&GenerationBoundDatabaseKey` and a caller-supplied already-open handle. It creates one fixed 67-byte owner containing exact lowercase `x'<64 hex characters>'`, invokes `sqlite3_key` exactly once, treats only `SQLITE_OK` as success, and best-effort zeroizes the owned encoded buffer. The key remains Rust/backend-only: it is not logged, serialized, sent through IPC, exposed to React, or accepted from a Tauri command.
- The production inspector remains metadata-only: it retains parent and database-file proofs and exposes no database-content capability. A separate private `ConnectionLifetimeWriteGuard` deliberately opens the inspected file with native content-read capability using `FILE_READ_ATTRIBUTES | FILE_READ_DATA`, `FILE_SHARE_READ`, `OPEN_EXISTING`, and `FILE_FLAG_OPEN_REPARSE_POINT`, but it exposes no guard handle, file, path, identity, content-read API, raw handle, or database-content capability.
- The guard is identity-matched to the inspected proof before the one SQLite open. SQLite's borrowed main-database Windows handle is retrieved through `SQLITE_FCNTL_WIN32_GET_HANDLE` and identity-matched to the same proof. Raw SQLite and Windows handles never escape.
- The handoff performs exactly one application-level path-based `Connection::open_with_flags_and_vfs(..., "win32")` using `SQLITE_OPEN_READ_ONLY | SQLITE_OPEN_FULL_MUTEX | SQLITE_OPEN_PRIVATE_CACHE | SQLITE_OPEN_NOFOLLOW`. It excludes read-write, create, shared-cache, no-mutex, memory, and URI flags. No-create means no main-database creation, and one attempt means no application retry loop or writable fallback; SQLite internal retries remain engine behavior.
- Before key application the connection receives a five-second busy timeout, disabled extension loading, defensive mode, disabled trusted schema, no-checkpoint-on-close, disabled attach-create and attach-write, and confirmation that the main database is read-only. The existing generation-bound key is applied exactly once; query-only is enabled and verified only afterward.
- Native failures and all returned/logged states disclose no path, key, SQL, PRAGMA, identity, native code, raw SQLite or Windows handle, or raw error chain. Key-application success proves only acceptance of the one key-submission call. It does not prove key correctness, encryption validity, readability, integrity, metadata, correspondence, freshness, startup permission, or operational authority.
- The opaque keyed-but-unvalidated owner retains the `rusqlite::Connection`, guard, and inspection proof as one lifetime unit. Construction-time and normal close failures retain the full unit, and only consuming close or close-retry transitions are exposed. There is no operational runtime caller, frontend work, Tauri command, or IPC operation.
- The independent database key is 256 random bits from the Windows OS cryptographic RNG. It is never password-derived, value-derived, or shared with the evidence HMAC key. Its owner is non-`Copy`, has no unrestricted `Clone`, redacts formatting, and best-effort zeroizes owned buffers.
- A dedicated CurrentUser-DPAPI wrapper protects the live database key. Portable recovery uses a separate random recovery key and an authenticated recovery envelope. Recovery-key custody, duplication, rotation, loss, and destruction are explicit operator/security duties; live-key DPAPI protection does not replace portable recovery policy.
- Metadata fails closed on a missing, duplicate, partial, wrong-class, wrong-length, unsupported, or malformed singleton row. In particular, `permanent_application_identifier` is bounded canonical UTF-8 SQLite `TEXT`, while `database_format_identity` is the existing typed `ApplicationDatabaseFormatIdentity` encoded only as an exactly 16-byte SQLite `BLOB`, directly decoded to that type and compared by exact typed equality. It is never text or a textual, numeric, hashed, prefixed, or fingerprint representation.
- The implemented live adapter uses only `PRAGMA main.application_id`, `PRAGMA main.user_version`, and the fixed explicit 12-column `SELECT` from `main.church_app_database_metadata` with `LIMIT 2`. Both PRAGMAs require exactly one column, exactly one row, SQLite `INTEGER`, signed-32-bit representability, and normal terminal completion. The metadata statement requires 12 columns; it copies the first row into a private owned raw observation before checking for a second row. `LIMIT 2` bounds work while distinguishing zero, exactly one, and more than one row, and no `singleton_id` filter hides additional rows.
- Live storage-class matching is exact and conversion-free: `INTEGER`, raw `TEXT`, `BLOB`, and `NULL` adapt to the existing database-neutral raw values; `TEXT` must pass strict UTF-8; `REAL` fails as `MalformedMetadata`. No cast, `typeof()`, schema introspection, dynamic SQL, arbitrary content query, or duplicate field parser is approved. Temporary headers, raw observations, parsed metadata, and partial validation state are discarded on failure before explicit close.
- Raw SQLite or rusqlite error-message text must never be parsed to infer table absence or schema shape. An absent named relation, missing required column, malformed relation shape, or other failure to prepare/start the fixed query is coarsely `MetadataObservationUnavailable`; normal zero-row completion is `MetadataRowMissing`. This stage makes no physical DDL, constraint, index, trigger, object-kind, or wider-schema claim.
- The live stage exposes exactly `HeaderObservationUnavailable`, `WrongApplicationId`, `MetadataObservationUnavailable`, `MetadataObservationInterruptedOrIncomplete`, `MetadataRowMissing`, `DuplicateMetadataRows`, `MalformedMetadata`, `UnsupportedMetadataContractVersion`, `UnsupportedDatabaseSchemaVersion`, and `UserVersionMismatch` as primary categories. `CloseFailed` is ownership-bearing rather than primary, and processing stops at the first failed stage without collecting or exposing multiple defects.
- Exact typed correspondence covers the permanent application identifier, database-format identity, parish identifier, installation identifier, database-key generation identifier, and setup-publication identifier. Correspondence ignores both generation values; only the later local-anchor freshness boundary may compare them. Evidence-format fields stay evidence-only, and timestamps affect neither correspondence nor freshness authorization.
- The implemented freshness boundary consumes `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection` and exactly one preloaded `NormalizedFreshnessAnchorObservation`, invokes the existing pure classifier once, and advances only on `Fresh`. It performs no anchor loading or database read. The internal startup-authorization boundary after freshness is implemented; operational Tauri/application-startup integration, operational opening, and an operational caller remain absent and separately scoped.
- Correspondence establishes no rollback resistance. HMAC, DPAPI, timestamps, identity correspondence, and integrity checks do not establish freshness or startup authority.
- The approved production path is the exact application-owned directory and `parish-data.db` on local NTFS. Reparse points, symlinks, junctions, mount traversal, cloud placeholders, hard links, network or removable storage, unstable identity, unexpected sidecars, WAL, and SHM fail closed. Startup never deletes or repairs sidecars. A dedicated database adapter with database-aware race protection is required; evidence-wrapper loading is not a drop-in database hardening implementation.
- Read-only/no-create applies to the main database file. Later SQLite operations may still involve auxiliary-file behavior. `SQLITE_OPEN_NOFOLLOW` is defense in depth rather than the primary Windows reparse guarantee, and query-only is defense in depth rather than the primary read-only guarantee.
- While the verified guard remains open, ordinary Windows filesystem opens requesting incompatible write or delete access to the guarded file object are refused under normal Windows sharing semantics. This is not total external-write prevention: it does not protect against pre-guard mutation, kernel-mode activity, filesystem filters, raw-volume access, privileged bypass, storage or hardware faults, or operating-system/filesystem defects.
- The implemented and accepted readability-and-integrity boundary consumes `ProductionReadOnlyDatabaseConnection` and runs only `PRAGMA cipher_integrity_check` to normal zero-row completion, followed by `PRAGMA main.quick_check(1)` to exactly one complete SQLite `TEXT` value `ok` and normal end-of-stream. No separate readability query exists, and `sqlite_master`, metadata tables, `application_id`, `user_version`, product tables, arbitrary content, and unrestricted SQL remain outside this boundary.
- The exact primary validation categories are `EncryptedDatabaseAuthenticationOrCipherIntegrityFailed`, `SQLiteReadabilityOrIntegrityFailed`, `ValidationUnavailable`, and `ValidationInterruptedOrIncomplete`. They intentionally do not expose an unsupported distinction between wrong key and cipher-page/HMAC failure or between corrupt and unsupported/unreadable SQLite content. SQL or PRAGMA text, cipher-integrity result text, quick-check diagnostic text, paths, keys, identifiers or identities, SQLite/native codes, raw handles, and rusqlite errors or chains are neither retained nor exposed.
- Cipher success requires normal zero-row completion. The first returned cipher row establishes failure without decoding, copying, formatting, retaining, or logging its text; later rows need not be consumed, and the row stream must be dropped or reset before close. Stopping after the first failure row is not an incomplete success because failure is already established.
- Quick-check success requires exactly one text row equal to `ok` followed by normal completion. Zero rows, multiple rows, non-text output, non-`ok` output, stepping errors, interruption, malformed result shape, or premature termination fail closed. The diagnostic value is not exposed. This limited `quick_check` is not full `integrity_check` and does not prove every SQLite invariant; full integrity remains required at separately controlled mutation and recovery boundaries.
- No active cancellation API or rusqlite hooks feature is approved for this stage. No validation counts as success without its complete required terminal result. `SQLITE_INTERRUPT`, statement errors, malformed shape, and premature termination map to coarse fail-closed outcomes; active user cancellation remains separately scoped.
- Both checks run sequentially on the same privately owned connection while the existing guard and inspection proof remain retained and query-only, defensive mode, and read-only opening remain active. No explicit SQLite transaction is added and no stronger snapshot guarantee is claimed beyond the guarded same-connection lifetime.
- On validation failure, statement and row-stream state is released before explicit close. Close success returns the primary coarse category. Close failure preserves both that category and an opaque close-retry owner containing the complete connection, guard, and inspection-proof lifetime unit; raw close detail is discarded. There is no automatic repair, header/metadata repair, schema downgrade, journal-mode switch, VACUUM, or cleanup.
- The implemented live stage follows the same lifetime discipline. Every primary failure releases `Rows` and `Statement`, discards all temporary observation and metadata values, then explicitly closes. Close failure retains the same primary category and complete connection/guard/inspection lifetime unit across consuming retries. `LiveMetadataAndHeaderValidatedProductionDatabaseConnection` retains the unchanged lifetime unit plus one owned `DatabaseMetadataContractV1`, but no separate header values; its explicit close discards the contract first, and a failed close retains only the complete lifetime unit.
- Outward `Debug`, `Display`, logs, panic text, IPC, and frontend state for the live stage never expose SQL or PRAGMA text; table or column names; raw rusqlite errors or messages; SQLite codes; `application_id`; `user_version`; metadata versions; identifiers; generations; creation timestamp; canonical application identifier; database-format identity; row values; byte lengths; or failing field index. `DatabaseMetadataContractV1` exposes some non-secret values through its own `Debug`, so live-stage owners, outcomes, and close failures use manual redacted formatting and never delegate to that implementation.
- `cipher_integrity_check` may synchronously scan the whole database with work proportional to database pages. No production database-size ceiling, bounded-latency guarantee, or active-cancellation claim is introduced. Carlo accepts this only because the boundary has no operational caller; later operational integration must define size limits, execution-time expectations, responsiveness, and cancellation policy.
- Initial durability is rollback-journal `DELETE`, `synchronous=FULL`, explicit transactions, `secure_delete=ON`, and `auto_vacuum=NONE`; WAL/SHM and WAL checkpoints are outside the initial policy.
- Setup is the sole creation authority. Persisted presence, evidence validation, path validation, key recovery, opening, metadata decoding, integrity, correspondence, freshness, installation classification, startup authorization, and operational opening remain separate typed stages. Recovery, restore, migration, rekey, anchor replacement, database replacement, and destructive cleanup each require separate authorization. No generic repair authority exists.

The implemented handoff exposes only coarse failures and an opaque keyed-but-unvalidated owner. The succeeding implemented transitions return `ReadabilityAndIntegrityValidatedProductionDatabaseConnection`, `LiveMetadataAndHeaderValidatedProductionDatabaseConnection`, `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection`, `DatabaseFreshnessValidatedProductionDatabaseConnection`, and then the current internal endpoint `StartupAuthorizedProductionDatabaseConnection`, while retaining the same connection, guard, and inspection proof. The live owner additionally retains one owned validated metadata contract; the correspondence, freshness, and startup-authorized owners retain that contract plus the full trusted assessment, while the freshness owner retains no normalized observation or anchor. Validation-failure close paths preserve the primary category and complete lifetime unit, owner close failures preserve that unit, and retries are consuming. Errors and logs expose no path, SQL or PRAGMA text, validation diagnostic, key, identifier, identity, generation, timestamp, header value, metadata value, DPAPI data, native status code, raw handle, raw error chain, username, or profile directory. Key success and query-only establish neither database validity nor authorization. Live metadata/header success does not establish database/evidence correspondence, correspondence success does not establish freshness, freshness success does not establish startup authority, and startup authorization does not establish operational opening. Freshness success also does not establish absolute newest state or coordinated-rollback detection. Startup authorization grants no setup authority, operational opening, setup completion, physical DDL, relation object kind, constraints, indexes, triggers, wider product-schema correctness, migration status, recovery fitness, backup/restore suitability, replacement authority, or business-data correctness. Correspondence, freshness, sidecar, startup, and operational categories remain separate, and no operational caller exists.

### Implemented identity-only correspondence security boundary

The implemented adapter consumes the live metadata/header owner and exactly one preloaded `TrustedCurrentInstallationEvidenceAssessment`. It does not load evidence, resolve a path, access the filesystem, invoke DPAPI or HMAC, parse an envelope or plaintext, execute SQL or a PRAGMA, perform a database read, mutate a schema, or expose a connection. The trusted assessment is mandatory because it preserves the completed trusted-loading and authentication provenance; a bare structurally validated evidence value is not an accepted live input.

The adapter invokes the existing pure six-field correspondence classifier exactly once. It does not reproduce individual comparisons or consult installation generation, recovery/replacement generation, timestamps, or evidence-format fields. The two generation values remain retained unchanged only for the later anchor-based freshness boundary. Identity correspondence grants no lineage or freshness classification.

The opaque success owner retains exactly the unchanged lifetime owner, validated database metadata, and full trusted assessment. This retains validated evidence and `TrustedCurrentInstallationIdentity` as trusted provenance for the approved next freshness composition without retaining paths, wrapper bytes, HMAC or database keys, envelope bytes, native errors, or loader state. It exposes no metadata, evidence, identity, generation, connection, handle, path, or detachable correspondence proof.

On mismatch, all temporary borrows end and both metadata and the trusted assessment are discarded before explicit close. Close success returns only coarse `DatabaseEvidenceCorrespondenceMismatch`. Close failure retains only that same category and the complete `ConnectionLifetimeOwner`; metadata and evidence never remain in the close-failure owner. Consuming retries attempt only close and preserve the category and lifetime owner on repeated failure. Closing a successful correspondence owner also discards metadata and the trusted assessment before close; failure reuses the existing capability-free `ProductionDatabaseConnectionCloseFailure` and retains only lifetime ownership.

The success owner, mismatch category, validation outcome, mismatch close-failure owner, and close-retry outcome use manual coarse `Debug`. They do not delegate to metadata, evidence, or trusted-assessment formatting. Debug, Display, ordinary logs, intentionally produced panic text, IPC, and frontend state reveal none of: the differing field; mismatch count or comparison order; identifiers; generations; timestamps; permanent application identifier; database-format identity; metadata/evidence values; evidence-format fields; paths or wrappers; DPAPI/HMAC state; SQL/PRAGMA text; SQLite/native errors or codes; handles; or connection details. Ordinary success, mismatch, and close failure require no log entry.

Success proves only continued satisfaction of the preceding database stages, exact correspondence of the approved six identity fields, continued ownership of the same guarded connection lifetime, retention of the compared trusted assessment, and internal availability of both generations for later freshness. It proves no equal lineage, freshness, rollback resistance, startup authorization, operational opening, setup completion, migration state, physical DDL or object kind, wider schema correctness, recovery fitness, backup/restore suitability, replacement authority, or business-data correctness.

Test construction uses only the `#[cfg(test)] pub(crate)` helper owned by the trusted-assessment module. It accepts synthetic structurally validated evidence, derives `TrustedCurrentInstallationIdentity` internally through the existing pure derivation, cannot accept an independently supplied identity, performs no filesystem, DPAPI, HMAC, loading, or parsing, is absent from production compilation, and grants no production authority.

### Implemented preloaded normalized freshness security boundary

The implemented adapter consumes `DatabaseEvidenceCorrespondenceValidatedProductionDatabaseConnection` and exactly one preloaded `NormalizedFreshnessAnchorObservation`. It accepts all four normalized states but no path, `FreshnessAnchorPersistencePaths`, `LoadedActiveFreshnessAnchorWrapperPair`, `AuthenticatedActiveFreshnessAnchor`, `InstallationBoundAuthenticatedActiveFreshnessAnchor`, bare `AssuredFreshnessAnchor`, or generic alternative. It has no presence-inspection, filesystem/path, wrapper-loading, DPAPI, HMAC, parsing, authentication, generation-matching, installation-binding, assurance, normalization, SQL, PRAGMA, query, row-read, database-content, schema-mutation, or migration capability.

Possession of the consumed correspondence owner is the proof used to construct one ephemeral `DatabaseMetadataCorrespondence::Corresponds` only for the exactly-once call to `classify_database_freshness`. The value is not retained, returned, logged, exposed, or added to the success owner, and correspondence is not recomputed. The pure freshness classifier remains the sole authority for generation ordering, three-way anchor identities, lineage combination, existing precedence, and the rule that timestamps cannot authorize freshness.

Only `Fresh` advances to opaque `DatabaseFreshnessValidatedProductionDatabaseConnection`; every other existing `DatabaseFreshnessClassification` variant is non-success and begins explicit close. The live primary category is the existing pure enum. Successful close returns the original non-Fresh classification; failed close retains that classification plus the complete lifetime owner. `CloseFailed` is ownership-bearing, not primary. `Fresh` is forbidden from every failure or retry form, and no non-Fresh result retains a usable database owner.

The success owner retains exactly `ConnectionLifetimeOwner`, `DatabaseMetadataContractV1`, and `TrustedCurrentInstallationEvidenceAssessment`. It retains no classification, correspondence enum, normalized observation, assured/authenticated/bound anchor, anchor contract or fields, path, wrappers, loader state, or protection errors. The supplied observation and any assured anchor are discarded after classification, including on success. No future startup consumer is assumed to require the anchor.

For non-success, all borrows end before metadata, trusted assessment, normalized observation, and any assured anchor are discarded. Only then is the lifetime owner closed. `ProductionDatabaseFreshnessValidationCloseFailure` retains exactly the copyable classification and complete `ConnectionLifetimeOwner`; retry performs only close, preserves both on repeated failure, and returns the same classification on eventual success. No metadata, assessment, trusted identity, observation, or anchor survives close failure. Successful-owner close discards metadata and assessment first and reuses `ProductionDatabaseConnectionCloseFailure`, retaining only lifetime ownership.

Manual coarse `Debug` may disclose only a payload-free non-Fresh classification name. Success owners, close-failure owners, and failed retries are redacted. Formatting and ordinary logs must not reveal identifiers, generations, timestamps, metadata, evidence, trusted identity, anchor values, wrapper or protection state, paths, DPAPI/HMAC detail, SQL/PRAGMA text, SQLite/native errors or codes, handles, connection details, or raw error chains. No ordinary success, non-success, or close-failure log is required.

The adapter resides in the private nested child `live_metadata_and_header_validation/database_evidence_correspondence_validation/database_freshness_validation.rs`, where it destructures the correspondence owner without widening production visibility. There is no crate-root bridge or arbitrary production `Connection` callback. Canonical lifetime-owner close paths are used; injected close callbacks are `cfg(test)`-only.

Freshness success proves only that the preceding guarded/keyed/readability/integrity/header/metadata/correspondence chain remains represented, correspondence had already succeeded, the pure classifier ran once with ephemeral `Corresponds`, the supplied observation was assured and present, its three-way identity checks passed, both lineages were `Current`, and the same guarded lifetime remains owned. It does not provide cryptographic monotonic rollback prevention, prove absolute newest state, detect a coordinated rollback to a mutually consistent older snapshot, grant startup or operational authority, establish setup or migration state, validate DDL/object-kind/wider schema correctness, authorize recovery/replacement, establish backup/restore suitability or business-data correctness, or guarantee validity after external change. No operational caller exists.

The anchor loader provides bounded, stable selection and validation of the current active wrapper pair. It does not guarantee absolute historical continuity or detection of every indistinguishable transient disappearance/recreation. Disappearance without replacement fails; after recreation the loader may fail or may succeed only with the fully validated current recreated pair. Stale or mixed-pair success remains forbidden. Neither loader success nor freshness success grants startup or operational authority.

### Implemented internal startup-authorization security boundary

The implemented adapter consumes exactly `DatabaseFreshnessValidatedProductionDatabaseConnection` plus one preloaded `installation_state::InstallationEvidence`. It performs exactly one policy evaluation and may advance only `InstallationEvidence::Initialized(ExpectedStorageEvidence::Present)`. It performs no hidden evidence loading and no filesystem, path, file-identity, sidecar, database, freshness-anchor, environment, current-account, administrator-group, standard-user, or elevation observation. It does not rerun freshness, correspondence, metadata/header, readability/integrity, database-file inspection, path/sidecar inspection, or anchor loading/normalization.

All other observations fail closed under four coarse categories: `NeverInitialized`, `ExpectedStorageMissing`, `InstallationStateInconsistent`, and `InstallationStateUnavailable`; both unavailable observations intentionally map to the last category. These categories carry no payload. `NeverInitialized` grants no setup permission. The adapter does not construct or call first-time setup authority, use `SetupAuthorizationState` or `StorageDecision`, treat `FutureOpenPermitted` as startup authority, create storage, or branch into setup.

On success, the supplied installation evidence is discarded and opaque `StartupAuthorizedProductionDatabaseConnection` retains exactly the unchanged `ConnectionLifetimeOwner`, `DatabaseMetadataContractV1`, and `TrustedCurrentInstallationEvidenceAssessment`. It retains no installation-state observation, storage/setup decision, authorization flag or enum, freshness classification, correspondence result, normalized or assured/authenticated/bound anchor, path, or sidecar observation.

On primary failure, the category is preserved; the supplied `InstallationEvidence` is discarded and not retained, while `DatabaseMetadataContractV1` and `TrustedCurrentInstallationEvidenceAssessment` are dropped before `ConnectionLifetimeOwner` is explicitly closed. Close success returns the original category. Close failure retains exactly the original `ProductionDatabaseStartupAuthorizationError` and complete lifetime owner. It retains no metadata, assessment, installation state or evidence, identity, path, sidecar fact, anchor, setup authority, or other capability.

The close-failure owner exposes a consuming retry that performs only close. Repeated failure preserves the original category and complete lifetime owner; eventual success returns the original category. Retry performs no authorization, observation, loading, database access, recovery, or setup. Closing a successful startup-authorized owner likewise destroys metadata and trusted assessment before explicit close, then reuses `ProductionDatabaseConnectionCloseOutcome` and `ProductionDatabaseConnectionCloseFailure`; successful-owner close failure retains only lifetime ownership.

Manual `Debug` is strictly coarse. Only `NeverInitialized`, `ExpectedStorageMissing`, `InstallationStateInconsistent`, and `InstallationStateUnavailable` may be visible as payload-free primary names. Owners and ownership-bearing failures remain redacted. Formatting must not delegate to metadata, trusted assessment, installation evidence, lifetime ownership, rusqlite, or native errors. No identifiers, generations, timestamps, metadata/evidence values, trusted identity, paths, sidecars, handles, SQL/PRAGMA text, native codes, connection detail, or raw error chains may escape. No ordinary success or failure logging is required.

The startup-authorized owner is not an operational database and exposes no connection, SQL/query interface, arbitrary callback, path, metadata, trusted-assessment, or evidence accessor. It grants no operational read/write SQL, DDL or wider-schema correctness, migration or migration authority, setup or storage creation, recovery, replacement, rekey, backup/restore, destructive cleanup, sidecar repair/deletion, WAL/SHM handling, business-data, account/elevation, stronger rollback/freshness, or continued-validity authority. Product direction for non-elevated ordinary use is not an observation or proof at this boundary. Whether a later operational-opening boundary requires new path or sidecar inspection after external change remains separately undecided.

The implementation resides in a private `database_freshness_validation/startup_authorization.rs` child declared by `database_freshness_validation.rs`. No crate-root bridge, visibility widening, generic or arbitrary `Connection` callback, production success constructor, unsafe/FFI, dependency, Cargo feature, Tauri/IPC/frontend surface, or ordinary logging was added.

## Controlled-host harness safety boundary

The Windows-test-only harness records no path, drive letter, volume GUID, device number, vendor/model/serial, username, SID, native code/text, environment-variable contents, or raw descriptor bytes. It never enumerates drives or sibling entries, formats or repartitions media, changes labels, reads unrelated content, retries with elevation, or converts a result into authority.

For the USB case it mutates only one exact `church-app-usb-flash-proof-{pid}-{nanos}-{counter}` child beneath the manually supplied root and creates only `unrelated-sentinel.synthetic` inside that child. The sentinel must remain unchanged. Cleanup is attempted once against that exact child only; cleanup failure remains a failure. All production-policy, persistence, database-opening, setup, startup, publication/replacement, and installation-state authority fields remain false.

The controlled-host prerequisite diagnostic exposes only fixed stage names, `Unavailable` or `Rejected`, fixed sentinel states (`NotReached`, `Preserved`, `Unavailable`, or `Changed`), Booleans, and bounded call counts. It exposes no path, drive letter, GUID or serial, filesystem name, file identity, link count, native detail, device detail, user identity, environment value, raw UTF-16, or sentinel bytes. Child creation and sentinel-write failures remain possible generally but cannot explain the recorded USB failure because fixture construction completed and exact-root cleanup later succeeded. No filesystem-format cause is claimed.

On the accepted diagnostic USB rerun, unavailable `FILE_ATTRIBUTE_TAG_INFO` facts caused `DirectoryAttributeInfoUnavailable` with disposition `Unavailable` and prevented local-volume and device-property classification. Drive-type, volume-open, property-IOCTL, and hot-plug counts remained zero; sentinel verification was not reached. Exact-root cleanup was attempted once and succeeded, and the manual leftover-folder check found no entries. No path, drive letter, filesystem name, GUID, device identity, native error, or other sensitive host fact was retained, and every production and operational authority field remained false.
